---
phase: 35.1-mission-control-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["35.1-02"]
files_modified:
  - src/app/missions/missions-client.tsx
  - src/components/missions/mission-card.tsx
autonomous: false
requirements: [MC-02, MC-05]

must_haves:
  truths:
    - "Each card connects its own WebSocket to a live PTY session and displays real terminal output"
    - "When a task execution completes (exit 0), the card shows Completed badge for 3 seconds then fades out"
    - "When user stops an execution, the card shows Stopped badge for 3 seconds then fades out"
    - "The terminal re-fits when grid layout preset changes"
    - "Navigating away from /missions clears all polling timers and fade timeouts"
  artifacts:
    - path: "src/app/missions/missions-client.tsx"
      provides: "Complete polling + lifecycle integration"
      contains: "clearInterval"
    - path: "src/components/missions/mission-card.tsx"
      provides: "Live terminal in card with session end handling"
      contains: "TaskTerminal"
  key_links:
    - from: "src/components/missions/mission-card.tsx"
      to: "src/components/task/task-terminal.tsx"
      via: "dynamic import TaskTerminal"
      pattern: "task-terminal"
    - from: "src/components/missions/mission-card.tsx"
      to: "ws://localhost:3001/terminal"
      via: "TaskTerminal WebSocket connection"
      pattern: "worktreePath"
---

<objective>
Final integration pass: verify full end-to-end lifecycle works — terminals render live output, card removal on completion/stop, timer cleanup, and visual polish. Human verification checkpoint.

Purpose: Ensure the Mission Control dashboard works as a real multi-terminal monitoring tool with correct lifecycle management.
Output: Fully functional /missions page ready for user testing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35.1-mission-control-dashboard/35.1-CONTEXT.md
@.planning/phases/35.1-mission-control-dashboard/35.1-UI-SPEC.md
@.planning/phases/35.1-mission-control-dashboard/35.1-01-SUMMARY.md
@.planning/phases/35.1-mission-control-dashboard/35.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end integration fixes and polish</name>
  <files>src/app/missions/missions-client.tsx, src/components/missions/mission-card.tsx</files>
  <read_first>
    - src/app/missions/missions-client.tsx (full file — verify polling, DndContext, fade-out logic)
    - src/components/missions/mission-card.tsx (full file — verify terminal integration, running time, badge states)
    - src/components/task/task-terminal.tsx (worktreePath guard at line 194, WebSocket connection flow)
    - .claude/rules/process-lifecycle.md (timer cleanup rules)
    - .planning/phases/35.1-mission-control-dashboard/35.1-UI-SPEC.md (interaction contract section)
  </read_first>
  <action>
Review and fix any integration issues between Plans 01+02:

1. **Verify timer cleanup completeness in missions-client.tsx**:
   - The polling `setInterval` must be cleared in its own useEffect cleanup via `clearInterval(timer)`
   - The `fadeTimers` Map must have ALL entries cleared via `fadeTimers.current.forEach(t => clearTimeout(t))` in a separate useEffect cleanup
   - The running time `setInterval(1000)` in MissionCard must be cleared in its useEffect cleanup AND when `isRemoving` becomes true

2. **Verify MissionCard terminal connection**:
   - TaskTerminal receives `worktreePath={execution.worktreePath ?? execution.projectLocalPath}` — ensures no "no worktree" state shows
   - TaskTerminal receives `taskId={execution.taskId}` — matches the PTY session key
   - The dynamic import has `ssr: false` and a loading skeleton `<div className="h-full w-full bg-[#0a0a0a] animate-pulse" />`

3. **Verify MissionCard fade-out badge states**:
   - When `isRemoving` is true, the status badge text should show `t("missions.statusCompleted")` (default) or `t("missions.statusStopped")` based on how removal was triggered
   - To distinguish: add a `removingStatus` state to MissionCard or pass it as prop. If too complex, just show `t("missions.statusCompleted")` for all removals (acceptable per Claude's discretion).

4. **Verify grid layout refit**:
   - When preset changes, CSS grid columns change → card containers resize → TaskTerminal's ResizeObserver fires → FitAddon.fit() called automatically
   - No manual intervention needed — verify the ResizeObserver in task-terminal.tsx handles this

5. **Run `pnpm test:run`** to confirm no regressions.

6. **Run `npx tsc --noEmit`** to confirm no type errors in new files.

7. Fix any issues found during verification. Common issues to check:
   - Missing imports
   - Type mismatches between ActiveExecutionInfo fields and component props
   - Incorrect shadcn component API usage (e.g., Tooltip render prop pattern must match app-sidebar.tsx pattern)
   - Missing `"use client"` directive
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && npx tsc --noEmit 2>&1 | head -20 && pnpm test:run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0 (no type errors in new mission files)
    - `pnpm test:run` passes
    - src/app/missions/missions-client.tsx contains `clearInterval` for polling timer
    - src/components/missions/mission-card.tsx contains `clearInterval` or `clearTimeout` for running time timer
    - src/components/missions/mission-card.tsx contains `ssr: false` in dynamic import
    - src/components/missions/mission-card.tsx contains `worktreePath ?? execution.projectLocalPath`
  </acceptance_criteria>
  <done>All timers have proper cleanup, terminal connections work via dynamic import, type checking passes, test suite green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Mission Control Dashboard</name>
  <files>src/app/missions/page.tsx</files>
  <action>
Present the completed Mission Control Dashboard to the user for visual and functional verification. The dashboard should be fully operational with live terminal cards, drag-and-drop reordering, grid layout presets, task launcher dialog, and stop/auto-remove lifecycle.

What was built:
- Mission Control Dashboard at /missions — multi-terminal grid with live PTY sessions
- Drag-and-drop card reorder via dnd-kit
- Grid layout presets (1x1 through 3x3) persisted in localStorage
- Task Picker dialog with cascading workspace/project/task selects
- Stop and auto-remove lifecycle with 3-second fade-out
- Sidebar navigation link in both collapsed and expanded views

How to verify:
1. Start the dev server: `pnpm dev`
2. Navigate to http://localhost:3000/missions
3. Verify the sidebar shows "Missions" / "任务控制台" link — click it
4. Verify the empty state shows "No running tasks" with a "Launch Task" button
5. Click "Launch Task" — verify the dialog opens with cascading selects
6. Select a workspace, then a project, then a TODO/IN_PROGRESS task, click "Launch"
7. Verify a card appears with breadcrumb header, running time ticking, RUNNING badge
8. Verify the terminal inside the card shows live output
9. Try switching grid layout presets (top right dropdown) — cards should re-layout
10. Try dragging a card by its handle — cards should reorder
11. Click the arrow icon on a card — should navigate to task detail page
12. Click the X icon on a card — should show badge briefly then fade out
13. Switch between collapsed and expanded sidebar — /missions link should appear in both
  </action>
  <verify>User confirms all 13 verification steps pass</verify>
  <done>User types "approved" confirming the Mission Control Dashboard is functional and visually correct</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `pnpm test:run` passes
3. Human verifies full end-to-end flow
</verification>

<success_criteria>
- Terminals render live PTY output in each card
- Stop and auto-remove lifecycle works with 3-second fade
- Grid layout presets change the grid
- Drag-and-drop reorders cards
- All timers cleaned up on navigation away
- No type errors, no test failures
- User approves the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/35.1-mission-control-dashboard/35.1-03-SUMMARY.md`
</output>
