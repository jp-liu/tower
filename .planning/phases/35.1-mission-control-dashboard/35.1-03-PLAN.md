---
phase: 35.1-mission-control-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["35.1-02"]
files_modified:
  - src/app/missions/missions-client.tsx
  - src/components/missions/mission-card.tsx
autonomous: false
requirements: [MC-02, MC-05]

must_haves:
  truths:
    - "Each card connects its own WebSocket to a live PTY session and displays real terminal output"
    - "When a task execution completes (exit 0), the card shows Completed badge for 3 seconds then fades out"
    - "When user stops an execution, the card shows Stopped badge for 3 seconds then fades out"
    - "The terminal re-fits when grid layout preset changes"
    - "Navigating away from /missions clears all polling timers and fade timeouts"
  artifacts:
    - path: "src/app/missions/missions-client.tsx"
      provides: "Complete polling + lifecycle integration"
      contains: "clearInterval"
    - path: "src/components/missions/mission-card.tsx"
      provides: "Live terminal in card with session end handling"
      contains: "TaskTerminal"
  key_links:
    - from: "src/components/missions/mission-card.tsx"
      to: "src/components/task/task-terminal.tsx"
      via: "dynamic import TaskTerminal"
      pattern: "task-terminal"
    - from: "src/components/missions/mission-card.tsx"
      to: "ws://localhost:3001/terminal"
      via: "TaskTerminal WebSocket connection"
      pattern: "worktreePath"
---

<objective>
Type-check and test the full integration, then human-verify the end-to-end Mission Control dashboard lifecycle.

Purpose: Ensure all plans compile cleanly and the dashboard works as a real multi-terminal monitoring tool.
Output: Fully functional /missions page confirmed by user testing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35.1-mission-control-dashboard/35.1-CONTEXT.md
@.planning/phases/35.1-mission-control-dashboard/35.1-UI-SPEC.md
@.planning/phases/35.1-mission-control-dashboard/35.1-01-SUMMARY.md
@.planning/phases/35.1-mission-control-dashboard/35.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type-check and test suite validation</name>
  <files>src/app/missions/missions-client.tsx, src/components/missions/mission-card.tsx</files>
  <read_first>
    - src/app/missions/missions-client.tsx (full file)
    - src/components/missions/mission-card.tsx (full file)
    - src/components/missions/task-picker-dialog.tsx (full file)
    - src/app/missions/page.tsx (full file)
  </read_first>
  <action>
Run `npx tsc --noEmit` and `pnpm test:run` against the full codebase. Fix any errors found.

**Expected outcomes (deterministic):**
- `npx tsc --noEmit` exits 0. If not, fix ALL type errors in `src/app/missions/**` and `src/components/missions/**` files.
- `pnpm test:run` exits 0. If not, fix failing tests.

**Common type issues to fix if found:**
1. Missing imports (e.g., `DragEndEvent` type from `@dnd-kit/core`)
2. Type mismatch between `ActiveExecutionInfo.startedAt` (string | null) and `new Date()` usage
3. Incorrect shadcn component prop types (e.g., Tooltip render prop)
4. `getWorkspacesWithProjects` may not exist yet -- if so, create it in `src/actions/workspace-actions.ts` as a simple `findMany` with `include: { projects: true }`
5. Missing `"use server"` directive in action files

**If both commands pass on first try:** No changes needed. Commit the clean result.
**If errors found:** Fix each error, re-run both commands, and iterate until both pass.
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && npx tsc --noEmit 2>&1 | tail -5 && pnpm test:run 2>&1 | tail -10</automated>
  </verify>
  <done>`npx tsc --noEmit` exits 0 with no type errors in mission files. `pnpm test:run` passes all tests including Wave 0 agent-actions and missions-merge tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Mission Control Dashboard</name>
  <files>src/app/missions/page.tsx</files>
  <action>
Present the completed Mission Control Dashboard to the user for visual and functional verification.

What was built:
- Mission Control Dashboard at /missions with multi-terminal grid and live PTY sessions
- Drag-and-drop card reorder via dnd-kit
- Grid layout presets (1x1 through 3x3) persisted in localStorage
- Task Picker dialog with cascading workspace/project/task selects
- Stop and auto-remove lifecycle with 3-second fade-out
- Sidebar navigation link in both collapsed and expanded views

How to verify:
1. Start the dev server: `pnpm dev`
2. Navigate to http://localhost:3000/missions
3. Verify the sidebar shows "Missions" / "任务控制台" link -- click it
4. Verify the empty state shows "No running tasks" with a "Launch Task" button
5. Click "Launch Task" -- verify the dialog opens with cascading selects
6. Select a workspace, then a project, then a TODO/IN_PROGRESS task, click "Launch"
7. Verify a card appears with breadcrumb header, running time ticking, RUNNING badge
8. Verify the terminal inside the card shows live output
9. Try switching grid layout presets (top right dropdown) -- cards should re-layout
10. Try dragging a card by its handle -- cards should reorder
11. Click the arrow icon on a card -- should navigate to task detail page
12. Click the X icon on a card -- should show badge briefly then fade out
13. Switch between collapsed and expanded sidebar -- /missions link should appear in both
  </action>
  <verify>User confirms all 13 verification steps pass</verify>
  <done>User types "approved" confirming the Mission Control Dashboard is functional and visually correct</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `pnpm test:run` passes
3. Human verifies full end-to-end flow
</verification>

<success_criteria>
- Terminals render live PTY output in each card
- Stop and auto-remove lifecycle works with 3-second fade
- Grid layout presets change the grid
- Drag-and-drop reorders cards
- All timers cleaned up on navigation away
- No type errors, no test failures
- User approves the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/35.1-mission-control-dashboard/35.1-03-SUMMARY.md`
</output>
