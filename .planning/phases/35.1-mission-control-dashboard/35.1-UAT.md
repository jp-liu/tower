---
status: complete
phase: 35.1-mission-control-dashboard
source: [35.1-01-SUMMARY.md, 35.1-02-SUMMARY.md, 35.1-03-SUMMARY.md]
started: 2026-04-13T17:30:00Z
updated: 2026-04-13T17:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Sidebar Navigation to Missions
expected: In the sidebar (both collapsed and expanded views), a "Missions" link with a Gauge icon is visible. Clicking it navigates to `/missions`. The link shows active state when on the missions page.
result: pass

### 2. Empty State on Missions Page
expected: When no task executions are currently running, the `/missions` page shows an empty state with a Rocket icon, a title, description text, and a "Launch Task" CTA button.
result: pass

### 3. Grid Layout Preset Selector
expected: A dropdown in the toolbar lets you choose from 6 grid presets (1×1, 2×1, 2×2, 3×2, 4×2, 3×3). Selecting a preset changes the card grid layout. Refreshing the page preserves the selected preset (localStorage persistence).
result: pass

### 4. Launch Task via TaskPickerDialog
expected: Clicking "Launch Task" opens a dialog with three cascading selects: Workspace → Project → Task. Only TODO and IN_PROGRESS tasks are shown. Selecting a task and clicking Launch starts an execution — the dialog closes and a new card appears in the grid.
result: issue
reported: "功能正常，但 IN_REVIEW 状态的任务也应该支持选择和启动"
severity: minor

### 5. MissionCard Terminal Display
expected: Each running execution shows as a card with an embedded terminal (xterm.js) displaying live Claude CLI output with ANSI colors. The card header shows a breadcrumb (Workspace > Project > Task), a RUNNING badge, live running time, and control buttons.
result: pass

### 6. Stop Task — Stopped Badge
expected: Clicking the stop (X) button on a card stops the execution. The card shows a "Stopped" badge (not "Completed") for approximately 3 seconds, then fades out and is removed from the grid.
result: pass

### 7. Natural Completion — Completed Badge
expected: When a task execution finishes naturally (Claude exits with code 0), the card shows a "Completed" badge for approximately 3 seconds, then fades out and is removed from the grid.
result: pass

### 8. Drag-and-Drop Card Reordering
expected: Cards have a drag handle (grip icon). Dragging a card to a different position in the grid reorders it. The new order is maintained across polling cycles (cards don't jump back).
result: pass

### 9. Open Full View Navigation
expected: Clicking the "Open full view" button (arrow icon) on a MissionCard navigates to the task's detail page at `/workspaces/[wsId]/tasks/[taskId]`.
result: pass

### 10. Live Polling Updates
expected: When a new task execution starts (from another tab or MCP), the missions page automatically detects it within ~4 seconds and adds a new card to the grid without manual refresh.
result: pass

## Summary

total: 10
passed: 9
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "TaskPicker should allow selecting IN_REVIEW tasks in addition to TODO and IN_PROGRESS"
  status: failed
  reason: "User reported: IN_REVIEW 状态的任务也应该支持选择和启动"
  severity: minor
  test: 4
  artifacts: [src/components/missions/task-picker-dialog.tsx]
  missing: [IN_REVIEW filter in task status filtering]
