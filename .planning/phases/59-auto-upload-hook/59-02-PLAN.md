---
phase: 59-auto-upload-hook
plan: 02
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - src/components/settings/system-config.tsx
  - src/app/api/internal/hooks/install/route.ts
  - src/lib/i18n/locales/zh.ts
  - src/lib/i18n/locales/en.ts
autonomous: true
requirements: [HOOK-03, HOOK-06]

must_haves:
  truths:
    - "Settings page shows auto-upload file types as editable comma-separated list"
    - "Settings page has install/uninstall hook button reflecting current state"
    - "Install button writes PostToolUse entry to ~/.claude/settings.json without destroying existing hooks"
    - "Uninstall button removes only the Tower hook entry"
  artifacts:
    - path: "src/components/settings/system-config.tsx"
      provides: "Hook config UI section"
      contains: "autoUploadTypes"
    - path: "src/app/api/internal/hooks/install/route.ts"
      provides: "Hook install/uninstall API"
      exports: ["POST", "DELETE", "GET"]
  key_links:
    - from: "src/components/settings/system-config.tsx"
      to: "/api/internal/hooks/install"
      via: "fetch POST/DELETE/GET"
      pattern: "hooks/install"
    - from: "src/app/api/internal/hooks/install/route.ts"
      to: "~/.claude/settings.json"
      via: "fs read/write"
      pattern: "settings\\.json"
---

<objective>
Add Settings UI for configuring auto-upload file types and installing/uninstalling the PostToolUse hook into Claude Code's settings.json.

Purpose: Users can control which file types are captured and manage hook lifecycle from Tower's settings page.
Output: Settings section with type whitelist editor and install button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/59-auto-upload-hook/59-01-SUMMARY.md
@src/components/settings/system-config.tsx
@src/actions/config-actions.ts
</context>

<interfaces>
From src/actions/config-actions.ts:
```typescript
export async function getConfigValue<T>(key: string, defaultValue: T): Promise<T>;
export async function setConfigValue(key: string, value: unknown): Promise<void>;
```

From scripts/post-tool-hook.js (Plan 01 output):
- Hook path: `{project_root}/scripts/post-tool-hook.js`
- Matcher: `Write|Edit|MultiEdit`
- Hook entry format in ~/.claude/settings.json:
```json
{
  "hooks": [{ "command": "node \"/path/to/scripts/post-tool-hook.js\"", "timeout": 10, "type": "command" }],
  "matcher": "Write|Edit|MultiEdit"
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Hook install/uninstall API and Settings UI section</name>
  <files>src/app/api/internal/hooks/install/route.ts, src/components/settings/system-config.tsx, src/lib/i18n/locales/zh.ts, src/lib/i18n/locales/en.ts</files>
  <read_first>src/components/settings/system-config.tsx, src/actions/config-actions.ts, src/lib/i18n/locales/zh.ts, src/lib/i18n/locales/en.ts</read_first>
  <action>
1. Create `src/app/api/internal/hooks/install/route.ts`:
   - `export const runtime = "nodejs"` and `export const dynamic = "force-dynamic"`
   - GET handler: reads `~/.claude/settings.json`, checks if Tower hook entry exists in `hooks.PostToolUse` array (match by command containing "post-tool-hook.js"). Returns `{ installed: boolean, hookPath: string }`.
   - POST handler: reads `~/.claude/settings.json`, parses JSON, appends Tower hook entry to `hooks.PostToolUse` array (creates array if absent). The hook entry:
     ```json
     {
       "hooks": [{ "command": "node \"{absolute_path_to_project}/scripts/post-tool-hook.js\"", "timeout": 10, "type": "command" }],
       "matcher": "Write|Edit|MultiEdit"
     }
     ```
     Get absolute project path from `process.cwd()`. Write back with JSON.stringify(data, null, 2). Return `{ success: true }`.
   - DELETE handler: reads settings.json, removes the Tower hook entry from PostToolUse array (filter by command containing "post-tool-hook.js"). Write back. Return `{ success: true }`.
   - Resolve settings path: `path.join(os.homedir(), ".claude", "settings.json")`
   - All handlers: NO requireLocalhost guard needed (this is a user-facing settings action, but keep it internal for now with localhost guard for safety).
   - Handle missing file gracefully (create with empty `{}` structure).

2. Add to `src/components/settings/system-config.tsx`:
   After the Missions Grid section, add a new "Hooks" section:

   a) **Auto-Upload File Types** — editable input field showing comma-separated extensions (e.g., "png, jpg, jpeg, gif, webp, svg, pdf, md, txt, json"). On save, split by comma, trim each, store as `hooks.autoUploadTypes` via `setConfigValue`. Load on mount via `getConfigValue("hooks.autoUploadTypes", ["png","jpg","jpeg","gif","webp","svg","pdf","md","txt","json"])`.

   b) **Install Hook Button** — on mount, GET `/api/internal/hooks/install` to check status. Show button text:
      - If not installed: "安装 Hook" (primary button)
      - If installed: "卸载 Hook" (destructive variant)
      Click calls POST or DELETE accordingly, then re-checks status.
      Below button, show small helper text explaining what the hook does.

3. Add i18n keys (both zh.ts and en.ts):
   - `settings.config.hooks.title`: "Hooks 配置" / "Hook Configuration"
   - `settings.config.hooks.desc`: "管理 Claude Code 的 PostToolUse 钩子" / "Manage Claude Code PostToolUse hooks"
   - `settings.config.hooks.autoUploadTypes`: "自动上传文件类型" / "Auto-upload File Types"
   - `settings.config.hooks.autoUploadTypesHint`: "逗号分隔的文件扩展名" / "Comma-separated file extensions"
   - `settings.config.hooks.install`: "安装 Hook" / "Install Hook"
   - `settings.config.hooks.uninstall`: "卸载 Hook" / "Uninstall Hook"
   - `settings.config.hooks.installed`: "Hook 已安装" / "Hook installed"
   - `settings.config.hooks.notInstalled`: "Hook 未安装" / "Hook not installed"
   - `settings.config.hooks.installHint`: "安装后，Claude Code 写入匹配类型的文件时会自动上传为任务资产" / "Once installed, files matching the configured types written by Claude Code will be auto-uploaded as task assets"
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && test -f src/app/api/internal/hooks/install/route.ts && grep -q "autoUploadTypes" src/components/settings/system-config.tsx && grep -q "hooks.install" src/lib/i18n/locales/zh.ts && pnpm tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Install route exists with GET/POST/DELETE handlers
    - Settings page has hooks section with type input and install button
    - i18n keys added for both zh and en
    - TypeScript compiles without errors
    - Install button reads/writes ~/.claude/settings.json correctly
    - Type whitelist stored as hooks.autoUploadTypes in SystemConfig
  </acceptance_criteria>
  <done>Settings UI shows hook config section with editable file types and install/uninstall button that correctly manages ~/.claude/settings.json</done>
</task>

</tasks>

<verification>
- Settings page renders hooks section without errors
- GET /api/internal/hooks/install returns correct installed state
- POST creates the hook entry, DELETE removes it
- Type whitelist is saved and loaded from SystemConfig
- `pnpm tsc --noEmit` passes
</verification>

<success_criteria>
- File types configurable in Settings and persisted in SystemConfig
- Install button writes correct PostToolUse entry to ~/.claude/settings.json
- Uninstall removes only the Tower entry, preserving other hooks
- Button reflects actual installation state on page load
</success_criteria>

<output>
After completion, create `.planning/phases/59-auto-upload-hook/59-02-SUMMARY.md`
</output>
