---
phase: 50-mcp-tools-test-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/tools/__tests__/report-tools.test.ts
autonomous: true
requirements: [COV-13]

must_haves:
  truths:
    - "daily_summary returns tasks grouped by workspace/project with completed and inProgress arrays"
    - "daily_summary respects date filter — only returns tasks active on the given date"
    - "daily_summary defaults to today when no date provided"
    - "daily_todo returns pending tasks sorted by priority severity (CRITICAL first)"
    - "daily_todo respects workspaceId filter"
    - "daily_todo respects projectId filter"
    - "daily_todo respects status filter array"
    - "daily_todo respects priority filter array"
    - "daily_todo defaults status filter to [TODO, IN_PROGRESS, IN_REVIEW]"
  artifacts:
    - path: "src/mcp/tools/__tests__/report-tools.test.ts"
      provides: "Unit tests for report-tools MCP handlers"
      min_lines: 100
  key_links:
    - from: "src/mcp/tools/__tests__/report-tools.test.ts"
      to: "src/mcp/tools/report-tools.ts"
      via: "import and mock verification"
      pattern: "vi\\.mock.*\\.\\..*db"
---

<objective>
Create unit tests for report-tools.ts MCP tool handlers.

Purpose: Verify daily_summary and daily_todo correctly filter by date/workspace/project/status/priority, group results by workspace/project hierarchy, and compute stats.
Output: One test file covering COV-13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/mcp/tools/report-tools.ts
@src/mcp/db.ts

<interfaces>
From src/mcp/tools/report-tools.ts:
```typescript
// daily_summary queries db.task.findMany with date range filter (gte/lt on updatedAt, executions.startedAt, messages.createdAt)
// Returns: { date, workspaces: [{ id, name, projects: [{ completed: [], inProgress: [] }] }], stats }

// daily_todo queries db.task.findMany with status/priority/projectId/workspaceId filters
// Sorts by PRIORITY_ORDER (CRITICAL=0, HIGH=1, MEDIUM=2, LOW=3) then updatedAt desc
// Returns: { date, workspaces: [...], stats: { total, byStatus, byPriority } }

const PRIORITY_ORDER: Record<string, number> = { CRITICAL: 0, HIGH: 1, MEDIUM: 2, LOW: 3 };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: report-tools.ts unit tests</name>
  <files>src/mcp/tools/__tests__/report-tools.test.ts</files>
  <read_first>
    - src/mcp/tools/report-tools.ts (full file — understand date logic, grouping, stats)
  </read_first>
  <behavior>
    daily_summary:
    - With specific date: db.task.findMany where clause uses date range [00:00, next day 00:00)
    - Without date: defaults to today
    - DONE tasks appear in completed array with completedAt
    - Non-DONE, non-CANCELLED tasks appear in inProgress array with progressSummary
    - CANCELLED tasks are excluded from both arrays
    - Stats include totalCompleted and totalInProgress counts
    - Empty result: returns empty workspaces array with zero stats

    daily_todo:
    - Default call: status filter is ["TODO", "IN_PROGRESS", "IN_REVIEW"]
    - With workspaceId: adds project.workspaceId to where clause
    - With projectId: adds projectId to where clause
    - With status filter: uses provided statuses
    - With priority filter: adds priority.in to where clause
    - Results sorted by priority severity then updatedAt desc
    - Stats include byStatus and byPriority breakdowns
    - lastSessionId extracted from most recent execution with non-null sessionId
  </behavior>
  <action>
    Create `src/mcp/tools/__tests__/report-tools.test.ts`.

    Mock `vi.mock("../db", ...)` with task model (findMany).

    Create helper factory functions:
    - `mockTask(overrides)` — returns a task object with sensible defaults including nested project.workspace, messages, executions, labels
    - Use `new Date()` objects for timestamps (not strings) since Prisma returns Date objects

    **daily_summary tests** (4 tests):
    1. With date param: verifies db.task.findMany called with date range in OR conditions; DONE task appears in completed, IN_PROGRESS in inProgress
    2. Without date param: verifies defaults to today's date boundaries
    3. CANCELLED task excluded: task with status CANCELLED appears in neither completed nor inProgress
    4. Empty tasks: returns { workspaces: [], stats: { totalCompleted: 0, totalInProgress: 0, totalActive: 0 } }

    **daily_todo tests** (5 tests):
    1. Default filters: verifies where clause has `status: { in: ["TODO", "IN_PROGRESS", "IN_REVIEW"] }`
    2. With workspaceId: verifies `where.project.workspaceId` is set
    3. With projectId: verifies `where.projectId` is set
    4. With priority filter: verifies `where.priority.in` contains specified priorities
    5. Stats computation: verifies byStatus and byPriority counts match returned tasks

    Important: db.task.findMany mock must return task objects with `.sort()` method available (return a real array). The handler calls `tasks.sort()` in-place for daily_todo.

    For daily_summary, the handler accesses `task.project.workspace` so mock tasks must have this nested structure.
  </action>
  <acceptance_criteria>
    - All 9 test cases pass
    - daily_summary date filter test verifies gte/lt boundaries passed to findMany
    - daily_todo filter tests verify each filter parameter is correctly passed to the where clause
    - Stats computation is verified against actual mock data counts
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm vitest run src/mcp/tools/__tests__/report-tools.test.ts</automated>
  </verify>
  <done>report-tools.ts has passing unit tests confirming daily_summary and daily_todo respect filter parameters and compute correct stats</done>
</task>

</tasks>

<verification>
cd /Users/liujunping/project/i/ai-manager && pnpm vitest run src/mcp/tools/__tests__/report-tools.test.ts
</verification>

<success_criteria>
- daily_summary tests confirm date range filtering and workspace/project grouping
- daily_todo tests confirm all 4 filter parameters (workspaceId, projectId, status, priority) are respected
- Stats computation matches mock data
- All tests pass with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/50-mcp-tools-test-coverage/50-03-SUMMARY.md`
</output>
