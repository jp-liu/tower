---
phase: 50-mcp-tools-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/tools/__tests__/workspace-tools.test.ts
  - src/mcp/tools/__tests__/label-tools.test.ts
  - src/mcp/tools/__tests__/terminal-tools.test.ts
autonomous: true
requirements: [COV-10, COV-11, COV-12]

must_haves:
  truths:
    - "workspace-tools CRUD delegates to db.workspace with correct args"
    - "workspace-tools delete calls db.workspace.delete which cascades via Prisma schema"
    - "label-tools set_task_labels deletes all existing labels then creates new ones in a transaction"
    - "label-tools set_task_labels with empty array deletes all labels without creating"
    - "terminal-tools get_output calls bridgeFetch with /buffer?lines=N"
    - "terminal-tools send_input calls bridgeFetch POST with /input and text body"
    - "terminal-tools get_status queries db.taskExecution then calls bridgeFetch for terminal state"
    - "terminal-tools validates CUID format and rejects invalid taskIds"
  artifacts:
    - path: "src/mcp/tools/__tests__/workspace-tools.test.ts"
      provides: "Unit tests for workspace-tools MCP handlers"
      min_lines: 50
    - path: "src/mcp/tools/__tests__/label-tools.test.ts"
      provides: "Unit tests for label-tools MCP handlers"
      min_lines: 50
    - path: "src/mcp/tools/__tests__/terminal-tools.test.ts"
      provides: "Unit tests for terminal-tools MCP handlers"
      min_lines: 80
  key_links:
    - from: "src/mcp/tools/__tests__/terminal-tools.test.ts"
      to: "src/mcp/tools/terminal-tools.ts"
      via: "mock fetch for HTTP bridge calls"
      pattern: "global\\.fetch.*mock"
---

<objective>
Create unit tests for workspace-tools.ts, label-tools.ts, and terminal-tools.ts MCP tool handlers.

Purpose: Verify workspace cascade delete delegation, label set_task_labels full replacement semantics, and terminal tools HTTP bridge endpoint calls with CUID validation.
Output: Three test files covering COV-10, COV-11, COV-12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/mcp/tools/workspace-tools.ts
@src/mcp/tools/label-tools.ts
@src/mcp/tools/terminal-tools.ts
@src/mcp/db.ts

<interfaces>
From src/mcp/tools/terminal-tools.ts:
```typescript
const PORT = process.env.PORT ?? "3000";
const BRIDGE_BASE = `http://localhost:${PORT}/api/internal/terminal`;
const CUID_RE = /^c[a-z0-9]{20,30}$/;

function validateMcpTaskId(taskId: string): string | null {
  if (!CUID_RE.test(taskId)) return "Invalid taskId format — expected CUID";
  return null;
}
```

From src/mcp/tools/label-tools.ts — set_task_labels uses $transaction:
```typescript
handler: async (args) => {
  await db.$transaction(async (tx) => {
    await tx.taskLabel.deleteMany({ where: { taskId: args.taskId } });
    if (args.labelIds.length > 0) {
      await tx.taskLabel.createMany({ data: ... });
    }
  });
  return { taskId: args.taskId, labelIds: args.labelIds };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: workspace-tools.ts and label-tools.ts unit tests</name>
  <files>src/mcp/tools/__tests__/workspace-tools.test.ts, src/mcp/tools/__tests__/label-tools.test.ts</files>
  <read_first>
    - src/mcp/tools/workspace-tools.ts (full file)
    - src/mcp/tools/label-tools.ts (full file)
  </read_first>
  <behavior>
    workspace-tools:
    - list_workspaces: calls db.workspace.findMany, returns projectCount from projects array length
    - create_workspace: calls db.workspace.create with name and optional description
    - update_workspace: calls db.workspace.update with correct where and data
    - delete_workspace: calls db.workspace.delete, returns { deleted: true, workspaceId }

    label-tools:
    - list_labels: calls db.label.findMany with OR condition (isBuiltin OR workspaceId match)
    - create_label: calls db.label.create with name, color, workspaceId
    - delete_label: calls db.label.delete, returns { deleted: true }
    - set_task_labels with non-empty labelIds: tx.taskLabel.deleteMany THEN tx.taskLabel.createMany in transaction
    - set_task_labels with empty labelIds: tx.taskLabel.deleteMany only, no createMany call
  </behavior>
  <action>
    Create `src/mcp/tools/__tests__/workspace-tools.test.ts`:
    - Mock `vi.mock("../db", ...)` with workspace model
    - 4 test cases: list (with projectCount mapping), create, update, delete
    - Delete test verifies db.workspace.delete is called (cascade is Prisma schema responsibility, but we confirm the delete call happens)

    Create `src/mcp/tools/__tests__/label-tools.test.ts`:
    - Mock `vi.mock("../db", ...)` with label and taskLabel models, plus `$transaction` that executes the callback with the mock as tx
    - 5 test cases: list, create, delete, set_task_labels with labels, set_task_labels with empty array
    - **Key COV-12 assertion**: set_task_labels with ["label1", "label2"] verifies deleteMany called first, then createMany with both IDs
    - **Key COV-12 assertion**: set_task_labels with [] verifies deleteMany called but createMany NOT called

    For $transaction mock, use: `$transaction: vi.fn((cb) => cb(mockDb.db))` so the callback receives the same mock object as `tx`.
  </action>
  <acceptance_criteria>
    - workspace-tools: 4 tests pass covering CRUD with projectCount
    - label-tools: 5 tests pass, set_task_labels verifies full replacement (deleteMany then createMany)
    - set_task_labels with empty array does NOT call createMany
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm vitest run src/mcp/tools/__tests__/workspace-tools.test.ts src/mcp/tools/__tests__/label-tools.test.ts</automated>
  </verify>
  <done>workspace-tools and label-tools have passing unit tests covering CRUD and label replacement semantics</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: terminal-tools.ts unit tests</name>
  <files>src/mcp/tools/__tests__/terminal-tools.test.ts</files>
  <read_first>
    - src/mcp/tools/terminal-tools.ts (full file — understand bridgeFetch, CUID validation, all 4 handlers)
  </read_first>
  <behavior>
    - start_task_execution: POST to /{taskId}/start with prompt body, returns { ok: true, ...data } on success
    - start_task_execution error: returns error object with status on non-ok response
    - get_task_terminal_output: GET /{taskId}/buffer?lines=N, returns lines/total/killed on 200
    - get_task_terminal_output 404: returns "No active terminal session" error
    - get_task_terminal_output default lines: uses 50 when lines param not provided
    - send_task_terminal_input: POST /{taskId}/input with text body, returns { ok: true } on success
    - send_task_terminal_input 404: returns "No active terminal session" error
    - send_task_terminal_input 410: returns "Terminal session has exited" error
    - get_task_execution_status: queries db.taskExecution.findFirst, then GET /{taskId}/buffer?lines=10
    - get_task_execution_status no execution: returns error "No execution found"
    - get_task_execution_status buffer 404 + COMPLETED status: terminalStatus = "exited"
    - get_task_execution_status buffer 404 + RUNNING status: terminalStatus = "not_running"
    - get_task_execution_status buffer ok + killed=true: terminalStatus = "exited"
    - get_task_execution_status buffer ok + killed=false: terminalStatus = "running" with outputSnippet
    - CUID validation: invalid taskId returns error immediately without calling fetch
  </behavior>
  <action>
    Create `src/mcp/tools/__tests__/terminal-tools.test.ts`.

    Mock setup:
    1. `vi.mock("../db", ...)` — mock db.taskExecution.findFirst for get_task_execution_status
    2. Mock `global.fetch` via `vi.fn()` — all handlers use `bridgeFetch` which calls `fetch(BRIDGE_BASE + path)`
    3. Set `process.env.PORT = "3000"` in beforeAll (or rely on default)

    Helper: create a `mockFetchResponse(status, data)` helper returning `{ ok: status < 400, status, json: () => Promise.resolve(data) }`.

    Test groups:
    - **CUID validation** (1 test): call any handler with `taskId: "invalid"` -> returns error without fetch call
    - **start_task_execution** (2 tests): success returns merged data; error returns status
    - **get_task_terminal_output** (3 tests): success, 404, default lines=50
    - **send_task_terminal_input** (3 tests): success, 404, 410
    - **get_task_execution_status** (4 tests): no execution, buffer 404 + COMPLETED, buffer 404 + RUNNING, buffer ok + running

    Verify fetch URLs contain `http://localhost:3000/api/internal/terminal/{taskId}/...` pattern.
    Verify CUID validation rejects IDs like "invalid", "123", "not-a-cuid".
  </action>
  <acceptance_criteria>
    - All 13+ test cases pass
    - CUID validation test confirms fetch is NOT called for invalid taskId
    - Each handler test verifies the correct HTTP bridge endpoint path
    - get_task_execution_status tests cover all terminalStatus branches (running, exited, not_running)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm vitest run src/mcp/tools/__tests__/terminal-tools.test.ts</automated>
  </verify>
  <done>terminal-tools.ts has passing unit tests confirming HTTP bridge endpoint calls, CUID validation, and all status branches</done>
</task>

</tasks>

<verification>
cd /Users/liujunping/project/i/ai-manager && pnpm vitest run src/mcp/tools/__tests__/workspace-tools.test.ts src/mcp/tools/__tests__/label-tools.test.ts src/mcp/tools/__tests__/terminal-tools.test.ts
</verification>

<success_criteria>
- workspace-tools tests verify CRUD delegation and cascade delete call
- label-tools tests verify set_task_labels full replacement (not merge)
- terminal-tools tests verify correct HTTP bridge endpoint paths and CUID validation
- All tests pass with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/50-mcp-tools-test-coverage/50-02-SUMMARY.md`
</output>
