---
phase: 50-mcp-tools-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/tools/__tests__/task-tools.test.ts
  - src/mcp/tools/__tests__/project-tools.test.ts
autonomous: true
requirements: [COV-08, COV-09]

must_haves:
  truths:
    - "task-tools create_task handler delegates to db.task.create with correct priority/status defaults"
    - "task-tools create_task copies reference files to assets dir with UUID stripping for cache paths"
    - "task-tools create_task auto-starts execution via HTTP bridge when autoStart=true"
    - "task-tools create_task auto-detects baseBranch via git when useWorktree=true and no baseBranch given"
    - "task-tools update_task replaces all labels in a transaction when labelIds provided"
    - "task-tools move_task updates status field"
    - "task-tools delete_task removes task and returns confirmation"
    - "project-tools create_project derives type=GIT when gitUrl present, NORMAL otherwise"
    - "project-tools list_projects includes task and repository counts"
  artifacts:
    - path: "src/mcp/tools/__tests__/task-tools.test.ts"
      provides: "Unit tests for task-tools MCP handlers"
      min_lines: 150
    - path: "src/mcp/tools/__tests__/project-tools.test.ts"
      provides: "Unit tests for project-tools MCP handlers"
      min_lines: 60
  key_links:
    - from: "src/mcp/tools/__tests__/task-tools.test.ts"
      to: "src/mcp/tools/task-tools.ts"
      via: "import and mock verification"
      pattern: "vi\\.mock.*\\.\\..*db"
    - from: "src/mcp/tools/__tests__/project-tools.test.ts"
      to: "src/mcp/tools/project-tools.ts"
      via: "import and mock verification"
      pattern: "vi\\.mock.*\\.\\..*db"
---

<objective>
Create unit tests for task-tools.ts and project-tools.ts MCP tool handlers.

Purpose: Verify that task CRUD operations correctly delegate to Prisma, handle references with UUID stripping, support worktree mode with auto-detected baseBranch, and auto-start execution. Verify project type derivation from gitUrl.
Output: Two test files covering COV-08 and COV-09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/mcp/tools/task-tools.ts
@src/mcp/tools/project-tools.ts
@src/mcp/db.ts

<interfaces>
<!-- MCP tools import db from their own db.ts, NOT from @/lib/db -->

From src/mcp/db.ts:
```typescript
import { PrismaClient } from "@prisma/client";
export const db = new PrismaClient();
```

From src/mcp/tools/task-tools.ts — imports to mock:
```typescript
import { execFileSync } from "child_process";
import { copyFileSync, existsSync, statSync, mkdirSync } from "fs";
import { db } from "../db";
import { stripCacheUuidSuffix, isAssistantCachePath } from "@/lib/file-utils";
```

From src/mcp/tools/project-tools.ts:
```typescript
import { db } from "../db";
```

Pattern from Phase 49 tests — vi.mock hoisting before imports:
```typescript
vi.mock("../db", () => ({ db: { task: { create: vi.fn(), ... } } }));
// then import { taskTools } from "../task-tools";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: task-tools.ts unit tests</name>
  <files>src/mcp/tools/__tests__/task-tools.test.ts</files>
  <read_first>
    - src/mcp/tools/task-tools.ts (full file — understand all 5 handlers)
    - src/actions/__tests__/workspace-actions.test.ts (first 60 lines — mock pattern reference)
    - src/lib/file-utils.ts (stripCacheUuidSuffix, isAssistantCachePath signatures)
  </read_first>
  <behavior>
    - list_tasks: calls db.task.findMany with projectId filter and status filter when provided, flattens labels
    - create_task: calls db.task.create with defaults (MEDIUM priority, TODO status)
    - create_task with labelIds: calls db.taskLabel.createMany after task creation
    - create_task with references: copies files via copyFileSync, creates ProjectAsset records, strips UUID for cache paths
    - create_task with references collision: uses counter suffix "(1)" for cache files, timestamp suffix for non-cache
    - create_task with useWorktree=true and no baseBranch: calls execFileSync("git", ["branch", "--show-current"]) on project localPath
    - create_task with explicit baseBranch: uses provided baseBranch, skips git detection
    - create_task with autoStart=true: calls fetch to /api/internal/terminal/{taskId}/start
    - create_task with autoStart=false: does NOT call fetch
    - update_task: calls db.task.update; when labelIds provided, deletes existing then creates new in transaction
    - move_task: calls db.task.update with new status
    - delete_task: calls db.task.delete, returns { deleted: true, taskId }
  </behavior>
  <action>
    Create `src/mcp/tools/__tests__/task-tools.test.ts`.

    Mock setup (vi.mock calls BEFORE imports, per Phase 49 decision):
    1. `vi.mock("../db", ...)` — mock db with task, taskLabel, projectAsset, project models, plus `$transaction` that executes the callback with `tx` (same mock)
    2. `vi.mock("child_process", ...)` — mock execFileSync
    3. `vi.mock("fs", ...)` — mock existsSync, statSync, copyFileSync, mkdirSync
    4. `vi.mock("@/lib/file-utils", ...)` — mock stripCacheUuidSuffix, isAssistantCachePath

    Mock global fetch for autoStart tests.

    Test cases:
    - **list_tasks** (2 tests): basic list with label flattening; with status filter
    - **create_task** (6 tests):
      - Basic create with defaults (MEDIUM, TODO)
      - With labelIds creates TaskLabel records
      - With references: existsSync true, statSync returns file, isAssistantCachePath true -> stripCacheUuidSuffix called, copyFileSync called, ProjectAsset created
      - With references collision: existsSync returns true for dest -> uses counter suffix
      - With useWorktree=true, no baseBranch: reads project.localPath, calls execFileSync
      - With autoStart=true: global.fetch called with correct URL and prompt
    - **update_task** (2 tests): basic update; with labelIds triggers deleteMany + createMany
    - **move_task** (1 test): updates status
    - **delete_task** (1 test): deletes and returns confirmation

    Use `beforeEach(() => vi.clearAllMocks())` to reset between tests.
  </action>
  <acceptance_criteria>
    - All 12+ test cases pass
    - create_task reference handling tests verify copyFileSync destination and ProjectAsset data
    - autoStart test verifies fetch URL includes task ID and body includes prompt
    - useWorktree test verifies execFileSync called with correct cwd
    - update_task with labelIds verifies transaction callback executes deleteMany then createMany
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm vitest run src/mcp/tools/__tests__/task-tools.test.ts</automated>
  </verify>
  <done>task-tools.ts has passing unit tests covering create (references, worktree, autoStart), update (labels), move, delete</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: project-tools.ts unit tests</name>
  <files>src/mcp/tools/__tests__/project-tools.test.ts</files>
  <read_first>
    - src/mcp/tools/project-tools.ts (full file)
  </read_first>
  <behavior>
    - list_projects: calls db.project.findMany with workspaceId, returns taskCount and repositoryCount
    - create_project with gitUrl: sets type to "GIT"
    - create_project without gitUrl: sets type to "NORMAL"
    - create_project with alias: passes alias to db.project.create
    - update_project: spreads data fields to db.project.update, excludes projectId from data
    - delete_project: calls db.project.delete, returns { deleted: true, projectId }
  </behavior>
  <action>
    Create `src/mcp/tools/__tests__/project-tools.test.ts`.

    Mock `vi.mock("../db", ...)` with project model (findMany, create, update, delete).

    Test cases:
    - **list_projects** (1 test): returns mapped results with taskCount/repositoryCount from array lengths
    - **create_project with gitUrl** (1 test): verifies db.project.create called with `type: "GIT"`
    - **create_project without gitUrl** (1 test): verifies db.project.create called with `type: "NORMAL"`
    - **create_project with alias** (1 test): verifies alias passed through
    - **update_project** (1 test): verifies projectId excluded from update data, remaining fields spread
    - **delete_project** (1 test): verifies delete called and returns confirmation

    Key assertion for COV-09: The type field is ALWAYS derived from gitUrl presence in the handler, never passed as an arg. Verify `create` is called with `type: "GIT"` when gitUrl is present and `type: "NORMAL"` when absent — the caller cannot override this.
  </action>
  <acceptance_criteria>
    - All 6 test cases pass
    - Type derivation tests explicitly verify type is "GIT" when gitUrl present, "NORMAL" when absent
    - list_projects test verifies taskCount/repositoryCount are computed from array lengths
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm vitest run src/mcp/tools/__tests__/project-tools.test.ts</automated>
  </verify>
  <done>project-tools.ts has passing unit tests confirming type derivation from gitUrl and CRUD delegation</done>
</task>

</tasks>

<verification>
cd /Users/liujunping/project/i/ai-manager && pnpm vitest run src/mcp/tools/__tests__/task-tools.test.ts src/mcp/tools/__tests__/project-tools.test.ts
</verification>

<success_criteria>
- task-tools tests cover create (references, worktree, autoStart), update (label replacement), move, delete
- project-tools tests confirm type derived from gitUrl, never set independently
- All tests pass with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/50-mcp-tools-test-coverage/50-01-SUMMARY.md`
</output>
