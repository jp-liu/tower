---
phase: 49-server-actions-test-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/__tests__/asset-actions.test.ts
  - src/actions/__tests__/report-actions.test.ts
autonomous: true
requirements: [COV-05, COV-07]

must_haves:
  truths:
    - "asset-actions uploadAsset validates file size against config limit"
    - "asset-actions uploadAsset rejects invalid projectId"
    - "asset-actions deleteAsset removes file from disk before DB delete"
    - "report-actions getDailySummary groups tasks by workspace and project"
    - "report-actions getDailyTodo sorts by priority severity (CRITICAL first)"
    - "report-actions getDailyTodo respects filter parameters"
  artifacts:
    - path: "src/actions/__tests__/asset-actions.test.ts"
      provides: "asset-actions unit tests"
      contains: "uploadAsset"
    - path: "src/actions/__tests__/report-actions.test.ts"
      provides: "report-actions unit tests"
      contains: "getDailySummary"
  key_links:
    - from: "src/actions/__tests__/asset-actions.test.ts"
      to: "src/actions/asset-actions.ts"
      via: "import and vi.mock of @/lib/db, node:fs"
      pattern: "vi\\.mock.*@/lib/db"
    - from: "src/actions/__tests__/report-actions.test.ts"
      to: "src/actions/report-actions.ts"
      via: "import and vi.mock of @/lib/db"
      pattern: "vi\\.mock.*@/lib/db"
---

<objective>
Unit tests for asset-actions and report-actions server action modules.

Purpose: Cover asset file operations with I/O mocking (COV-05) and report aggregation queries with complex grouping logic (COV-07).
Output: Two test files in src/actions/__tests__/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/actions/asset-actions.ts
@src/actions/report-actions.ts
@src/lib/db.ts
@src/lib/file-utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: asset-actions tests</name>
  <files>src/actions/__tests__/asset-actions.test.ts</files>
  <read_first>src/actions/asset-actions.ts, src/lib/file-utils.ts, src/actions/config-actions.ts</read_first>
  <action>
Create unit tests for asset-actions.ts by mocking `@/lib/db`, `next/cache`, `node:fs`, `@/lib/file-utils`, and `@/actions/config-actions`.

**Mock setup:**
```typescript
vi.mock("@/lib/db", () => ({
  db: {
    projectAsset: { create: vi.fn(), delete: vi.fn(), findUnique: vi.fn(), findMany: vi.fn() },
    project: { findUnique: vi.fn() },
  },
}));
vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }));
vi.mock("node:fs", async (importOriginal) => {
  const actual = await importOriginal<typeof import("node:fs")>();
  return {
    ...actual,
    existsSync: vi.fn(() => false),
    promises: { unlink: vi.fn(), writeFile: vi.fn() },
  };
});
vi.mock("@/lib/file-utils", () => ({
  ensureAssetsDir: vi.fn(() => "/mock/assets/dir"),
}));
vi.mock("@/actions/config-actions", () => ({
  getConfigValue: vi.fn(() => 52428800), // 50MB default
}));
```

**Tests** (per COV-05):
- `createAsset`: verify schema validation, db.projectAsset.create called, revalidatePath
- `createAsset` validation error: empty filename should throw
- `deleteAsset` with file: mock findUnique returning asset with path, verify fs.promises.unlink called, then db delete
- `deleteAsset` without file: mock findUnique returning asset with null path, verify unlink NOT called, db delete still called
- `getProjectAssets`: verify findMany with projectId and taskId: null filter
- `getTaskAssets`: verify findMany with taskId filter
- `getAssetById`: verify findUnique
- `uploadAsset` happy path: create a mock FormData with file (use a minimal File-like object), mock project.findUnique returning a project, verify writeFile + createAsset flow
- `uploadAsset` missing file: FormData without file, expect error "Missing file or projectId"
- `uploadAsset` file too large: mock getConfigValue returning small limit (100), create file larger than that, expect error about "File too large"
- `uploadAsset` invalid projectId: mock project.findUnique returning null, expect error "Invalid projectId"
- `uploadAsset` filename sanitization: file with `../../../etc/passwd` name, verify path.basename strips directory components

Note: For FormData mock in Node/Vitest, create a minimal mock. The `File` constructor may not be available — use a Blob-based approach or create a simple mock object with `name`, `size`, `type`, and `arrayBuffer()` method.
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm vitest run src/actions/__tests__/asset-actions.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
- asset-actions.test.ts has at least 10 test cases
- All tests pass with 0 failures
- grep "uploadAsset" src/actions/__tests__/asset-actions.test.ts returns at least 4 matches (multiple upload scenarios)
- grep "unlink" src/actions/__tests__/asset-actions.test.ts returns at least 1 match (file deletion test)
- grep "too large\|Invalid projectId" src/actions/__tests__/asset-actions.test.ts returns at least 2 matches
  </acceptance_criteria>
  <done>asset-actions has passing tests covering CRUD, uploadAsset size validation, projectId validation, filename sanitization, and file deletion cleanup</done>
</task>

<task type="auto">
  <name>Task 2: report-actions tests</name>
  <files>src/actions/__tests__/report-actions.test.ts</files>
  <read_first>src/actions/report-actions.ts</read_first>
  <action>
Create unit tests for report-actions.ts by mocking `@/lib/db`.

**Mock setup:**
```typescript
vi.mock("@/lib/db", () => ({
  db: {
    task: { findMany: vi.fn() },
  },
}));
```

**Tests** (per COV-07):

For `getDailySummary`:
- Happy path: mock task.findMany returning 2 tasks (one DONE, one IN_PROGRESS) with nested project.workspace. Verify return has `workspaces` array with correct grouping, `stats.totalCompleted` = 1, `stats.totalInProgress` = 1.
- Empty day: mock findMany returning [], verify workspaces is empty array, stats are all 0
- Custom date: pass dateStr "2026-01-15", verify the date range filter passed to findMany spans that day (gte 2026-01-15T00:00, lt 2026-01-16T00:00)
- CANCELLED tasks excluded: mock task with status CANCELLED, verify it appears in neither completed nor inProgress
- Progress summary from execution.summary: mock task with execution having summary field, verify progressSummary uses execution summary
- Progress summary fallback to message content: mock task with no execution summary but with a message, verify progressSummary truncates at 200 chars with "..."

For `getDailyTodo`:
- Happy path: mock findMany returning tasks with different priorities, verify returned tasks sorted by priority (CRITICAL first)
- Filter by workspaceId: call with `{ workspaceId: "ws1" }`, verify findMany where clause includes `project: { workspaceId: "ws1" }`
- Filter by projectId: call with `{ projectId: "p1" }`, verify where includes projectId
- Filter by priority: call with `{ priority: ["CRITICAL", "HIGH"] }`, verify where includes priority filter
- Stats breakdown: verify byStatus and byPriority counts match the mocked data
- Default status filter: call without filters, verify status filter defaults to ["TODO", "IN_PROGRESS", "IN_REVIEW"]

Mock task objects need nested `project: { workspace: { id, name }, id, name, alias }`, `labels: [{ label: { name } }]`, and `executions: [{ sessionId }]` to match the include clause.
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm vitest run src/actions/__tests__/report-actions.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
- report-actions.test.ts has at least 10 test cases
- All tests pass with 0 failures
- grep "getDailySummary" src/actions/__tests__/report-actions.test.ts returns at least 4 matches
- grep "getDailyTodo" src/actions/__tests__/report-actions.test.ts returns at least 4 matches
- grep "CRITICAL\|priority" src/actions/__tests__/report-actions.test.ts returns matches for priority sorting tests
  </acceptance_criteria>
  <done>report-actions has passing tests covering getDailySummary grouping/stats/date-filtering and getDailyTodo priority sorting/filter parameters/stats breakdown</done>
</task>

</tasks>

<verification>
All test files pass:
```bash
pnpm vitest run src/actions/__tests__/asset-actions.test.ts src/actions/__tests__/report-actions.test.ts
```
</verification>

<success_criteria>
- 2 test files created, all passing
- asset-actions: uploadAsset size/projectId/filename validation, deleteAsset file cleanup tested
- report-actions: grouping by workspace/project, priority sorting, filter parameters, stats accuracy tested
- Each module has multiple error/edge case tests
</success_criteria>

<output>
After completion, create `.planning/phases/49-server-actions-test-coverage/49-03-SUMMARY.md`
</output>
