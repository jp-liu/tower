---
phase: 49-server-actions-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/__tests__/prompt-actions.test.ts
  - src/actions/__tests__/cli-profile-actions.test.ts
autonomous: true
requirements: [COV-04, COV-06]

must_haves:
  truths:
    - "prompt-actions setDefaultPrompt unsets previous default before setting new one"
    - "prompt-actions content length validation rejects oversized content"
    - "cli-profile-actions command allowlist blocks non-whitelisted commands"
    - "cli-profile-actions envVars blocks dangerous system keys"
    - "cli-profile-actions baseArgs rejects shell metacharacters"
  artifacts:
    - path: "src/actions/__tests__/prompt-actions.test.ts"
      provides: "prompt-actions unit tests"
      contains: "setDefaultPrompt"
    - path: "src/actions/__tests__/cli-profile-actions.test.ts"
      provides: "cli-profile-actions unit tests"
      contains: "BLOCKED_ENV_KEYS\\|ALLOWED_COMMANDS\\|不允许"
  key_links:
    - from: "src/actions/__tests__/prompt-actions.test.ts"
      to: "src/actions/prompt-actions.ts"
      via: "import and vi.mock of @/lib/db"
      pattern: "vi\\.mock.*@/lib/db"
    - from: "src/actions/__tests__/cli-profile-actions.test.ts"
      to: "src/actions/cli-profile-actions.ts"
      via: "import and vi.mock of @/lib/db"
      pattern: "vi\\.mock.*@/lib/db"
---

<objective>
Unit tests for prompt-actions and cli-profile-actions server action modules.

Purpose: Cover the two modules with "default uniqueness" logic and security validation (COV-04, COV-06). Both modules enforce business rules: prompt default toggle and CLI profile command/env security.
Output: Two test files in src/actions/__tests__/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/actions/prompt-actions.ts
@src/actions/cli-profile-actions.ts
@src/lib/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: prompt-actions tests</name>
  <files>src/actions/__tests__/prompt-actions.test.ts</files>
  <read_first>src/actions/prompt-actions.ts, src/lib/db.ts</read_first>
  <action>
Create unit tests for prompt-actions.ts by mocking `@/lib/db` and `next/cache`.

**Mock setup:**
```typescript
vi.mock("@/lib/db", () => ({
  db: {
    agentPrompt: { findMany: vi.fn(), findUnique: vi.fn(), create: vi.fn(), update: vi.fn(), delete: vi.fn(), updateMany: vi.fn() },
    $transaction: vi.fn((fn) => fn(mockTx)),
  },
}));
vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }));
```

The `mockTx` object needs `agentPrompt: { updateMany, update }` for the setDefaultPrompt transaction.

**Tests** (per COV-04):
- `getPrompts` with workspaceId: verify OR query includes both workspace-specific and global (null) prompts
- `getPrompts` without workspaceId: verify empty where clause (returns all)
- `getPromptById`: verify findUnique with correct id
- `createPrompt`: verify create called with data, verify revalidatePath called for "/workspaces" and "/settings"
- `createPrompt` content too long: create content > 100000 chars, expect error about maximum length
- `updatePrompt`: verify update called, verify revalidatePath
- `updatePrompt` content too long: expect error about maximum length
- `deletePrompt`: verify delete called, verify revalidatePath
- `setDefaultPrompt` (KEY TEST): verify transaction calls updateMany to unset previous defaults FIRST, then update to set new default. Mock the transaction to capture both calls and verify order.
- `setDefaultPrompt` with workspaceId: verify updateMany where clause includes workspaceId filter (scoped unset)
- `setDefaultPrompt` without workspaceId: verify updateMany where clause only filters by isDefault (global unset)
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm vitest run src/actions/__tests__/prompt-actions.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
- prompt-actions.test.ts has at least 10 test cases
- All tests pass with 0 failures
- grep "setDefaultPrompt" src/actions/__tests__/prompt-actions.test.ts returns at least 3 matches
- grep "updateMany" src/actions/__tests__/prompt-actions.test.ts returns at least 2 matches (verifying unset-previous behavior)
- grep "100.000\|MAX_PROMPT" src/actions/__tests__/prompt-actions.test.ts returns at least 1 match (content length test)
  </acceptance_criteria>
  <done>prompt-actions has passing tests covering CRUD, content length validation, and setDefaultPrompt unset-previous-then-set-new behavior</done>
</task>

<task type="auto">
  <name>Task 2: cli-profile-actions tests</name>
  <files>src/actions/__tests__/cli-profile-actions.test.ts</files>
  <read_first>src/actions/cli-profile-actions.ts</read_first>
  <action>
Create unit tests for cli-profile-actions.ts by mocking `@/lib/db` and `next/cache`.

**Mock setup:**
```typescript
vi.mock("@/lib/db", () => ({
  db: {
    cliProfile: { findFirst: vi.fn(), update: vi.fn() },
  },
}));
vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }));
```

**Tests** (per COV-06):
- `getDefaultCliProfile` found: mock findFirst returning a profile, verify returned shape has id/name/command/baseArgs/envVars
- `getDefaultCliProfile` not found: mock findFirst returning null, verify returns null
- `updateCliProfile` happy path: valid command "claude", valid baseArgs `'["--model","opus"]'`, valid envVars `'{"FOO":"bar"}'` — verify update called
- `updateCliProfile` command validation: command "bash" should throw (not in allowlist)
- `updateCliProfile` command with path: command "/usr/bin/claude" should pass (basename is "claude")
- `updateCliProfile` baseArgs invalid JSON: pass "not json" — expect error about JSON array
- `updateCliProfile` baseArgs not array: pass `'"string"'` — expect error about JSON array
- `updateCliProfile` baseArgs metacharacters: pass `'["--flag;rm -rf"]'` — expect error about disallowed characters
- `updateCliProfile` baseArgs non-string element: pass `'[123]'` — expect error about string
- `updateCliProfile` envVars invalid JSON: expect error about JSON object
- `updateCliProfile` envVars array: pass `'[]'` — expect error about JSON object
- `updateCliProfile` envVars blocked key PATH: pass `'{"PATH":"/evil"}'` — expect error about system critical variable
- `updateCliProfile` envVars blocked key NODE_OPTIONS: pass `'{"NODE_OPTIONS":"--inspect"}'` — expect error
- `updateCliProfile` envVars blocked key lowercase: pass `'{"path":"/evil"}'` — verify behavior (the code uses `key.toUpperCase()` so lowercase "path" should also be blocked)
- `updateCliProfile` P2025 error: mock update throwing `{ code: "P2025" }`, expect user-friendly error "CLI Profile 不存在"
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm vitest run src/actions/__tests__/cli-profile-actions.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
- cli-profile-actions.test.ts has at least 12 test cases
- All tests pass with 0 failures
- grep "ALLOWED_COMMANDS\|allowlist\|claude" src/actions/__tests__/cli-profile-actions.test.ts returns matches for command validation tests
- grep "PATH\|NODE_OPTIONS\|BLOCKED" src/actions/__tests__/cli-profile-actions.test.ts returns matches for env var blocking tests
- grep "P2025\|不存在" src/actions/__tests__/cli-profile-actions.test.ts returns at least 1 match
  </acceptance_criteria>
  <done>cli-profile-actions has passing tests covering getDefaultCliProfile, updateCliProfile command allowlist, baseArgs JSON/metachar validation, envVars blocked keys, and P2025 error handling</done>
</task>

</tasks>

<verification>
All test files pass:
```bash
pnpm vitest run src/actions/__tests__/prompt-actions.test.ts src/actions/__tests__/cli-profile-actions.test.ts
```
</verification>

<success_criteria>
- 2 test files created, all passing
- prompt-actions: setDefaultPrompt unset-then-set verified, content length guard tested
- cli-profile-actions: command allowlist, baseArgs metacharacter rejection, envVars blocked keys, P2025 error handling all tested
- Each module has multiple error/edge case tests
</success_criteria>

<output>
After completion, create `.planning/phases/49-server-actions-test-coverage/49-02-SUMMARY.md`
</output>
