---
phase: 52-hooks-logic-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/__tests__/use-image-upload.test.ts
autonomous: true
requirements: [COV-19]

must_haves:
  truths:
    - "use-image-upload hook has unit tests covering upload initiation, progress, success, and error"
    - "Cleanup on unmount is verified (blob URLs revoked, in-flight XHRs aborted)"
    - "removeImage aborts the XHR and revokes the blob URL"
    - "clearAll aborts all XHRs and revokes all blob URLs"
  artifacts:
    - path: "src/hooks/__tests__/use-image-upload.test.ts"
      provides: "Unit tests for useImageUpload hook"
      min_lines: 120
  key_links:
    - from: "src/hooks/__tests__/use-image-upload.test.ts"
      to: "src/hooks/use-image-upload.ts"
      via: "import { useImageUpload }"
      pattern: "useImageUpload"
---

<objective>
Add comprehensive unit tests for the existing use-image-upload.ts hook, covering upload lifecycle, progress tracking, error handling, image removal, clearAll, and cleanup on unmount.

Purpose: COV-19 requires use-image-upload.ts to have unit tests. The hook manages XHR uploads, blob URLs, and progress state — all testable by mocking XMLHttpRequest and URL APIs.

Output: use-image-upload.test.ts with full coverage of the hook's behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/hooks/use-image-upload.ts

<interfaces>
From src/hooks/use-image-upload.ts:
```typescript
export interface PendingImage {
  id: string;
  file: File;
  blobUrl: string;
  status: "uploading" | "done" | "error";
  progress: number; // 0-100
  filename?: string; // populated after successful upload
}

export function useImageUpload(): {
  pendingImages: PendingImage[];
  addImages: (files: File[]) => void;
  removeImage: (id: string) => void;
  clearAll: () => void;
  hasUploading: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for useImageUpload hook</name>
  <files>src/hooks/__tests__/use-image-upload.test.ts</files>
  <read_first>
    - src/hooks/use-image-upload.ts (full file — understand XHR setup, progress handler, onload parsing, onerror, removeImage abort, clearAll, unmount cleanup)
    - src/hooks/__tests__/use-assistant-chat.test.ts (reference for hook test patterns in this project)
  </read_first>
  <behavior>
    - Test: addImages creates pending images with status "uploading" and progress 0
    - Test: addImages creates blob URLs via URL.createObjectURL
    - Test: addImages opens XHR to POST /api/internal/assistant/images with FormData
    - Test: XHR upload.onprogress updates progress percentage
    - Test: XHR onload with status 200 and valid JSON sets status "done" and filename
    - Test: XHR onload with status 200 but invalid JSON (no filename) sets status "error"
    - Test: XHR onload with non-200 status sets status "error"
    - Test: XHR onerror sets status "error"
    - Test: removeImage aborts the XHR and revokes the blob URL
    - Test: removeImage removes the image from pendingImages
    - Test: clearAll aborts all XHRs, revokes all blob URLs, and empties the array
    - Test: hasUploading is true when any image has status "uploading"
    - Test: hasUploading is false when all images are "done" or "error"
    - Test: unmount cleanup revokes all blob URLs and aborts all XHRs
  </behavior>
  <acceptance_criteria>
    - All 14+ test cases pass
    - No DOM-heavy dependencies (mock XMLHttpRequest and URL at global level)
    - Tests verify actual state transitions, not just that functions exist
  </acceptance_criteria>
  <action>
Create src/hooks/__tests__/use-image-upload.test.ts:

1. **Mock setup** (before imports, using vi.hoisted):
   - Mock `crypto.randomUUID` to return deterministic IDs (e.g., `"uuid-1"`, `"uuid-2"`, ...)
   - Mock `URL.createObjectURL` to return `"blob:mock-{n}"` and track calls
   - Mock `URL.revokeObjectURL` to track calls
   - Create a MockXHR class that simulates XMLHttpRequest:
     ```ts
     class MockXHR {
       open = vi.fn();
       send = vi.fn();
       abort = vi.fn();
       status = 200;
       responseText = "";
       upload: { onprogress: ((e: any) => void) | null } = { onprogress: null };
       onload: (() => void) | null = null;
       onerror: (() => void) | null = null;
     }
     ```
   - Track created MockXHR instances in an array for later assertions
   - Assign `global.XMLHttpRequest = MockXHR as any`

2. **Use renderHook from @testing-library/react** (jsdom environment, default):
   - Import `renderHook, act` from `@testing-library/react`
   - Each test: `const { result, unmount } = renderHook(() => useImageUpload())`

3. **Test groups:**

   **describe("addImages"):**
   - Call `act(() => result.current.addImages([mockFile]))` where `mockFile = new File(["x"], "test.png", { type: "image/png" })`
   - Assert `result.current.pendingImages` has 1 item with status "uploading", progress 0
   - Assert `URL.createObjectURL` was called with the file
   - Assert the MockXHR instance's `open` was called with `("POST", "/api/internal/assistant/images")`
   - Assert `send` was called with a FormData containing the file

   **describe("progress tracking"):**
   - After addImages, trigger `xhrInstances[0].upload.onprogress({ lengthComputable: true, loaded: 50, total: 100 })`
   - Wrap in `act()`
   - Assert `result.current.pendingImages[0].progress === 50`

   **describe("upload success"):**
   - Set `xhrInstances[0].status = 200` and `xhrInstances[0].responseText = JSON.stringify({ filename: "2026-04/images/test-abc123.png" })`
   - Call `act(() => xhrInstances[0].onload!())`
   - Assert status is "done", progress is 100, filename matches

   **describe("upload error - invalid response"):**
   - Set status 200 but responseText with no filename field: `JSON.stringify({ error: "no file" })`
   - Call onload → assert status "error"

   **describe("upload error - HTTP error"):**
   - Set status 500, call onload → assert status "error"

   **describe("upload error - network"):**
   - Call onerror → assert status "error"

   **describe("removeImage"):**
   - addImages, then `act(() => result.current.removeImage(id))`
   - Assert XHR abort called, URL.revokeObjectURL called with the blob URL, image removed from array

   **describe("clearAll"):**
   - addImages with 2 files, then clearAll
   - Assert both XHRs aborted, both blob URLs revoked, pendingImages is empty

   **describe("hasUploading"):**
   - After addImages: `result.current.hasUploading === true`
   - After successful upload: `result.current.hasUploading === false`

   **describe("unmount cleanup"):**
   - addImages, then `unmount()`
   - Assert URL.revokeObjectURL called for each blob URL
   - Assert XHR abort called for each in-flight upload
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm test:run -- --reporter=verbose src/hooks/__tests__/use-image-upload.test.ts</automated>
  </verify>
  <done>
    - 14+ test cases pass covering upload initiation, progress, success, error (3 types), remove, clearAll, hasUploading, and unmount cleanup
    - Tests use mocked XMLHttpRequest — no real network calls
    - Hook behavior verified through actual state assertions (pendingImages array contents)
  </done>
</task>

</tasks>

<verification>
- `pnpm test:run -- src/hooks/__tests__/use-image-upload.test.ts` passes with all tests green
- Tests cover: addImages, progress, success, invalid-response error, HTTP error, network error, removeImage, clearAll, hasUploading, unmount
- No flaky tests (all deterministic via mocks)
</verification>

<success_criteria>
- use-image-upload.test.ts has 14+ passing tests
- Upload initiation verified (XHR open/send called correctly)
- Progress tracking verified (onprogress -> state update)
- Error handling verified (3 error paths: invalid JSON, HTTP error, network error)
- Cleanup on unmount verified (blob URLs revoked, XHRs aborted)
- removeImage and clearAll verified with abort + revoke assertions
</success_criteria>

<output>
After completion, create `.planning/phases/52-hooks-logic-extraction/52-02-SUMMARY.md`
</output>
