---
phase: 52-hooks-logic-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/sse-event-reducer.ts
  - src/hooks/__tests__/sse-event-reducer.test.ts
  - src/hooks/use-assistant-chat.ts
autonomous: true
requirements: [COV-18]

must_haves:
  truths:
    - "SSE event parsing logic lives in a standalone pure function, not inside a React hook"
    - "The pure reducer is fully tested without React or DOM dependencies"
    - "use-assistant-chat.ts still works identically after the extraction refactor"
  artifacts:
    - path: "src/hooks/sse-event-reducer.ts"
      provides: "Pure SSE event reducer function and types"
      exports: ["applySSEEvent", "SSEEvent", "ReducerState"]
    - path: "src/hooks/__tests__/sse-event-reducer.test.ts"
      provides: "Unit tests for SSE event reducer"
      min_lines: 80
  key_links:
    - from: "src/hooks/use-assistant-chat.ts"
      to: "src/hooks/sse-event-reducer.ts"
      via: "import { applySSEEvent }"
      pattern: "applySSEEvent"
---

<objective>
Extract the SSE event parsing/state-machine logic from use-assistant-chat.ts into a pure utility function, then add comprehensive unit tests for the extracted logic.

Purpose: The switch/case SSE event reducer (~100 lines) inside use-assistant-chat.ts is pure state transformation logic buried in a React hook closure. Extracting it enables testing without React/DOM, satisfying COV-18 (business logic extraction with tests).

Output: sse-event-reducer.ts (pure function), sse-event-reducer.test.ts (unit tests), refactored use-assistant-chat.ts (imports and calls the pure function).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/hooks/use-assistant-chat.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract SSE event reducer and add unit tests</name>
  <files>src/hooks/sse-event-reducer.ts, src/hooks/__tests__/sse-event-reducer.test.ts, src/hooks/use-assistant-chat.ts</files>
  <read_first>
    - src/hooks/use-assistant-chat.ts (full file — understand SSEEvent type, the switch/case block lines 141-227, and how msgsRef/assistantMsgId/thinkingId are used)
    - src/hooks/__tests__/use-assistant-chat.test.ts (existing tests — must not break)
  </read_first>
  <behavior>
    - Test: applySSEEvent with type="text" and no prior assistant message creates a new assistant message
    - Test: applySSEEvent with type="text" and existing assistant message appends content to it
    - Test: applySSEEvent with type="text" removes the thinking message
    - Test: applySSEEvent with type="tool_use" adds a tool message with JSON-stringified toolInput
    - Test: applySSEEvent with type="tool_use" finalizes (isStreaming=false) the current assistant message
    - Test: applySSEEvent with type="tool_result" appends a tool result message
    - Test: applySSEEvent with type="error" removes thinking and adds error assistant message
    - Test: applySSEEvent with type="done" marks assistant message as not streaming and removes thinking
    - Test: applySSEEvent with type="done" when no assistant message just removes thinking
    - Test: unknown event type returns state unchanged
  </behavior>
  <action>
1. Create src/hooks/sse-event-reducer.ts with:
   - Export the SSEEvent interface (move from use-assistant-chat.ts):
     ```ts
     export interface SSEEvent {
       type: "text" | "tool_use" | "tool_result" | "error" | "done";
       content?: string;
       sessionId?: string;
       toolInput?: unknown;
       toolOutput?: string;
     }
     ```
   - Export a ReducerState interface:
     ```ts
     export interface ReducerState {
       messages: ChatMessage[];
       assistantMsgId: string | null;
       status: "idle" | "connecting" | "streaming" | "error";
     }
     ```
   - Import ChatMessage and MessageRole types from use-assistant-chat.ts.
   - Export a pure function `applySSEEvent(state: ReducerState, event: SSEEvent, thinkingId: string, idGenerator: () => string): ReducerState` that implements the exact same switch/case logic currently at lines 141-227 of use-assistant-chat.ts. The function takes an idGenerator param (instead of calling nextId() directly) to make it testable with deterministic IDs.
   - The function must be pure — no side effects, no React state, no refs.

2. Create src/hooks/__tests__/sse-event-reducer.test.ts with:
   - // @vitest-environment node (pure logic, no DOM needed)
   - Tests per the behavior block above
   - Use a counter-based idGenerator: `let counter = 0; const idGen = () => "id-" + counter++;`
   - Build initial state objects inline per test

3. Refactor src/hooks/use-assistant-chat.ts:
   - Remove the SSEEvent interface (import from sse-event-reducer.ts)
   - Import applySSEEvent and ReducerState from sse-event-reducer.ts
   - Replace the switch/case block (lines 141-227) with a call to applySSEEvent, then apply the returned state to msgsRef.current. For sessionId handling, keep it in the hook (it updates a ref). For status changes from "error" and "done" events, apply from the returned state.
   - The flush() call after each event stays in the hook (it's a React concern).
   - Keep nextId as the idGenerator passed to applySSEEvent.
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm test:run -- --reporter=verbose src/hooks/__tests__/sse-event-reducer.test.ts src/hooks/__tests__/use-assistant-chat.test.ts</automated>
  </verify>
  <done>
    - sse-event-reducer.ts exports applySSEEvent as a pure function with no React/DOM imports
    - All 10+ test cases pass in sse-event-reducer.test.ts
    - Existing use-assistant-chat.test.ts still passes
    - use-assistant-chat.ts imports and delegates to applySSEEvent
  </done>
</task>

</tasks>

<verification>
- `pnpm test:run -- src/hooks/__tests__/sse-event-reducer.test.ts` passes with all tests green
- `pnpm test:run -- src/hooks/__tests__/use-assistant-chat.test.ts` passes (no regression)
- `grep -c "switch" src/hooks/use-assistant-chat.ts` returns 0 (logic moved out)
- `grep "applySSEEvent" src/hooks/use-assistant-chat.ts` confirms the import and usage
</verification>

<success_criteria>
- SSE event parsing logic is a standalone pure function in sse-event-reducer.ts
- 10+ unit tests cover all event types (text, tool_use, tool_result, error, done) plus edge cases
- Tests run with `// @vitest-environment node` — no DOM required
- use-assistant-chat.ts delegates to the pure function; existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/52-hooks-logic-extraction/52-01-SUMMARY.md`
</output>
