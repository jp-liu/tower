---
phase: 60-resource-attribution-task-drawer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/asset-actions.ts
  - src/components/assets/asset-item.tsx
  - src/components/assets/asset-list.tsx
  - src/components/task/task-overview-drawer.tsx
  - src/app/workspaces/[workspaceId]/assets/assets-page-client.tsx
  - src/lib/i18n/locales/zh.ts
  - src/lib/i18n/locales/en.ts
autonomous: true
requirements: [RES-01, RES-02, RES-03, RES-04]

must_haves:
  truths:
    - "Project assets page shows ALL assets (task-bound + project-level)"
    - "Task-bound assets show a clickable badge with truncated task title"
    - "Clicking task badge opens TaskOverviewDrawer with task metadata"
    - "TaskOverviewDrawer shows title, status, priority, description, labels, last execution summary, resource count, creation date"
  artifacts:
    - path: "src/components/task/task-overview-drawer.tsx"
      provides: "Shared drawer component for task quick preview"
      exports: ["TaskOverviewDrawer"]
    - path: "src/actions/asset-actions.ts"
      provides: "Updated getProjectAssets that includes task-bound assets with task title"
      contains: "include.*task"
    - path: "src/components/assets/asset-item.tsx"
      provides: "Asset row with task attribution badge"
      contains: "taskTitle"
  key_links:
    - from: "src/components/assets/asset-item.tsx"
      to: "TaskOverviewDrawer"
      via: "onTaskClick callback"
      pattern: "onTaskClick"
    - from: "src/app/workspaces/[workspaceId]/assets/assets-page-client.tsx"
      to: "src/components/task/task-overview-drawer.tsx"
      via: "state-driven open/close"
      pattern: "TaskOverviewDrawer"
---

<objective>
Show all project assets (including task-bound) in the assets page with task attribution badges, and create a reusable TaskOverviewDrawer component that opens when clicking a task badge.

Purpose: Users currently cannot see task-generated assets in the project assets view, and have no quick-preview for tasks from asset context.
Output: Updated assets page with full asset visibility, task badges, and TaskOverviewDrawer component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@prisma/schema.prisma
@src/actions/asset-actions.ts
@src/components/assets/asset-item.tsx
@src/components/assets/asset-list.tsx
@src/app/workspaces/[workspaceId]/assets/assets-page-client.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From prisma/schema.prisma:
```prisma
model ProjectAsset {
  id          String   @id @default(cuid())
  filename    String
  path        String
  mimeType    String?
  size        Int?
  description String?  @default("")
  projectId   String
  taskId      String?
  createdAt   DateTime @default(now())
  project Project @relation(...)
  task    Task?   @relation(...)
}
```

From src/components/assets/asset-item.tsx:
```typescript
export interface AssetItemType {
  id: string;
  filename: string;
  path: string;
  mimeType: string | null;
  size: number | null;
  description: string | null;
  createdAt: Date;
}
```

From src/actions/asset-actions.ts (current):
```typescript
export async function getProjectAssets(projectId: string) {
  return db.projectAsset.findMany({
    where: { projectId, taskId: null },  // <-- REMOVE taskId: null filter
    orderBy: { createdAt: "desc" },
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update asset query and create TaskOverviewDrawer component</name>
  <files>src/actions/asset-actions.ts, src/components/task/task-overview-drawer.tsx, src/lib/i18n/locales/zh.ts, src/lib/i18n/locales/en.ts</files>
  <read_first>src/actions/asset-actions.ts, src/components/task/task-detail-panel.tsx, src/components/task/task-metadata.tsx, src/components/ui/sheet.tsx, src/lib/i18n/locales/zh.ts</read_first>
  <action>
1. In `src/actions/asset-actions.ts`:
   - Modify `getProjectAssets` to remove the `taskId: null` filter — query ALL assets for the project
   - Add `include: { task: { select: { id: true, title: true, status: true, priority: true, description: true, createdAt: true, labels: { include: { label: true } }, executions: { orderBy: { createdAt: 'desc' }, take: 1, select: { summary: true, status: true, endedAt: true } }, _count: { select: { assets: true } } } } }` to the query
   - Export a new type `ProjectAssetWithTask` that includes the task relation data

2. Create `src/components/task/task-overview-drawer.tsx`:
   - A Sheet (from shadcn) that opens from the right side
   - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `taskId: string | null`
   - When `taskId` changes and is non-null, fetch task data via a new server action `getTaskOverview(taskId)` in `src/actions/task-actions.ts` that returns: title, status, priority, description, labels, last execution (summary + status), asset count (_count.assets), createdAt
   - Display layout:
     - Header: task title (h3) + status badge + priority badge
     - Body (ScrollArea): description (if any), labels as colored badges, creation date, resource count (asset count), last execution summary (if any, show status + summary text truncated to 200 chars)
   - All text via `t()` i18n keys
   - Close button in header

3. Add i18n keys (zh + en): `taskDrawer.title`, `taskDrawer.description`, `taskDrawer.labels`, `taskDrawer.createdAt`, `taskDrawer.resources`, `taskDrawer.lastExecution`, `taskDrawer.noDescription`, `taskDrawer.noExecution`
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - getProjectAssets returns all assets (no taskId:null filter) with task relation included
    - TaskOverviewDrawer component exists, exports correctly, renders task metadata in a Sheet
    - i18n keys added for both zh and en locales
  </acceptance_criteria>
  <done>getProjectAssets includes task-bound assets with task data; TaskOverviewDrawer component created and type-checks</done>
</task>

<task type="auto">
  <name>Task 2: Wire task badge into asset list and connect TaskOverviewDrawer</name>
  <files>src/components/assets/asset-item.tsx, src/components/assets/asset-list.tsx, src/app/workspaces/[workspaceId]/assets/assets-page-client.tsx</files>
  <read_first>src/components/assets/asset-item.tsx, src/components/assets/asset-list.tsx, src/app/workspaces/[workspaceId]/assets/assets-page-client.tsx, src/components/task/task-overview-drawer.tsx</read_first>
  <action>
1. Update `AssetItemType` in `asset-item.tsx`:
   - Add optional fields: `taskId?: string | null`, `taskTitle?: string | null`
   - In the info section (below filename), if `taskTitle` is truthy, render a clickable badge: `[任务: {title truncated to 20 chars}]` using a `<button>` styled as a small colored badge (bg-blue-500/10 text-blue-600 text-xs px-1.5 py-0.5 rounded cursor-pointer hover:bg-blue-500/20)
   - Add `onTaskClick?: (taskId: string) => void` to AssetItemProps
   - Badge click calls `onTaskClick(asset.taskId!)` with `e.stopPropagation()`

2. Update `AssetList`:
   - Pass through new `onTaskClick` prop to each `AssetItem`

3. Update `assets-page-client.tsx`:
   - Import `TaskOverviewDrawer` from `@/components/task/task-overview-drawer`
   - Add state: `const [drawerTaskId, setDrawerTaskId] = useState<string | null>(null)`
   - Pass `onTaskClick={setDrawerTaskId}` to `AssetList`
   - Map assets to include `taskId` and `taskTitle` from the included task relation (the Prisma query now returns `task: { id, title } | null`)
   - Render `<TaskOverviewDrawer open={!!drawerTaskId} onOpenChange={(o) => { if (!o) setDrawerTaskId(null) }} taskId={drawerTaskId} />` at the bottom of the component
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Task-bound assets show a blue badge "[任务: xxx]" with title truncated to 20 chars
    - Clicking the badge opens the TaskOverviewDrawer for that task
    - Non-task assets show no badge
    - All existing asset functionality (preview, reveal, delete) still works
  </acceptance_criteria>
  <done>Asset list shows task attribution badges; clicking badge opens TaskOverviewDrawer with full task metadata</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Navigate to assets page — all assets (task-bound + project-level) visible
3. Task-bound assets show blue "[任务: xxx]" badge beside filename
4. Clicking badge opens right-side drawer with task overview
</verification>

<success_criteria>
- Project assets page shows ALL assets regardless of taskId
- Task-bound assets have clickable task attribution badge
- TaskOverviewDrawer shows complete task metadata
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/60-resource-attribution-task-drawer/60-01-SUMMARY.md`
</output>
