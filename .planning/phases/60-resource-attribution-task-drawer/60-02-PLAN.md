---
phase: 60-resource-attribution-task-drawer
plan: 02
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - src/app/workspaces/[workspaceId]/board-page-client.tsx
  - src/components/board/kanban-board.tsx
  - src/components/board/board-column.tsx
  - src/components/board/task-card.tsx
autonomous: true
requirements: [RES-05]

must_haves:
  truths:
    - "Clicking a DONE or CANCELLED task in the Kanban board opens TaskOverviewDrawer instead of the full TaskDetailPanel"
  artifacts:
    - path: "src/app/workspaces/[workspaceId]/board-page-client.tsx"
      provides: "Conditional click handler for completed/cancelled tasks"
      contains: "TaskOverviewDrawer"
  key_links:
    - from: "src/app/workspaces/[workspaceId]/board-page-client.tsx"
      to: "src/components/task/task-overview-drawer.tsx"
      via: "drawerTaskId state on DONE/CANCELLED click"
      pattern: "DONE.*CANCELLED.*drawerTaskId"
---

<objective>
Make completed and cancelled tasks open the TaskOverviewDrawer (read-only preview) instead of the full TaskDetailPanel when clicked in the Kanban board.

Purpose: Completed/cancelled tasks don't need the full terminal/execution panel — a lightweight overview drawer provides faster context without navigation.
Output: Board click handler routes DONE/CANCELLED tasks to TaskOverviewDrawer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-resource-attribution-task-drawer/60-01-SUMMARY.md
@src/app/workspaces/[workspaceId]/board-page-client.tsx
@src/components/board/kanban-board.tsx

<interfaces>
From src/components/task/task-overview-drawer.tsx (created in plan 01):
```typescript
interface TaskOverviewDrawerProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  taskId: string | null;
}
export function TaskOverviewDrawer({ open, onOpenChange, taskId }: TaskOverviewDrawerProps): JSX.Element;
```

From src/app/workspaces/[workspaceId]/board-page-client.tsx:
```typescript
// Current click handler:
onTaskClick={(task) => setSelectedTask(task)}
// selectedTask drives TaskDetailPanel visibility
```

From src/types/index.ts:
```typescript
export type TaskWithLabels = Task & {
  labels: (TaskLabel & { label: Label })[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route DONE/CANCELLED task clicks to TaskOverviewDrawer</name>
  <files>src/app/workspaces/[workspaceId]/board-page-client.tsx</files>
  <read_first>src/app/workspaces/[workspaceId]/board-page-client.tsx, src/components/task/task-overview-drawer.tsx</read_first>
  <action>
1. Import `TaskOverviewDrawer` from `@/components/task/task-overview-drawer`
2. Add state: `const [drawerTaskId, setDrawerTaskId] = useState<string | null>(null)`
3. Modify the `onTaskClick` handler on `KanbanBoard`:
   - If `task.status === "DONE" || task.status === "CANCELLED"`, call `setDrawerTaskId(task.id)` instead of `setSelectedTask(task)`
   - Otherwise, keep existing behavior: `setSelectedTask(task)`
4. Render `<TaskOverviewDrawer open={!!drawerTaskId} onOpenChange={(o) => { if (!o) setDrawerTaskId(null) }} taskId={drawerTaskId} />` alongside the existing TaskDetailPanel / RepoSidebar section (after the closing div, before the component's final closing elements)
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Clicking a DONE task opens the TaskOverviewDrawer (Sheet from right)
    - Clicking a CANCELLED task opens the TaskOverviewDrawer
    - Clicking TODO/IN_PROGRESS/IN_REVIEW tasks still opens the full TaskDetailPanel as before
    - Both drawer and detail panel can coexist (closing drawer does not affect detail panel state)
  </acceptance_criteria>
  <done>DONE/CANCELLED task clicks open lightweight TaskOverviewDrawer; other statuses open full TaskDetailPanel as before</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Click a DONE task card — TaskOverviewDrawer opens from right with task overview
3. Click a TODO task card — TaskDetailPanel opens as before
4. Close drawer — board state unaffected
</verification>

<success_criteria>
- Completed/cancelled tasks open drawer not detail panel
- Active tasks (TODO/IN_PROGRESS/IN_REVIEW) open detail panel as before
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/60-resource-attribution-task-drawer/60-02-SUMMARY.md`
</output>
