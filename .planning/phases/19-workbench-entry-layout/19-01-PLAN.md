---
phase: 19-workbench-entry-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/i18n.tsx
  - src/components/task/task-detail-panel.tsx
autonomous: true
requirements:
  - WB-01

must_haves:
  truths:
    - "react-resizable-panels@^2.x is installed (version 2.x, not 4.x)"
    - "Six new i18n keys exist in both zh and en blocks in i18n.tsx"
    - "The 查看详情 button in task-detail-panel.tsx navigates to /workspaces/[workspaceId]/tasks/[taskId]"
  artifacts:
    - path: "package.json"
      provides: "react-resizable-panels dependency at ^2.x"
      contains: "react-resizable-panels"
    - path: "src/lib/i18n.tsx"
      provides: "New taskPage.* i18n keys for tab labels and placeholders"
      exports: ["taskPage.tabFiles", "taskPage.tabPreview", "taskPage.filesPlaceholder", "taskPage.previewPlaceholder", "taskPage.comingSoon", "taskPage.comingSoonPhase23"]
    - path: "src/components/task/task-detail-panel.tsx"
      provides: "Working 查看详情 navigation button"
      contains: "router.push"
  key_links:
    - from: "task-detail-panel.tsx button onClick"
      to: "/workspaces/${workspaceId}/tasks/${task.id}"
      via: "router.push()"
      pattern: "router\\.push.*workspaces.*tasks"
---

<objective>
Install the `react-resizable-panels@^2.x` package, add the six new i18n translation keys needed by Phase 19, and verify the "查看详情" navigation button in `task-detail-panel.tsx` is correctly wired to the task workbench route.

Purpose: Establish the dependency and translation foundation that Plan 02 builds on. Without `react-resizable-panels` installed, the layout refactor cannot proceed. Without i18n keys, the tab labels and placeholder text will be missing strings at runtime.

Output: Updated `package.json` with react-resizable-panels@^2.x, updated `src/lib/i18n.tsx` with 12 new translation entries (6 keys × 2 locales), and confirmed `task-detail-panel.tsx` "查看详情" button navigates correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-workbench-entry-layout/19-CONTEXT.md
@.planning/phases/19-workbench-entry-layout/19-RESEARCH.md
@.planning/phases/19-workbench-entry-layout/19-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-resizable-panels and add i18n keys</name>
  <files>package.json, src/lib/i18n.tsx</files>
  <read_first>
    - /Users/liujunping/project/i/ai-manager/package.json (confirm react-resizable-panels is not yet installed, see existing dependency format)
    - /Users/liujunping/project/i/ai-manager/src/lib/i18n.tsx (read FULL file to find exact insertion points for new keys in both zh and en blocks)
  </read_first>
  <action>
    Step 1 — Install dependency:
    Run: `npm install react-resizable-panels@^2.1.9`
    Verify the installed version is 2.x (not 4.x) by checking package.json after install.
    The v2 API exports `Panel`, `PanelGroup`, `PanelResizeHandle` — NOT `ResizablePanel`/`ResizablePanelGroup` (those are shadcn v4 wrappers, which is a different package). Per D-01.

    Step 2 — Add i18n keys to `src/lib/i18n.tsx`:
    In the `zh` translations block, add these 6 keys adjacent to the existing `taskPage.*` keys (after `"taskPage.loadingDiff"`):
    ```
    "taskPage.tabFiles": "文件",
    "taskPage.tabPreview": "预览",
    "taskPage.filesPlaceholder": "文件浏览器",
    "taskPage.previewPlaceholder": "预览面板",
    "taskPage.comingSoon": "即将推出",
    "taskPage.comingSoonPhase23": "即将推出（Phase 23）",
    ```

    In the `en` translations block, add the same 6 keys adjacent to the existing `taskPage.*` keys (after `"taskPage.loadingDiff"`):
    ```
    "taskPage.tabFiles": "Files",
    "taskPage.tabPreview": "Preview",
    "taskPage.filesPlaceholder": "File browser",
    "taskPage.previewPlaceholder": "Preview panel",
    "taskPage.comingSoon": "Coming in next phase",
    "taskPage.comingSoonPhase23": "Coming in Phase 23",
    ```

    Do NOT add a `taskPage.changes` key — it already exists as `"taskPage.changes": "变更"` / `"Changes"`.
    Do NOT add a `taskPage.viewDetails` key — it already exists.
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && node -e "const pkg = require('./package.json'); const v = pkg.dependencies['react-resizable-panels']; if (!v) { process.exit(1); } const major = v.replace(/[^0-9]/, '').split('.')[0]; if (major >= 4) { console.error('Wrong version:', v); process.exit(1); } console.log('OK:', v);" && grep -c "taskPage.tabFiles" src/lib/i18n.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"react-resizable-panels"` with version matching `^2.x` (e.g. `"^2.1.9"`)
    - `grep "react-resizable-panels" package.json` outputs a line containing `"2.`
    - `grep -c "taskPage.tabFiles" src/lib/i18n.tsx` outputs `2` (one entry per locale)
    - `grep -c "taskPage.tabPreview" src/lib/i18n.tsx` outputs `2`
    - `grep -c "taskPage.filesPlaceholder" src/lib/i18n.tsx` outputs `2`
    - `grep -c "taskPage.previewPlaceholder" src/lib/i18n.tsx` outputs `2`
    - `grep -c "taskPage.comingSoon" src/lib/i18n.tsx` outputs at least `4` (comingSoon + comingSoonPhase23, each in 2 locales)
    - `pnpm vitest run --reporter=verbose` passes (no regressions from i18n edit)
  </acceptance_criteria>
  <done>react-resizable-panels v2.x installed, 12 new i18n translation entries added (6 keys × 2 locales), all existing tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Verify and confirm 查看详情 button (WB-01)</name>
  <files>src/components/task/task-detail-panel.tsx</files>
  <read_first>
    - /Users/liujunping/project/i/ai-manager/src/components/task/task-detail-panel.tsx (read full file — the button is at lines 246-252; check exact placement, i18n key, icon, and route)
  </read_first>
  <action>
    Read `task-detail-panel.tsx` and confirm the "查看详情" button (lines 246-252) matches ALL of these criteria per D-07:
    1. Uses `ExternalLink` icon from lucide-react (h-3 w-3)
    2. `onClick` calls `router.push(\`/workspaces/${workspaceId}/tasks/${task.id}\`)`
    3. Uses i18n key `t("taskPage.viewDetails")`
    4. CSS classes include `text-muted-foreground hover:text-foreground`
    5. Button is placed in the drawer header/tab bar row (not in the content area)

    If ALL 5 criteria pass: No code changes needed. The button already works correctly.

    If any criterion fails: Make the minimal edit to satisfy it. For example, if the route is wrong, update the `router.push()` argument. If the icon is missing, add `<ExternalLink className="h-3 w-3" />` as the leading child.

    Do NOT move the button to a different location — its current placement in the tab bar row (adjacent to tab buttons) is acceptable per D-07 ("next to the task title area").
    Do NOT add any extra buttons or links — only this single "查看详情" entry point.
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && grep -n "router.push.*workspaces.*tasks.*task.id" src/components/task/task-detail-panel.tsx && grep -n "taskPage.viewDetails" src/components/task/task-detail-panel.tsx && grep -n "ExternalLink" src/components/task/task-detail-panel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "router.push.*workspaces.*tasks.*task.id" src/components/task/task-detail-panel.tsx` returns at least one match
    - `grep "taskPage.viewDetails" src/components/task/task-detail-panel.tsx` returns at least one match
    - `grep "ExternalLink" src/components/task/task-detail-panel.tsx` returns at least one match
    - `pnpm vitest run --reporter=verbose` passes (no regressions)
  </acceptance_criteria>
  <done>查看详情 button confirmed present with correct route `/workspaces/[workspaceId]/tasks/[taskId]`, ExternalLink icon, and viewDetails i18n key. WB-01 requirement is satisfied.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `grep "react-resizable-panels" package.json` shows version 2.x
- `grep -c "taskPage.tabFiles" src/lib/i18n.tsx` = 2
- `grep "router.push.*workspaces.*tasks.*task.id" src/components/task/task-detail-panel.tsx` has a match
- `pnpm vitest run --reporter=verbose` is green
</verification>

<success_criteria>
- react-resizable-panels@^2.x installed and in package.json
- 6 new i18n keys × 2 locales = 12 entries added to i18n.tsx
- 查看详情 button in task-detail-panel.tsx navigates to correct workbench route
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-workbench-entry-layout/19-01-SUMMARY.md`
</output>
