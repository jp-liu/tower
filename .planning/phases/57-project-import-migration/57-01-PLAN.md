---
phase: 57-project-import-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/top-bar.tsx
  - src/components/project/create-project-dialog.tsx
  - src/components/project/import-project-dialog.tsx
  - src/lib/i18n/zh.ts
  - src/lib/i18n/en.ts
autonomous: true
requirements: [PROJ-01, PROJ-02]

must_haves:
  truths:
    - "User can create a project from git URL with auto-resolved local path and clone button"
    - "User can import an existing folder with auto-detected git remote and auto-filled project name"
    - "Two separate buttons in TopBar trigger two distinct dialogs"
  artifacts:
    - path: "src/components/project/create-project-dialog.tsx"
      provides: "Git URL-first project creation dialog"
      min_lines: 80
    - path: "src/components/project/import-project-dialog.tsx"
      provides: "Folder browser-first project import dialog"
      min_lines: 80
  key_links:
    - from: "src/components/layout/top-bar.tsx"
      to: "src/components/project/create-project-dialog.tsx"
      via: "import and state toggle"
      pattern: "CreateProjectDialog"
    - from: "src/components/project/import-project-dialog.tsx"
      to: "/api/git"
      via: "fetch GET to detect remote"
      pattern: "fetch.*api/git"
---

<objective>
Split the monolithic "新建项目" dialog into two distinct flows: "新建项目" (create from git URL) and "导入项目" (import existing folder). Each flow auto-fills fields and provides the appropriate UX.

Purpose: Users have two clear entry points — one for repos they want to clone, one for folders already on disk.
Output: Two new dialog components, updated TopBar with two buttons, new i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/layout/top-bar.tsx
@src/lib/git-url.ts
@src/actions/config-actions.ts
@src/app/api/git/route.ts
@src/components/layout/folder-browser-dialog.tsx

<interfaces>
<!-- Existing utilities the executor needs -->

From src/lib/git-url.ts:
```typescript
export function gitUrlToLocalPath(url: string): string;
export function toCloneUrl(url: string): string;
export function parseGitUrl(raw: string): ParsedUrl | null;
export function matchGitPathRule(url: string, rules: GitPathRule[]): string;
```

From src/actions/config-actions.ts:
```typescript
export async function resolveGitLocalPath(url: string): Promise<string>;
```

From src/actions/workspace-actions.ts:
```typescript
export async function createProject(data: {
  name: string; alias?: string; description?: string;
  gitUrl?: string; localPath?: string; workspaceId: string;
  projectType?: "FRONTEND" | "BACKEND"; previewCommand?: string | null;
}): Promise<Project>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the two dialog components</name>
  <files>src/components/project/create-project-dialog.tsx, src/components/project/import-project-dialog.tsx, src/lib/i18n/zh.ts, src/lib/i18n/en.ts</files>
  <read_first>src/components/layout/top-bar.tsx, src/lib/i18n/zh.ts, src/lib/i18n/en.ts, src/components/layout/folder-browser-dialog.tsx</read_first>
  <action>
Create two new dialog components extracted from the existing top-bar.tsx inline dialog:

**CreateProjectDialog** (`src/components/project/create-project-dialog.tsx`):
- Props: `open`, `onOpenChange`, `onCreateProject` (same signature as TopBar's existing callback)
- Primary input: Git URL field (prominent, at the top)
- On git URL change: call `resolveGitLocalPath(url)` to auto-fill local path
- Shows localPath field (auto-filled but editable, with "Browse" button opening FolderBrowserDialog)
- Clone button appears when both gitUrl and localPath are set (reuse existing clone logic calling POST /api/git with action "clone")
- Clone status indicator (cloning/success/error) with same UX as current top-bar
- Name field auto-derived from git URL (last path segment via parseGitUrl) — user can override
- Alias, description, projectType fields remain
- "创建" button calls onCreateProject

**ImportProjectDialog** (`src/components/project/import-project-dialog.tsx`):
- Props: `open`, `onOpenChange`, `onCreateProject` (same signature)
- Primary action: "浏览" button that immediately opens FolderBrowserDialog
- On folder select: call `GET /api/git?path=<selected>` to detect if it's a git repo
  - If git repo with remote: auto-fill gitUrl field, auto-derive project name from repo name
  - If not git: leave gitUrl empty, derive project name from folder basename
- Shows read-only localPath (the selected folder)
- Name (auto-filled from folder/repo name, editable), alias, description, projectType fields
- "创建" button calls onCreateProject

Add i18n keys (both zh.ts and en.ts):
- `"topbar.importProject"`: "导入项目" / "Import Project"
- `"project.importHint"`: "选择本地已有项目文件夹" / "Select an existing local project folder"
- `"project.autoDetected"`: "已自动检测 Git 远程" / "Git remote auto-detected"

Use shadcn Dialog, Input, Button components. Follow existing patterns in top-bar.tsx for styling (ring-1 ring-primary/25 buttons, font-mono text-xs for paths, etc.).
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && npx tsc --noEmit --skipLibCheck 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
- File src/components/project/create-project-dialog.tsx exists with exported CreateProjectDialog component
- File src/components/project/import-project-dialog.tsx exists with exported ImportProjectDialog component
- Both files import from @/components/ui/dialog and @/lib/i18n
- CreateProjectDialog calls resolveGitLocalPath on git URL change
- ImportProjectDialog calls fetch("/api/git?path=...") on folder select
- i18n keys "topbar.importProject" and "project.importHint" exist in both zh.ts and en.ts
  </acceptance_criteria>
  <done>Two dialog components compile without errors, contain proper imports and logic for auto-fill</done>
</task>

<task type="auto">
  <name>Task 2: Wire dialogs into TopBar with two buttons</name>
  <files>src/components/layout/top-bar.tsx</files>
  <read_first>src/components/layout/top-bar.tsx, src/components/project/create-project-dialog.tsx, src/components/project/import-project-dialog.tsx</read_first>
  <action>
Refactor top-bar.tsx:

1. Remove the entire inline "Create Project Dialog" section (lines ~216-356 including all local state: projectName, projectAlias, projectDesc, gitUrl, localPath, localPathManual, showFolderBrowser, projectType, cloneStatus, cloneError, and all handlers: handleGitUrlChange, handleClone, handleLocalPathChange, handleCreateProject, resetForm).

2. Import the two new components:
   ```typescript
   import { CreateProjectDialog } from "@/components/project/create-project-dialog";
   import { ImportProjectDialog } from "@/components/project/import-project-dialog";
   ```

3. Add two state variables: `showCreateProject` and `showImportProject`.

4. Replace the single "新建项目" button with two buttons:
   - "导入项目" button (FolderOpen icon or similar, variant outline, ring style) — opens ImportProjectDialog
   - "新建项目" button (Plus icon, primary ring style as before) — opens CreateProjectDialog

5. Render both dialog components at the bottom of the JSX, passing `open`, `onOpenChange`, and `onCreateProject` props.

6. Remove the now-unused FolderBrowserDialog import and the inline folder browser (it's now internal to the dialog components).

7. Remove unused imports (toCloneUrl, resolveGitLocalPath, GitBranch, Loader2, Check, AlertCircle, SegmentedControl, FolderBrowserDialog) if no longer used in this file.
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && npx tsc --noEmit --skipLibCheck 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
- top-bar.tsx imports CreateProjectDialog and ImportProjectDialog
- top-bar.tsx has NO inline Dialog for project creation (the old 140-line block is gone)
- Two buttons exist: one with t("topbar.importProject"), one with t("topbar.newProject")
- Both dialog components are rendered with open/onOpenChange/onCreateProject props
- TypeScript compiles clean
  </acceptance_criteria>
  <done>TopBar is simplified to ~150 lines, two separate dialogs handle creation flows independently</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --skipLibCheck` passes
- `pnpm dev` starts without errors
- CreateProjectDialog renders git URL field and auto-resolves local path
- ImportProjectDialog renders folder browse button and auto-detects git remote
</verification>

<success_criteria>
- Two distinct project creation entry points visible in TopBar
- Create flow: git URL auto-resolves path, clone button works
- Import flow: folder selection auto-detects git remote and auto-fills name
- All text uses i18n keys
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/57-project-import-migration/57-01-SUMMARY.md`
</output>
