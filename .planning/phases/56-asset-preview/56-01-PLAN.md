---
phase: 56-asset-preview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/assets/image-lightbox.tsx
  - src/components/assets/text-preview-dialog.tsx
  - src/app/api/internal/assets/reveal/route.ts
  - src/lib/i18n/zh.ts
  - src/lib/i18n/en.ts
autonomous: true
requirements: [ASSET-01, ASSET-02, ASSET-03]

must_haves:
  truths:
    - "Image lightbox opens fullscreen with zoom/pan and Escape closes it"
    - "Text/md/json preview renders markdown or monospace appropriately"
    - "Reveal API route executes open -R on macOS to highlight file in Finder"
  artifacts:
    - path: "src/components/assets/image-lightbox.tsx"
      provides: "Fullscreen image viewer with zoom toggle and scroll-to-pan"
      exports: ["ImageLightbox"]
    - path: "src/components/assets/text-preview-dialog.tsx"
      provides: "Text/Markdown/JSON preview dialog"
      exports: ["TextPreviewDialog"]
    - path: "src/app/api/internal/assets/reveal/route.ts"
      provides: "POST endpoint to reveal file in system file manager"
      exports: ["POST"]
  key_links:
    - from: "src/components/assets/image-lightbox.tsx"
      to: "@/components/ui/dialog"
      via: "Dialog import"
      pattern: "import.*Dialog.*from.*@/components/ui/dialog"
    - from: "src/components/assets/text-preview-dialog.tsx"
      to: "react-markdown"
      via: "Markdown rendering for .md files"
      pattern: "import ReactMarkdown"
    - from: "src/app/api/internal/assets/reveal/route.ts"
      to: "@/lib/internal-api-guard"
      via: "requireLocalhost guard"
      pattern: "requireLocalhost"
---

<objective>
Create the three new modules needed for asset preview: an image lightbox component with zoom/pan, a text preview dialog for .txt/.md/.json, and an internal API route for revealing files in Finder. Add all required i18n keys.

Purpose: These are the building blocks that Plan 02 will wire into the existing asset list UI.
Output: Three new files (two components + one API route) plus i18n updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/56-asset-preview/56-RESEARCH.md
@src/components/assistant/image-preview-modal.tsx
@src/lib/internal-api-guard.ts
@src/components/notes/note-card.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/assets/asset-item.tsx:
```typescript
export interface AssetItemType {
  id: string;
  filename: string;
  path: string;
  mimeType: string | null;
  size: number | null;
  description: string | null;
  createdAt: Date;
}
```

From src/components/assistant/image-preview-modal.tsx (pattern to follow):
```typescript
interface ImagePreviewModalProps {
  imageUrl: string | null;
  open: boolean;
  onOpenChange: (open: boolean) => void;
}
```

From src/lib/internal-api-guard.ts:
```typescript
export function requireLocalhost(request: NextRequest): NextResponse | null;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Image lightbox and text preview components</name>
  <files>src/components/assets/image-lightbox.tsx, src/components/assets/text-preview-dialog.tsx</files>
  <read_first>
    - src/components/assistant/image-preview-modal.tsx
    - src/components/notes/note-card.tsx
    - src/components/ui/dialog.tsx
    - .claude/rules/ui.md
  </read_first>
  <action>
Create `src/components/assets/image-lightbox.tsx`:
- Props: `{ imageUrl: string | null; filename: string; open: boolean; onOpenChange: (open: boolean) => void }`
- Uses Dialog from `@/components/ui/dialog` with `showCloseButton={false}`, class `max-w-[90vw] max-h-[90vh] p-0 overflow-hidden bg-black/90 ring-0`
- Close button: DialogClose with X icon, positioned `absolute top-2 right-2 z-10`, ghost variant, white text, `hover:bg-white/20`
- Zoom state: `const [zoomed, setZoomed] = useState(false)` — reset on `open` change via useEffect
- When NOT zoomed: `<img className="max-w-full max-h-[85vh] object-contain cursor-zoom-in mx-auto" onClick={toggle zoom} />`
- When zoomed: wrap img in `<div className="overflow-auto max-w-[90vw] max-h-[90vh]">` and img gets `className="w-auto h-auto max-w-none cursor-zoom-out"` — scroll-to-pan is native via overflow
- Show filename in bottom-left: `<span className="absolute bottom-2 left-3 text-xs text-white/70 truncate max-w-[60%]">{filename}</span>`

Create `src/components/assets/text-preview-dialog.tsx`:
- Props: `{ url: string | null; filename: string; open: boolean; onOpenChange: (open: boolean) => void }`
- Uses Dialog, DialogContent with `max-w-3xl max-h-[80vh] flex flex-col`
- Header: filename displayed in `<h3 className="text-sm font-medium truncate px-4 pt-4">`
- Content area: `<div className="flex-1 overflow-auto px-4 pb-4 pt-2">`
- State: `content: string | null`, `loading: boolean`, `error: string | null`
- useEffect on open+url: fetch(url).then(r => r.text()).then(setContent).catch(setError); set loading states
- Size guard: if response content-length > 1MB, show `t("assets.fileTooLarge")` message instead of content
- Determine extension: `filename.split('.').pop()?.toLowerCase()`
- If ext === 'md': render `<ReactMarkdown remarkPlugins={[remarkGfm]} className="prose prose-sm dark:prose-invert max-w-none">{content}</ReactMarkdown>`
- If ext === 'json': render `<pre className="font-mono text-xs whitespace-pre-wrap text-foreground">{formatted json via JSON.stringify(JSON.parse(content), null, 2)}</pre>` — fallback to raw content if parse fails
- Otherwise (.txt etc): render `<pre className="font-mono text-sm whitespace-pre-wrap text-foreground">{content}</pre>`
- Loading state: centered `<Loader2 className="size-5 animate-spin" />`
- Error state: `<p className="text-sm text-destructive">{t("assets.previewError")}</p>`
- Both components use `"use client"` directive and import `useI18n`
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function ImageLightbox" src/components/assets/image-lightbox.tsx
    - grep -q "overflow-auto" src/components/assets/image-lightbox.tsx
    - grep -q "cursor-zoom-in" src/components/assets/image-lightbox.tsx
    - grep -q "export function TextPreviewDialog" src/components/assets/text-preview-dialog.tsx
    - grep -q "ReactMarkdown" src/components/assets/text-preview-dialog.tsx
    - grep -q "remarkGfm" src/components/assets/text-preview-dialog.tsx
    - grep -q "font-mono" src/components/assets/text-preview-dialog.tsx
  </acceptance_criteria>
  <done>ImageLightbox renders fullscreen image with zoom toggle and scroll-to-pan; TextPreviewDialog fetches content and renders .md as markdown, .json as formatted JSON, and .txt as monospace. Both compile without type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Reveal in Finder API route</name>
  <files>src/app/api/internal/assets/reveal/route.ts</files>
  <read_first>
    - src/app/api/internal/assets/[projectId]/[filename]/route.ts
    - src/lib/internal-api-guard.ts
    - .claude/rules/security.md
  </read_first>
  <action>
Create `src/app/api/internal/assets/reveal/route.ts`:
- Add `export const dynamic = "force-dynamic"` and `export const runtime = "nodejs"`
- Export async POST handler accepting NextRequest
- Call `requireLocalhost(request)` — return blocked response if non-null
- Parse JSON body: `{ path: string }` — validate path is a non-empty string
- Resolve absolute path: `path.resolve(process.cwd(), bodyPath)`
- Security: Validate resolved path starts with `path.resolve(process.cwd(), "data/assets/")` — return 400 "Invalid path" if not (prevents directory traversal)
- Also validate file exists using `fs.stat(resolvedPath)` — return 404 if missing
- Platform detection:
  - `process.platform === "darwin"`: execFile("open", ["-R", resolvedPath])
  - `process.platform === "linux"`: execFile("xdg-open", [path.dirname(resolvedPath)])
  - `process.platform === "win32"`: execFile("explorer", ["/select,", resolvedPath])
  - Other: return 400 "Unsupported platform"
- Use `execFile` from `node:child_process` via `promisify` — NOT `exec` (no shell invocation)
- Return `{ success: true }` on success, catch execFile errors and return 500 with error message
- Import `requireLocalhost` from `@/lib/internal-api-guard`
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && npx tsc --noEmit --pretty 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "requireLocalhost" src/app/api/internal/assets/reveal/route.ts
    - grep -q "execFile" src/app/api/internal/assets/reveal/route.ts
    - grep -q 'force-dynamic' src/app/api/internal/assets/reveal/route.ts
    - grep -q "data/assets" src/app/api/internal/assets/reveal/route.ts
    - grep -q "process.platform" src/app/api/internal/assets/reveal/route.ts
    - grep -v "exec(" src/app/api/internal/assets/reveal/route.ts | grep -q "execFile"
  </acceptance_criteria>
  <done>POST /api/internal/assets/reveal validates localhost, validates path within data/assets/, detects platform, and runs the appropriate reveal command without shell invocation.</done>
</task>

<task type="auto">
  <name>Task 3: Add i18n keys for asset preview</name>
  <files>src/lib/i18n/zh.ts, src/lib/i18n/en.ts</files>
  <read_first>
    - src/lib/i18n/zh.ts
    - src/lib/i18n/en.ts
  </read_first>
  <action>
Add the following keys to both i18n files, in the `assets.*` section (after existing assets keys):

**zh.ts:**
```
"assets.revealInFinder": "在文件夹中显示",
"assets.previewNotSupported": "不支持预览此文件类型",
"assets.loadingPreview": "加载中...",
"assets.previewError": "加载预览失败",
"assets.fileTooLarge": "文件过大，无法预览（超过 1MB）",
```

**en.ts:**
```
"assets.revealInFinder": "Reveal in Finder",
"assets.previewNotSupported": "Preview not supported for this file type",
"assets.loadingPreview": "Loading...",
"assets.previewError": "Failed to load preview",
"assets.fileTooLarge": "File too large to preview (over 1MB)",
```
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && grep -c "assets.revealInFinder" src/lib/i18n/zh.ts src/lib/i18n/en.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "assets.revealInFinder" src/lib/i18n/zh.ts
    - grep -q "assets.revealInFinder" src/lib/i18n/en.ts
    - grep -q "assets.previewError" src/lib/i18n/zh.ts
    - grep -q "assets.previewError" src/lib/i18n/en.ts
    - grep -q "assets.fileTooLarge" src/lib/i18n/zh.ts
    - grep -q "assets.fileTooLarge" src/lib/i18n/en.ts
  </acceptance_criteria>
  <done>All 5 new i18n keys exist in both zh.ts and en.ts.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- All new files export their named components/handlers
- i18n keys present in both language files
</verification>

<success_criteria>
- ImageLightbox component exists with zoom toggle and overflow-auto pan
- TextPreviewDialog component exists with markdown rendering and monospace fallback
- Reveal API route exists with localhost guard, path validation, and platform-aware reveal command
- All required i18n keys added to both zh.ts and en.ts
</success_criteria>

<output>
After completion, create `.planning/phases/56-asset-preview/56-01-SUMMARY.md`
</output>
