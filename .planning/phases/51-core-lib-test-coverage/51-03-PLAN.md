---
phase: 51-core-lib-test-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/__tests__/assistant-sessions.test.ts
  - src/lib/__tests__/execution-summary.test.ts
autonomous: true
requirements: [COV-21, COV-22]

must_haves:
  truths:
    - "Session CRUD operations correctly manage localStorage entries"
    - "addSession caps at MAX_SESSIONS (20) and deduplicates by id"
    - "buildSessionTitle truncates at 30 chars and defaults to 'New Session' for empty input"
    - "parseDiffStat extracts files changed, insertions, and deletions from git diff --stat output"
    - "stripAnsi removes ANSI escape codes, OSC sequences, and control characters"
    - "buildSummary produces single-line for 1 commit and 'N commits: first' for multiple"
  artifacts:
    - path: "src/lib/__tests__/assistant-sessions.test.ts"
      provides: "Tests for getSessions, addSession, updateSession, deleteSession, buildSessionTitle, active session management"
      min_lines: 80
    - path: "src/lib/__tests__/execution-summary.test.ts"
      provides: "Tests for parseDiffStat, stripAnsi, buildSummary, trimTerminalBuffer pure functions"
      min_lines: 60
  key_links:
    - from: "src/lib/__tests__/assistant-sessions.test.ts"
      to: "src/lib/assistant-sessions.ts"
      via: "import all CRUD functions"
      pattern: "import.*from.*assistant-sessions"
    - from: "src/lib/__tests__/execution-summary.test.ts"
      to: "src/lib/execution-summary.ts"
      via: "import or test via internal access"
      pattern: "import.*from.*execution-summary"
---

<objective>
Unit tests for assistant-sessions.ts (localStorage session management) and execution-summary.ts (git stats parsing, ANSI stripping, summary building).

Purpose: assistant-sessions handles chat session persistence in the browser; execution-summary captures post-execution data. Both contain data transformation logic that needs coverage.
Output: Two test files covering CRUD operations, boundary conditions, and text processing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/assistant-sessions.ts
@src/lib/execution-summary.ts
@src/lib/__tests__/internal-api-guard.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: assistant-sessions.ts unit tests</name>
  <files>src/lib/__tests__/assistant-sessions.test.ts</files>
  <read_first>
    - src/lib/assistant-sessions.ts — all exported functions, STORAGE_KEY, ACTIVE_KEY, MAX_SESSIONS constants
  </read_first>
  <behavior>
    - getSessions(): returns [] when localStorage is empty
    - getSessions(): returns parsed array when localStorage has valid JSON
    - getSessions(): returns [] when localStorage has invalid JSON (graceful fallback)
    - getSessions(): returns [] when localStorage has non-array JSON (e.g., object)
    - addSession(): prepends new session, deduplicates by id, caps at 20
    - addSession(): adding 21st session drops the oldest
    - updateSession(): merges updates and sets updatedAt
    - updateSession(): does not affect other sessions
    - deleteSession(): removes session by id
    - deleteSession(): clears active session if deleted session was active
    - getActiveSessionId(): returns null when no active session
    - setActiveSessionId(id): stores and retrieves id
    - setActiveSessionId(null): removes active session
    - buildSessionTitle("Hello world"): returns "Hello world"
    - buildSessionTitle(string >30 chars): returns first 30 chars
    - buildSessionTitle(""): returns "New Session"
    - buildSessionTitle("  "): returns "New Session" (whitespace only)
  </behavior>
  <action>
    Create src/lib/__tests__/assistant-sessions.test.ts (per COV-21):
    - Use `// @vitest-environment jsdom` at top since these functions use localStorage
    - Import all exported functions from ../assistant-sessions
    - beforeEach: clear localStorage
    - getSessions tests: empty, valid, invalid JSON, non-array
    - addSession tests: basic add, dedup by id, MAX_SESSIONS cap (create 21 sessions, verify only 20 stored)
    - updateSession tests: updates specific session, leaves others intact, sets updatedAt
    - deleteSession tests: removes session, clears active if deleted was active
    - Active session tests: get/set/clear
    - buildSessionTitle tests: normal, truncation at 30, empty string, whitespace-only
    - Note: functions return early (no-op) when typeof window === "undefined" — jsdom environment provides window, so all paths are exercised
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm vitest run src/lib/__tests__/assistant-sessions.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All CRUD operations on sessions verified (add, update, delete, get)
    - MAX_SESSIONS cap at 20 confirmed
    - buildSessionTitle truncation at 30 chars and empty-string default confirmed
    - Active session get/set/clear cycle verified
    - Graceful fallback on invalid localStorage data confirmed
  </acceptance_criteria>
  <done>assistant-sessions.test.ts passes with full CRUD, cap, title, and error handling coverage</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: execution-summary.ts pure function tests</name>
  <files>src/lib/__tests__/execution-summary.test.ts</files>
  <read_first>
    - src/lib/execution-summary.ts — parseDiffStat, stripAnsi, buildSummary, trimTerminalBuffer (module-private functions), captureExecutionSummary (exported async)
  </read_first>
  <behavior>
    Note: parseDiffStat, stripAnsi, buildSummary, trimTerminalBuffer are NOT exported. Test them via captureExecutionSummary or extract them for testing. Best approach: test the exported captureExecutionSummary with mocked dependencies, which exercises all internal functions indirectly. Alternatively, test by importing the module and accessing the internal functions if the module is refactored. Given this is a test-only phase (no refactoring), test captureExecutionSummary end-to-end with mocks.

    - captureExecutionSummary with valid worktreePath and git data: updates DB with summary, gitLog, gitStats, exitCode, terminalLog
    - captureExecutionSummary with null worktreePath: updates DB with only exitCode and terminalLog (no git data)
    - captureExecutionSummary with terminal buffer containing ANSI codes: terminalLog in DB is stripped clean
    - captureExecutionSummary with very large terminal buffer (>10KB): trimmed to last 10KB
    - captureExecutionSummary when DB update throws: does not throw (designed to never throw)
    - captureExecutionSummary fires background AI summary (verify it calls generateSummaryFromLog)
  </behavior>
  <action>
    Create src/lib/__tests__/execution-summary.test.ts (per COV-22):
    - Add `// @vitest-environment node` at top
    - vi.mock("@/lib/db") to mock db.taskExecution.update
    - vi.mock("@/lib/claude-session") to mock findLatestSessionId and generateSummaryFromLog
    - vi.mock("child_process") to mock execFileSync for git commands
    - vi.mock("fs") to mock existsSync

    Test cases:
    1. "updates DB with git data when worktreePath exists":
       - Mock existsSync(worktreePath) = true
       - Mock execFileSync to return appropriate git output for rev-parse, merge-base, log, diff --stat
       - Mock findLatestSessionId to return "session-abc"
       - Mock generateSummaryFromLog to return Promise.resolve("AI summary")
       - Call captureExecutionSummary(execId, taskId, 0, "terminal output", "/path/worktree")
       - Verify db.taskExecution.update called with summary, gitLog, gitStats, exitCode: 0, sessionId

    2. "updates DB without git data when worktreePath is null":
       - Call captureExecutionSummary(execId, taskId, 1, "some output", null)
       - Verify db.taskExecution.update called with exitCode: 1, gitLog: null, gitStats: null

    3. "strips ANSI from terminal buffer":
       - Call with terminalBuffer containing "\x1b[31mred text\x1b[0m"
       - Verify db.taskExecution.update called with terminalLog NOT containing \x1b

    4. "trims large terminal buffer to 10KB":
       - Call with terminalBuffer of 20KB of text
       - Verify terminalLog in update call is <= 10KB

    5. "does not throw when DB update fails":
       - Mock db.taskExecution.update to throw
       - Call captureExecutionSummary — should not throw (wrap in expect(...).not.toThrow is not needed since it returns Promise — just await and verify no rejection)

    6. "calls generateSummaryFromLog for background AI summary":
       - Provide both terminalLog and worktreePath
       - Verify generateSummaryFromLog was called with the cleaned terminal log and worktreePath

    Git mock strategy for execFileSync:
    - First call (rev-parse --git-dir): return ".git"
    - merge-base calls: return "abc123" for "main" branch
    - log --oneline: return "abc1234 feat: add feature\ndef5678 fix: bug fix"
    - diff --stat: return "2 files changed, 47 insertions(+), 12 deletions(-)"
    - Use mockImplementation that checks args[1] array to determine which command is being run
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm vitest run src/lib/__tests__/execution-summary.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - DB update receives correct git stats, log, summary, exitCode, and sessionId when worktree is valid
    - DB update receives null git fields when worktreePath is null
    - ANSI stripping confirmed via terminalLog content
    - Large buffer trimming confirmed (<= 10KB)
    - Function never throws even when DB fails
    - Background AI summary generation is triggered
  </acceptance_criteria>
  <done>execution-summary.test.ts passes with all 6 scenarios covering git data, ANSI stripping, trimming, error resilience, and AI summary trigger</done>
</task>

</tasks>

<verification>
Both test files pass:
```bash
pnpm vitest run src/lib/__tests__/assistant-sessions.test.ts src/lib/__tests__/execution-summary.test.ts
```
</verification>

<success_criteria>
1. assistant-sessions.ts CRUD, cap, title truncation, and active session management all tested
2. execution-summary.ts git data capture, ANSI stripping, buffer trimming, error resilience, and AI summary trigger all tested
3. Zero test failures
</success_criteria>

<output>
After completion, create `.planning/phases/51-core-lib-test-coverage/51-03-SUMMARY.md`
</output>
