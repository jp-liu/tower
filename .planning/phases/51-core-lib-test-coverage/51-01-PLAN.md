---
phase: 51-core-lib-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/__tests__/schemas.test.ts
  - src/lib/__tests__/diff-parser.test.ts
  - src/lib/__tests__/file-serve.test.ts
autonomous: true
requirements: [COV-15, COV-16, COV-17]

must_haves:
  truths:
    - "Every Zod schema rejects empty-string required fields, over-max-length strings, and invalid enum values"
    - "parseDiffOutput correctly structures numstat+unified diff into DiffFile arrays with totals"
    - "resolveAssetPath blocks directory traversal and MIME_MAP returns correct types"
  artifacts:
    - path: "src/lib/__tests__/schemas.test.ts"
      provides: "Boundary-value tests for all Zod schemas"
      min_lines: 80
    - path: "src/lib/__tests__/diff-parser.test.ts"
      provides: "parseDiffOutput tests for standard, added-only, removed-only, binary, empty input"
      min_lines: 60
    - path: "src/lib/__tests__/file-serve.test.ts"
      provides: "resolveAssetPath traversal + MIME_MAP tests"
      min_lines: 40
  key_links:
    - from: "src/lib/__tests__/schemas.test.ts"
      to: "src/lib/schemas.ts"
      via: "import all exported schemas"
      pattern: "import.*from.*schemas"
    - from: "src/lib/__tests__/diff-parser.test.ts"
      to: "src/lib/diff-parser.ts"
      via: "import parseDiffOutput"
      pattern: "import.*parseDiffOutput"
    - from: "src/lib/__tests__/file-serve.test.ts"
      to: "src/lib/file-serve.ts"
      via: "import resolveAssetPath, MIME_MAP"
      pattern: "import.*resolveAssetPath.*MIME_MAP"
---

<objective>
Unit tests for three pure-function library modules: schemas.ts, diff-parser.ts, and file-serve.ts.

Purpose: These modules handle input validation, git diff parsing, and file path security — all critical for correctness and safety.
Output: Three test files covering boundary values, edge cases, and error paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/schemas.ts
@src/lib/diff-parser.ts
@src/lib/file-serve.ts
@src/lib/file-serve-client.ts
@src/lib/__tests__/internal-api-guard.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: schemas.ts + diff-parser.ts unit tests</name>
  <files>src/lib/__tests__/schemas.test.ts, src/lib/__tests__/diff-parser.test.ts</files>
  <read_first>
    - src/lib/schemas.ts — all exported Zod schemas and their constraints
    - src/lib/diff-parser.ts — parseDiffOutput and checkConflicts functions
    - src/lib/__tests__/internal-api-guard.test.ts — test file pattern reference
  </read_first>
  <behavior>
    schemas.test.ts:
    - createWorkspaceSchema: accepts valid {name, description?}, rejects empty name, rejects name >100 chars, rejects description >500 chars
    - createProjectSchema: accepts valid project, rejects empty name, validates enum values for projectType (FRONTEND/BACKEND only), accepts empty string for gitUrl
    - createTaskSchema: accepts valid task, rejects empty title, rejects title >200 chars, validates priority enum (LOW/MEDIUM/HIGH/CRITICAL), validates status enum (all 5 values)
    - updateTaskSchema: all fields optional so empty object passes, but title="" (min 1) fails
    - taskStatusSchema: accepts all 5 valid values, rejects "UNKNOWN"
    - createLabelSchema: accepts valid hex color #aabbcc, rejects "red", rejects "#GGG000", rejects name >50 chars

    diff-parser.test.ts:
    - parseDiffOutput with standard numstat+unified diff: returns correct files array with added/removed counts and patch content
    - parseDiffOutput with added-only hunk (0 removed): totalRemoved is 0
    - parseDiffOutput with removed-only hunk (0 added): totalAdded is 0
    - parseDiffOutput with binary file (- - filename): isBinary=true, added=0, removed=0
    - parseDiffOutput with empty inputs ("", ""): returns {files: [], totalAdded: 0, totalRemoved: 0}
    - parseDiffOutput with large diff (>500KB unified): patches are truncated to 200 lines
  </behavior>
  <action>
    Create src/lib/__tests__/schemas.test.ts (per COV-15):
    - Import all exported schemas from ../schemas
    - For each schema, test .safeParse with valid data (success: true) and invalid data (success: false)
    - Boundary values: empty strings where min(1), strings at max+1 length, invalid enum values
    - Hex color regex: test #aabbcc (valid), #AABBCC (valid), "red" (invalid), "#GGG000" (invalid), "#aabb" (invalid, too short)

    Create src/lib/__tests__/diff-parser.test.ts (per COV-16):
    - Import parseDiffOutput from ../diff-parser
    - Do NOT test checkConflicts (requires real git) — only test parseDiffOutput which is pure
    - Use realistic numstat and unified diff strings as test fixtures
    - Numstat format: "10\t5\tfile.ts" for normal, "-\t-\timage.png" for binary
    - Unified diff format: "diff --git a/file.ts b/file.ts\n--- a/file.ts\n+++ b/file.ts\n@@ ... @@\n+added line\n-removed line"
    - For truncation test: generate a unified diff with >200 lines per file segment and verify "... (truncated)" suffix when total >500KB
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm vitest run src/lib/__tests__/schemas.test.ts src/lib/__tests__/diff-parser.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - schemas.test.ts has tests for all 8 exported schemas with boundary values (empty, max+1, invalid enum)
    - diff-parser.test.ts has tests for standard, added-only, removed-only, binary, empty, and truncation cases
    - All tests pass
  </acceptance_criteria>
  <done>All schema boundary tests and diff-parser edge case tests pass green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: file-serve.ts unit tests</name>
  <files>src/lib/__tests__/file-serve.test.ts</files>
  <read_first>
    - src/lib/file-serve.ts — resolveAssetPath, MIME_MAP, re-export
    - src/lib/file-serve-client.ts — localPathToApiUrl function
  </read_first>
  <behavior>
    - resolveAssetPath("proj123", "image.png"): returns resolved path under data/assets/proj123/image.png, error null
    - resolveAssetPath("proj123", "../../../etc/passwd"): returns null resolved, error "Invalid path"
    - resolveAssetPath("proj123", "sub/../../../etc/passwd"): traversal blocked
    - MIME_MAP[".png"] === "image/png", MIME_MAP[".jpg"] === "image/jpeg", MIME_MAP[".svg"] === "image/svg+xml"
    - MIME_MAP[".unknown"] === undefined (unknown extension not in map)
    - localPathToApiUrl("data/assets/abc123/file.png"): returns "/api/files/assets/abc123/file.png"
    - localPathToApiUrl("/absolute/data/assets/abc123/file.png"): returns API URL
    - localPathToApiUrl("unrelated/path.png"): returns input unchanged
  </behavior>
  <action>
    Create src/lib/__tests__/file-serve.test.ts (per COV-17):
    - Import { resolveAssetPath, MIME_MAP } from ../file-serve
    - Import { localPathToApiUrl } from ../file-serve-client (test the re-exported function directly from its source)
    - resolveAssetPath tests:
      - Valid filename: check resolved contains expected path segments, error is null
      - Traversal with "../": check resolved is null, error is "Invalid path"
      - Traversal with encoded or nested "../sub/../../": check blocked
    - MIME_MAP tests: spot-check 4-5 extensions for correct MIME types, verify unknown ext returns undefined
    - localPathToApiUrl tests: match pattern, non-match returns original
    - Note: resolveAssetPath uses process.cwd() for DATA_ROOT — tests run from project root so paths will resolve correctly
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm vitest run src/lib/__tests__/file-serve.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Path traversal attempts are confirmed blocked (resolved=null, error="Invalid path")
    - All MIME_MAP spot checks pass
    - localPathToApiUrl correctly transforms asset paths and passes through non-matching paths
  </acceptance_criteria>
  <done>file-serve.test.ts passes with traversal blocking, MIME checks, and URL conversion all verified</done>
</task>

</tasks>

<verification>
All three test files pass:
```bash
pnpm vitest run src/lib/__tests__/schemas.test.ts src/lib/__tests__/diff-parser.test.ts src/lib/__tests__/file-serve.test.ts
```
</verification>

<success_criteria>
1. schemas.ts boundary-value tests cover empty strings, max lengths, invalid enums for all 8 schemas
2. diff-parser.ts tests cover standard, added-only, removed-only, binary, empty, and truncation
3. file-serve.ts tests confirm traversal blocking and correct MIME types
4. Zero test failures
</success_criteria>

<output>
After completion, create `.planning/phases/51-core-lib-test-coverage/51-01-SUMMARY.md`
</output>
