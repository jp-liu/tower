---
phase: 29-adapter-dead-code-removal
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - src/lib/adapters/ (entire directory deleted)
  - src/app/api/tasks/[taskId]/execute/route.ts (deleted)
autonomous: true
requirements: [CLEAN-01, CLEAN-04, CLEAN-05]

must_haves:
  truths:
    - "src/lib/adapters/ directory no longer exists in the repository"
    - "src/app/api/tasks/[taskId]/execute/ directory no longer exists"
    - "tsc --noEmit exits with code 0"
    - "No file in src/ imports from @/lib/adapters/"
  artifacts:
    - path: "src/lib/adapters/"
      provides: "DELETED — must not exist"
    - path: "src/app/api/tasks/[taskId]/execute/"
      provides: "DELETED — must not exist"
  key_links:
    - from: "src/**/*.ts"
      to: "src/lib/adapters/"
      via: "import (must be zero)"
      pattern: "from.*@/lib/adapters"
---

<objective>
Delete all dead adapter code and the deprecated SSE execute route, then verify the build is clean.

Purpose: Complete the adapter cleanup — remove the entire adapters/ directory tree and the unused /api/tasks/[taskId]/execute route.
Output: Clean codebase with no dead adapter files, passing tsc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-adapter-dead-code-removal/29-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no remaining adapter imports, then delete dead files</name>
  <files>
    src/lib/adapters/claude-local/execute.ts (DELETE)
    src/lib/adapters/claude-local/parse.ts (DELETE)
    src/lib/adapters/claude-local/test.ts (DELETE)
    src/lib/adapters/claude-local/index.ts (DELETE)
    src/lib/adapters/registry.ts (DELETE)
    src/lib/adapters/types.ts (DELETE)
    src/lib/adapters/process-utils.ts (DELETE)
    src/lib/adapters/process-manager.ts (DELETE)
    src/lib/adapters/preview-process-manager.ts (DELETE)
    src/app/api/tasks/[taskId]/execute/route.ts (DELETE)
  </files>
  <read_first>
    src/lib/adapters/registry.ts
    src/lib/adapters/types.ts
    src/lib/adapters/process-manager.ts
    src/app/api/tasks/[taskId]/execute/route.ts
  </read_first>
  <action>
**Step 1 — Safety audit (MANDATORY before any deletion):**
Run `grep -r "from.*adapters" src/ --include="*.ts" --include="*.tsx"` and verify the ONLY remaining import sites are:
- Files inside `src/lib/adapters/` itself (about to be deleted)
- Zero external consumers (Plan 01 already migrated all of them)

If any unexpected external import is found, fix it FIRST before proceeding to deletion.

**Step 2 — Delete the entire adapters directory:**
```bash
rm -rf src/lib/adapters/
```
This removes all 9 files:
- `src/lib/adapters/claude-local/execute.ts`
- `src/lib/adapters/claude-local/parse.ts`
- `src/lib/adapters/claude-local/test.ts`
- `src/lib/adapters/claude-local/index.ts`
- `src/lib/adapters/registry.ts`
- `src/lib/adapters/types.ts`
- `src/lib/adapters/process-utils.ts`
- `src/lib/adapters/process-manager.ts`
- `src/lib/adapters/preview-process-manager.ts`

**Step 3 — Delete the deprecated execute route:**
```bash
rm -rf src/app/api/tasks/\[taskId\]/execute/
```
This removes `src/app/api/tasks/[taskId]/execute/route.ts` — the old SSE execution endpoint replaced by PTY in Phase 24-26.

**Step 4 — Verify no dangling references:**
Run `grep -r "adapters" src/ --include="*.ts" --include="*.tsx"` to catch any remaining string references (import paths, comments referencing deleted files, etc.). The only acceptable match is the route path `/api/adapters/test` in the test route itself (that route stays — it now imports from cli-test.ts).
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && test ! -d src/lib/adapters && test ! -f "src/app/api/tasks/[taskId]/execute/route.ts" && echo "DELETED_OK" || echo "DELETION_FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/lib/adapters/ 2>/dev/null` returns error (directory does not exist)
    - `ls src/app/api/tasks/\[taskId\]/execute/ 2>/dev/null` returns error (directory does not exist)
    - `grep -r "from.*@/lib/adapters" src/ --include="*.ts" --include="*.tsx"` returns zero matches
  </acceptance_criteria>
  <done>All 9 adapter files and 1 deprecated route file are deleted; no dangling import references remain</done>
</task>

<task type="auto">
  <name>Task 2: Run tsc --noEmit and fix any type errors</name>
  <files>Any file with type errors (expected: none)</files>
  <read_first>
    src/lib/cli-test.ts
    src/lib/preview-process.ts
    src/app/api/adapters/test/route.ts
  </read_first>
  <action>
Run `npx tsc --noEmit` from the project root. Expected outcome: exit code 0 with no new errors.

If there ARE errors:
1. Read each error carefully — it will likely be a missed import reference to a deleted file
2. Fix by updating the import to point to the new location:
   - `@/lib/adapters/types` -> `@/lib/cli-test` (for TestResult, TestCheck)
   - `@/lib/adapters/preview-process-manager` -> `@/lib/preview-process`
   - `@/lib/adapters/registry` -> remove entirely (registry is dead)
   - `@/lib/adapters/process-manager` -> remove entirely (process-manager is dead)
   - `@/lib/adapters/process-utils` -> should not exist outside cli-test.ts
3. Re-run `npx tsc --noEmit` after each fix until clean

Also run `npm run build` (or `pnpm build`) to confirm the Next.js build succeeds — this catches any runtime import issues tsc might miss (dynamic imports, route segments).

Note: If there are PRE-EXISTING type errors unrelated to this phase, document them but do not fix them. CLEAN-05 requires "no NEW type errors introduced by the reorganization."
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && npx tsc --noEmit 2>&1 | tail -5; echo "TSC_EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0
    - No error line contains "adapters" in the path
    - No error line references any deleted file
  </acceptance_criteria>
  <done>tsc --noEmit passes with zero new type errors; the build is clean after adapter removal</done>
</task>

</tasks>

<verification>
1. `test ! -d src/lib/adapters && echo PASS` — adapters directory gone
2. `test ! -d "src/app/api/tasks/[taskId]/execute" && echo PASS` — deprecated route gone
3. `grep -r "from.*@/lib/adapters" src/ --include="*.ts" --include="*.tsx" | wc -l` returns 0
4. `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- The entire src/lib/adapters/ directory tree is deleted (CLEAN-01)
- The /api/tasks/[taskId]/execute route is deleted (CLEAN-04)
- tsc --noEmit exits with code 0 (CLEAN-05)
- Zero imports from @/lib/adapters/ exist anywhere in src/
</success_criteria>

<output>
After completion, create `.planning/phases/29-adapter-dead-code-removal/29-02-SUMMARY.md`
</output>
