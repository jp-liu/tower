---
phase: 47-failing-test-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/lib/pty-session.test.ts
  - tests/unit/lib/preview-process-manager.test.ts
  - tests/unit/actions/preview-actions.test.ts
  - tests/unit/lib/instrumentation.test.ts
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "pty-session.test.ts passes all tests including exit listener test"
    - "preview-process-manager.test.ts passes all 5 tests with working .on() mock"
    - "preview-actions.test.ts passes all tests (unref test removed or aligned)"
    - "instrumentation.test.ts passes all 4 tests against refactored module"
  artifacts:
    - path: "tests/unit/lib/pty-session.test.ts"
      provides: "PTY session unit tests with setExitListener API"
      contains: "setExitListener"
    - path: "tests/unit/lib/preview-process-manager.test.ts"
      provides: "Preview process manager tests with .on() mock"
      contains: "on: vi.fn"
    - path: "tests/unit/actions/preview-actions.test.ts"
      provides: "Preview actions tests aligned with current source"
    - path: "tests/unit/lib/instrumentation.test.ts"
      provides: "Instrumentation tests matching refactored module structure"
      contains: "instrumentation-tasks"
  key_links:
    - from: "tests/unit/lib/pty-session.test.ts"
      to: "src/lib/pty/pty-session.ts"
      via: "setExitListener method call"
      pattern: "session\\.setExitListener"
    - from: "tests/unit/lib/preview-process-manager.test.ts"
      to: "src/lib/preview-process.ts"
      via: "child.on('exit') mock"
      pattern: "on:.*vi\\.fn"
---

<objective>
Fix all backend/lib test failures: pty-session (1 failure), preview-process-manager (5 failures), preview-actions (1 failure), instrumentation (4 failures).

Purpose: These 4 test files have 11 failures caused by API changes (addExitListener removed), missing mock methods (.on()), removed functionality (unref), and module refactoring (instrumentation split into instrumentation-tasks).

Output: All 4 test files pass with `pnpm test:run`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pty-session and preview-process-manager tests</name>
  <files>tests/unit/lib/pty-session.test.ts, tests/unit/lib/preview-process-manager.test.ts</files>
  <read_first>
    - src/lib/pty/pty-session.ts (current API — setExitListener replaces addExitListener, takes single fn)
    - src/lib/preview-process.ts (registerPreviewProcess calls child.on("exit", ...))
    - tests/unit/lib/pty-session.test.ts (test to fix)
    - tests/unit/lib/preview-process-manager.test.ts (test to fix)
  </read_first>
  <action>
**pty-session.test.ts (1 failure — line 99):**
The test "notifies all registered exit listeners" calls `session.addExitListener()` which no longer exists. The API changed to `setExitListener()` which REPLACES (not adds) listeners. Rewrite the test to match the new semantics:
- Replace both `session.addExitListener(listener1)` and `session.addExitListener(listener2)` with a single `session.setExitListener(listener2)` call
- Update the test name to "notifies the registered exit listener via setExitListener"
- After triggering exit, assert `listener2` was called with `0` (only the last-set listener fires)
- Remove the assertion on `listener1` since setExitListener replaces previous listeners

**preview-process-manager.test.ts (5 failures — all from missing `.on()`):**
The `makeFakeChild()` helper creates a mock ChildProcess missing the `.on()` method that `registerPreviewProcess` calls (`child.on("exit", ...)`). Fix `makeFakeChild`:
- Add `on: vi.fn()` to the returned object in `makeFakeChild()`
- The full return should be: `{ killed, kill: vi.fn(), pid: Math.floor(Math.random() * 10000), on: vi.fn() } as unknown as ChildProcess`
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm test:run -- tests/unit/lib/pty-session.test.ts tests/unit/lib/preview-process-manager.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep for "setExitListener" in tests/unit/lib/pty-session.test.ts returns at least 1 match
    - grep for "addExitListener" in tests/unit/lib/pty-session.test.ts returns 0 matches
    - grep for "on: vi.fn" in tests/unit/lib/preview-process-manager.test.ts returns at least 1 match
    - `pnpm test:run -- tests/unit/lib/pty-session.test.ts` exits 0
    - `pnpm test:run -- tests/unit/lib/preview-process-manager.test.ts` exits 0
  </acceptance_criteria>
  <done>pty-session.test.ts passes all tests using setExitListener; preview-process-manager.test.ts passes all 5 tests with .on() in mock</done>
</task>

<task type="auto">
  <name>Task 2: Fix preview-actions and instrumentation tests</name>
  <files>tests/unit/actions/preview-actions.test.ts, tests/unit/lib/instrumentation.test.ts</files>
  <read_first>
    - src/actions/preview-actions.ts (startPreview no longer calls child.unref())
    - src/instrumentation.ts (refactored — now imports from @/lib/instrumentation-tasks)
    - src/lib/instrumentation-tasks.ts (contains pruneOrphanedWorktrees and cleanupStaleExecutions)
    - tests/unit/actions/preview-actions.test.ts (test to fix)
    - tests/unit/lib/instrumentation.test.ts (test to fix)
  </read_first>
  <action>
**preview-actions.test.ts (1 failure — line 84):**
The test "calls unref() on the spawned child process" asserts `fakeChild.unref` was called, but `startPreview()` in the source no longer calls `child.unref()`. Remove this test entirely (lines 78-85, the `it("calls unref()...")` block). The behavior was intentionally removed.

**instrumentation.test.ts (4 failures — all tests):**
The source `src/instrumentation.ts` was refactored. It now:
1. Imports `pruneOrphanedWorktrees` and `cleanupStaleExecutions` from `@/lib/instrumentation-tasks`
2. Calls both functions (not direct db/execFileSync calls)
3. Also calls `startWsServer` and `ensureTowerDir`

The test must be rewritten to mock the new structure:

1. Replace the mock of `@/lib/db` and `child_process` with a mock of `@/lib/instrumentation-tasks`:
```typescript
vi.mock("@/lib/instrumentation-tasks", () => ({
  pruneOrphanedWorktrees: vi.fn().mockResolvedValue(undefined),
  cleanupStaleExecutions: vi.fn().mockResolvedValue(undefined),
}));
```

2. Also mock `@/lib/pty/ws-server` and `@/lib/init-tower`:
```typescript
vi.mock("@/lib/pty/ws-server", () => ({
  startWsServer: vi.fn().mockResolvedValue(undefined),
}));
vi.mock("@/lib/init-tower", () => ({
  ensureTowerDir: vi.fn(),
}));
```

3. Remove the `execFileSync` and `db` imports/mocks entirely.

4. Rewrite the 5 test cases to match the new structure:
   - Test 1 "calls pruneOrphanedWorktrees and cleanupStaleExecutions on startup": Import `register` from `@/instrumentation`, call `register()`, assert both mocked functions were called once, and `startWsServer` was called once, and `ensureTowerDir` was called once.
   - Test 2 "skips all tasks in non-nodejs runtime": Set `process.env.NEXT_RUNTIME = "edge"`, call `register()`, assert none of the mocked functions were called.
   - Test 3 "handles pruneOrphanedWorktrees failure gracefully": Make `pruneOrphanedWorktrees` reject, assert `register()` resolves (does not throw) — note: the current source does NOT try/catch these, so if the function itself handles errors internally (it does in instrumentation-tasks.ts), this test should verify `register()` completes. If it throws, the test should verify it propagates. Read the source carefully — `register()` awaits both calls without try/catch, but the individual functions have internal try/catch. So `register()` should complete.
   - Test 4 "handles cleanupStaleExecutions failure gracefully": Similar — `cleanupStaleExecutions` has internal try/catch.
   - Remove the old "continues to next project" and "completes without error when no GIT projects" tests — those tested internal implementation details now encapsulated in `instrumentation-tasks.ts`.

Use `vi.resetModules()` in beforeEach and dynamic `await import("@/instrumentation")` to get fresh module per test.
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm test:run -- tests/unit/actions/preview-actions.test.ts tests/unit/lib/instrumentation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep for "unref" in tests/unit/actions/preview-actions.test.ts returns 0 matches
    - grep for "instrumentation-tasks" in tests/unit/lib/instrumentation.test.ts returns at least 1 match
    - grep for "pruneOrphanedWorktrees" in tests/unit/lib/instrumentation.test.ts returns at least 1 match
    - grep for "cleanupStaleExecutions" in tests/unit/lib/instrumentation.test.ts returns at least 1 match
    - `pnpm test:run -- tests/unit/actions/preview-actions.test.ts` exits 0
    - `pnpm test:run -- tests/unit/lib/instrumentation.test.ts` exits 0
  </acceptance_criteria>
  <done>preview-actions.test.ts passes (unref test removed); instrumentation.test.ts passes with mocks matching refactored module structure</done>
</task>

</tasks>

<verification>
Run all 4 backend/lib test files together:
```bash
pnpm test:run -- tests/unit/lib/pty-session.test.ts tests/unit/lib/preview-process-manager.test.ts tests/unit/actions/preview-actions.test.ts tests/unit/lib/instrumentation.test.ts
```
Expected: 0 failures across all 4 files.
</verification>

<success_criteria>
- All 4 backend/lib test files pass with 0 failures
- No other test files are broken by these changes (test files only, no source changes)
- Total failing test count drops from 27 to 16 (11 fixed in this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/47-failing-test-fixes/47-01-SUMMARY.md`
</output>
