---
phase: 47-failing-test-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/components/board-stats.test.tsx
  - tests/unit/components/create-task-dialog.test.tsx
  - tests/unit/components/prompts-config.test.tsx
  - tests/unit/components/asset-item.test.tsx
autonomous: true
requirements: [TEST-04, TEST-05, TEST-06]

must_haves:
  truths:
    - "board-stats.test.tsx passes all 3 tests with I18nProvider wrapper"
    - "create-task-dialog.test.tsx passes all 4 tests with getCurrentBranch in mock"
    - "prompts-config.test.tsx passes all 8 tests with router mock"
    - "asset-item.test.tsx passes with correct URL assertion matching real localPathToApiUrl"
  artifacts:
    - path: "tests/unit/components/board-stats.test.tsx"
      provides: "BoardStats tests with I18nProvider"
      contains: "I18nProvider"
    - path: "tests/unit/components/create-task-dialog.test.tsx"
      provides: "CreateTaskDialog tests with getCurrentBranch mock"
      contains: "getCurrentBranch"
    - path: "tests/unit/components/prompts-config.test.tsx"
      provides: "PromptsConfig tests with router mock"
      contains: "useRouter"
    - path: "tests/unit/components/asset-item.test.tsx"
      provides: "AssetItem tests with correct URL assertion"
      contains: "/api/files/assets/"
  key_links:
    - from: "tests/unit/components/board-stats.test.tsx"
      to: "src/lib/i18n.tsx"
      via: "I18nProvider wrapper"
      pattern: "I18nProvider"
    - from: "tests/unit/components/prompts-config.test.tsx"
      to: "next/navigation"
      via: "vi.mock useRouter"
      pattern: "vi\\.mock.*next/navigation"
    - from: "tests/unit/components/asset-item.test.tsx"
      to: "src/lib/file-serve-client.ts"
      via: "localPathToApiUrl mock"
      pattern: "file-serve-client"
---

<objective>
Fix all component test failures: board-stats (3 failures), create-task-dialog (2 failures), prompts-config (8 failures), asset-item (1 failure).

Purpose: Component tests fail due to missing React context providers (I18nProvider, Router), incomplete mock exports (getCurrentBranch), and wrong mock module path/URL assertion (file-serve vs file-serve-client).

Output: All 4 component test files pass with `pnpm test:run`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix board-stats, create-task-dialog, and prompts-config tests (context providers)</name>
  <files>tests/unit/components/board-stats.test.tsx, tests/unit/components/create-task-dialog.test.tsx, tests/unit/components/prompts-config.test.tsx</files>
  <read_first>
    - tests/unit/components/board-stats.test.tsx (missing I18nProvider — "useI18n must be used within I18nProvider")
    - tests/unit/components/create-task-dialog.test.tsx (mock of @/actions/git-actions missing getCurrentBranch)
    - tests/unit/components/prompts-config.test.tsx ("invariant expected app router to be mounted")
    - tests/unit/components/search-dialog.test.tsx (reference — shows correct pattern for mocking next/navigation with vi.mock)
    - src/components/board/board-stats.tsx (uses useI18n)
    - src/components/settings/prompts-config.tsx (uses useRouter from next/navigation)
    - src/actions/git-actions.ts (exports getCurrentBranch)
  </read_first>
  <action>
**board-stats.test.tsx (3 failures — all from missing I18nProvider):**
The `BoardStats` component calls `useI18n()` which requires `I18nProvider` in the render tree. Fix:
1. Add import: `import { I18nProvider } from "@/lib/i18n";`
2. Wrap each `render(<BoardStats ... />)` call with I18nProvider:
   - `render(<I18nProvider><BoardStats totalTasks={5} runningTasks={3} /></I18nProvider>);`
   - Apply to all 3 test cases

**create-task-dialog.test.tsx (2 failures — missing getCurrentBranch in git-actions mock):**
Error: `No "getCurrentBranch" export is defined on the "@/actions/git-actions" mock`
The component imports `getCurrentBranch` from `@/actions/git-actions` but the mock only provides `getProjectBranches`. Fix:
1. Add `getCurrentBranch` to the `vi.mock("@/actions/git-actions", ...)` factory:
```typescript
vi.mock("@/actions/git-actions", () => ({
  getProjectBranches: vi.fn().mockResolvedValue(["main", "develop"]),
  getCurrentBranch: vi.fn().mockResolvedValue("main"),
}));
```

**prompts-config.test.tsx (8 failures — all from missing router context):**
Error: `invariant expected app router to be mounted`
The `PromptsConfig` component uses `useRouter()` from `next/navigation`. Fix by adding a `vi.mock` for `next/navigation` BEFORE any component imports, following the pattern from `search-dialog.test.tsx`:
1. Add this mock block near the top of the file (after the vitest imports, before component imports):
```typescript
vi.mock("next/navigation", () => ({
  useRouter: () => ({ push: vi.fn(), refresh: vi.fn() }),
}));
```
Note: `refresh` is needed because `prompts-config.tsx` calls `router.refresh()` after create/delete/setDefault operations.

Also add mock for `next/cache` since the prompt-actions use `revalidatePath`:
```typescript
vi.mock("next/cache", () => ({
  revalidatePath: vi.fn(),
}));
```
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm test:run -- tests/unit/components/board-stats.test.tsx tests/unit/components/create-task-dialog.test.tsx tests/unit/components/prompts-config.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep for "I18nProvider" in tests/unit/components/board-stats.test.tsx returns at least 1 match
    - grep for "getCurrentBranch" in tests/unit/components/create-task-dialog.test.tsx returns at least 2 matches (mock + import)
    - grep for "useRouter" in tests/unit/components/prompts-config.test.tsx returns at least 1 match
    - grep for "next/navigation" in tests/unit/components/prompts-config.test.tsx returns at least 1 match
    - `pnpm test:run -- tests/unit/components/board-stats.test.tsx` exits 0
    - `pnpm test:run -- tests/unit/components/create-task-dialog.test.tsx` exits 0
    - `pnpm test:run -- tests/unit/components/prompts-config.test.tsx` exits 0
  </acceptance_criteria>
  <done>board-stats (3 tests), create-task-dialog (4 tests), and prompts-config (8 tests) all pass</done>
</task>

<task type="auto">
  <name>Task 2: Fix asset-item test URL assertion</name>
  <files>tests/unit/components/asset-item.test.tsx</files>
  <read_first>
    - tests/unit/components/asset-item.test.tsx (mocks @/lib/file-serve but component imports from @/lib/file-serve-client)
    - src/components/assets/asset-item.tsx (imports localPathToApiUrl from @/lib/file-serve-client)
    - src/lib/file-serve-client.ts (localPathToApiUrl extracts projectId and filename from path: "data/assets/{projectId}/{filename}" -> "/api/files/assets/{projectId}/{filename}")
  </read_first>
  <action>
**asset-item.test.tsx (1 failure — wrong URL assertion):**

Two problems:
1. The test mocks `@/lib/file-serve` but the component imports from `@/lib/file-serve-client`. The mock is never applied.
2. The URL assertion expects `/api/files/data/assets/proj-1/photo.png` but the real `localPathToApiUrl` function returns `/api/files/assets/proj-1/photo.png` (it extracts projectId/filename, not the full path).

Fix:
1. Change the mock target from `@/lib/file-serve` to `@/lib/file-serve-client`:
```typescript
vi.mock("@/lib/file-serve-client", () => ({
  localPathToApiUrl: (p: string) => {
    const match = p.match(/(?:^|\/)data\/assets\/([^/]+)\/([^/]+)$/);
    if (match) return `/api/files/assets/${match[1]}/${match[2]}`;
    return p;
  },
}));
```

Alternatively, since this is just testing the component rendering (not the URL transform), use the simplest approach — mock that returns the real behavior. The simplest fix:

1. Change mock module path from `@/lib/file-serve` to `@/lib/file-serve-client`
2. Update the mock to match real behavior: `localPathToApiUrl: (p: string) => { const m = p.match(/data\/assets\/([^/]+)\/([^/]+)$/); return m ? \`/api/files/assets/${m[1]}/${m[2]}\` : p; }`
3. Update the assertion on line 63 from:
   `expect(downloadLink).toHaveAttribute("href", "/api/files/data/assets/proj-1/photo.png");`
   to:
   `expect(downloadLink).toHaveAttribute("href", "/api/files/assets/proj-1/photo.png");`
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm test:run -- tests/unit/components/asset-item.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep for "file-serve-client" in tests/unit/components/asset-item.test.tsx returns at least 1 match
    - grep for "file-serve\"" in tests/unit/components/asset-item.test.tsx returns 0 matches (old mock path removed)
    - grep for "/api/files/assets/proj-1/photo.png" in tests/unit/components/asset-item.test.tsx returns at least 1 match
    - `pnpm test:run -- tests/unit/components/asset-item.test.tsx` exits 0
  </acceptance_criteria>
  <done>asset-item.test.tsx passes with correct mock module path and URL assertion</done>
</task>

</tasks>

<verification>
Run all 4 component test files together:
```bash
pnpm test:run -- tests/unit/components/board-stats.test.tsx tests/unit/components/create-task-dialog.test.tsx tests/unit/components/prompts-config.test.tsx tests/unit/components/asset-item.test.tsx
```
Expected: 0 failures across all 4 files.

Then run full test suite:
```bash
pnpm test:run
```
Expected: 0 failures total (combined with Plan 01 fixes).
</verification>

<success_criteria>
- All 4 component test files pass with 0 failures
- No other test files are broken by these changes
- Combined with Plan 01, total failing tests drops from 27 to 0
</success_criteria>

<output>
After completion, create `.planning/phases/47-failing-test-fixes/47-02-SUMMARY.md`
</output>
