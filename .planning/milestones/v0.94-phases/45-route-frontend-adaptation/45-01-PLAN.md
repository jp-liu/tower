---
phase: 45-route-frontend-adaptation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/internal/cache/[...segments]/route.ts
  - src/lib/build-multimodal-prompt.ts
  - src/lib/__tests__/build-multimodal-prompt.test.ts
  - src/app/api/internal/assistant/chat/route.ts
autonomous: true
requirements: [ROUTE-01, ROUTE-02, ROUTE-03]

must_haves:
  truths:
    - "GET /api/internal/cache/2026-04/images/foo.png resolves to the correct file in data/cache/assistant/2026-04/images/foo.png"
    - "Path traversal via segments like ['..','..','etc','passwd'] is rejected with 400"
    - "Frontend image URLs using sub-path filenames (e.g. 2026-04/images/xxx.png) render correctly without any frontend code change"
    - "buildMultimodalPrompt accepts sub-path filenames and resolves them to absolute filesystem paths"
    - "Chat route IMAGE_FILENAME_RE accepts sub-path format filenames and passes them to buildMultimodalPrompt"
  artifacts:
    - path: "src/app/api/internal/cache/[...segments]/route.ts"
      provides: "Catch-all cache serving route"
      contains: "segments"
    - path: "src/lib/build-multimodal-prompt.ts"
      provides: "Sub-path aware multimodal prompt builder"
      contains: "SAFE_SUBPATH_RE"
    - path: "src/app/api/internal/assistant/chat/route.ts"
      provides: "Sub-path aware image filename validation"
      contains: "SAFE_SUBPATH_RE"
  key_links:
    - from: "src/app/api/internal/assistant/chat/route.ts"
      to: "src/lib/build-multimodal-prompt.ts"
      via: "buildMultimodalPrompt call with sub-path filenames"
      pattern: "buildMultimodalPrompt.*safeImageFilenames.*getAssistantCacheRoot"
    - from: "src/app/api/internal/cache/[...segments]/route.ts"
      to: "data/cache/assistant/YYYY-MM/images/"
      via: "path.resolve(cacheRoot, subPath)"
      pattern: "getAssistantCacheRoot"
---

<objective>
Convert the cache serving route from single-segment `[filename]` to catch-all `[...segments]`, update all validation regexes from UUID-only to sub-path format, update tests, and delete old flat-path cache files.

Purpose: After Phase 44 changed the upload response to return sub-paths like `2026-04/images/foo.png`, the serving route and validation regexes still only accept UUID-format flat filenames. This plan closes that gap so the full upload-to-display chain works end-to-end.

Output: Working catch-all route, updated regex validators, passing tests, no old flat cache files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-cache-storage-refactor/44-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from codebase -->

From src/lib/file-utils.ts:
```typescript
export type CacheFileType = "images" | "files";
export function getAssistantCacheDir(type?: CacheFileType): string;
export function getAssistantCacheRoot(): string;
export function buildCacheFilename(originalName: string, ext: string): string;
```

From src/lib/file-serve.ts:
```typescript
export const MIME_MAP: Record<string, string>;
```

From src/lib/internal-api-guard.ts:
```typescript
export function requireLocalhost(request: NextRequest): NextResponse | null;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace [filename] route with [...segments] catch-all route</name>
  <files>
    src/app/api/internal/cache/[filename]/route.ts (DELETE)
    src/app/api/internal/cache/[...segments]/route.ts (CREATE)
  </files>
  <read_first>
    src/app/api/internal/cache/[filename]/route.ts
    src/lib/file-utils.ts
    src/lib/file-serve.ts
  </read_first>
  <action>
1. Delete the directory `src/app/api/internal/cache/[filename]/` entirely (rm -rf).
2. Create `src/app/api/internal/cache/[...segments]/route.ts` with:
   - `export const dynamic = "force-dynamic"` and `export const runtime = "nodejs"`
   - Import `getAssistantCacheRoot` from `@/lib/file-utils` (NOT `getAssistantCacheDir`)
   - Import `requireLocalhost` from `@/lib/internal-api-guard`
   - Import `MIME_MAP` from `@/lib/file-serve`
   - Define `SUBPATH_RE = /^\d{4}-\d{2}\/(images|files)\/[^/]+\.(jpg|jpeg|png|gif|webp)$/i` — uses `[^/]+` for the filename segment to allow Unicode/Chinese chars
   - `GET` handler signature: `{ params }: { params: Promise<{ segments: string[] }> }`
   - Await params: `const { segments } = await params`
   - Join segments: `const subPath = segments.join("/")`
   - Validate with `SUBPATH_RE.test(subPath)` — return 400 if invalid
   - Resolve path: `const cacheRoot = getAssistantCacheRoot(); const resolved = path.resolve(cacheRoot, subPath)`
   - Containment check: `resolved.startsWith(cacheRoot + path.sep)` — return 400 if fails
   - Read file with `fs.promises.readFile(resolved)` — return 404 on ENOENT, 500 on other errors
   - Get content type from `MIME_MAP[ext]` with fallback to `application/octet-stream`
   - Return `new Response(new Uint8Array(bytes), { status: 200, headers: { "Content-Type": contentType, "Cache-Control": "private, max-age=3600" } })`
   - Do NOT call `decodeURIComponent` — Next.js auto-decodes segments

IMPORTANT: Do NOT use `getAssistantCacheDir` — it creates directories. Use `getAssistantCacheRoot()` which just returns the base path.
  </action>
  <verify>
    <automated>ls src/app/api/internal/cache/\[...segments\]/route.ts && ! ls src/app/api/internal/cache/\[filename\]/route.ts 2>/dev/null && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/api/internal/cache/[...segments]/route.ts` exists
    - File `src/app/api/internal/cache/[filename]/route.ts` does NOT exist
    - Contains `SUBPATH_RE` regex with `\d{4}-\d{2}\/(images|files)\/[^/]+`
    - Contains `getAssistantCacheRoot` import (not `getAssistantCacheDir`)
    - Contains `params: Promise<{ segments: string[] }>`
    - Contains containment check `cacheRoot + path.sep`
  </acceptance_criteria>
  <done>Old [filename] route deleted, new [...segments] catch-all route serves files from sub-path structure with regex validation, path traversal protection, and correct MIME types</done>
</task>

<task type="auto">
  <name>Task 2: Update validation regexes in chat route and buildMultimodalPrompt + fix tests</name>
  <files>
    src/app/api/internal/assistant/chat/route.ts
    src/lib/build-multimodal-prompt.ts
    src/lib/__tests__/build-multimodal-prompt.test.ts
  </files>
  <read_first>
    src/app/api/internal/assistant/chat/route.ts
    src/lib/build-multimodal-prompt.ts
    src/lib/__tests__/build-multimodal-prompt.test.ts
  </read_first>
  <action>
1. In `src/lib/build-multimodal-prompt.ts`:
   - Replace `SAFE_FILENAME_RE` with `SAFE_SUBPATH_RE = /^\d{4}-\d{2}\/(images|files)\/[^/]+\.(jpg|jpeg|png|gif|webp)$/i`
   - Update the filter call: `.filter((subPath) => SAFE_SUBPATH_RE.test(subPath))`
   - The `path.resolve(cacheDir, subPath)` call already works correctly because `cacheDir` is `getAssistantCacheRoot()` and `subPath` is `2026-04/images/foo.png` — path.resolve handles this
   - Update JSDoc: change `imageFilenames` param description to "Array of sub-paths (e.g. '2026-04/images/name.png') relative to cacheDir"

2. In `src/app/api/internal/assistant/chat/route.ts`:
   - Replace `IMAGE_FILENAME_RE` (UUID-only, line 47-48) with the same sub-path regex: `const IMAGE_FILENAME_RE = /^\d{4}-\d{2}\/(images|files)\/[^/]+\.(jpg|jpeg|png|gif|webp)$/i`
   - The rest of the code (filter, buildMultimodalPrompt call, getAssistantCacheRoot()) needs no change

3. In `src/lib/__tests__/build-multimodal-prompt.test.ts`:
   - Change `CACHE_DIR` to `"/abs/cache/assistant"` (represents the assistant cache root)
   - Replace all UUID constants with sub-path format:
     - `UUID1` -> `SUBPATH1 = "2026-04/images/design-a1b2c3d4.png"`
     - `UUID2` -> `SUBPATH2 = "2026-04/images/photo-b2c3d4e5.jpg"`
     - `UUID3` -> `SUBPATH3 = "2025-12/images/test-c3d4e5f6.webp"`
     - `UUID_EXISTS` -> `SUBPATH_EXISTS = "2026-04/images/exists-d4e5f6a7.png"`
     - `UUID_MISSING` -> `SUBPATH_MISSING = "2026-04/images/missing-e5f6a7b8.jpg"`
   - Update `manyImages` generation (line 102-104) to produce sub-path format: `` `2026-04/images/img-${String(i).padStart(8, "0")}.png` ``
   - Update all assertions: `expect(result).toContain(...)` values to use new sub-path constants and new CACHE_DIR
   - Update the "rejects filenames with path traversal sequences" test: keep `../../etc/passwd` and `../.env` as rejection cases, but change the valid filename to `SUBPATH1`
   - Update the "rejects filenames that don't match UUID format" test: rename to "rejects filenames that don't match sub-path format", use `"not-valid.png"` and `"abc.jpg"` as rejection cases (no YYYY-MM/ prefix), keep `SUBPATH1` as the valid case
   - Add a new test: "accepts sub-paths with Chinese characters" — use `"2026-04/images/\u8bbe\u8ba1\u7a3f-a1b2c3d4.png"` (设计稿), mock existsSync to return true, assert the path appears in the result
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm test:run --reporter=verbose src/lib/__tests__/build-multimodal-prompt.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/build-multimodal-prompt.ts` contains `SAFE_SUBPATH_RE` and does NOT contain `SAFE_FILENAME_RE`
    - `src/app/api/internal/assistant/chat/route.ts` IMAGE_FILENAME_RE matches `^\d{4}-\d{2}\/(images|files)\/`
    - All tests in `build-multimodal-prompt.test.ts` pass
    - Test file uses sub-path format constants (SUBPATH1 etc.), not UUID constants
    - Test for Chinese character filenames exists and passes
  </acceptance_criteria>
  <done>Both regex validators accept the new YYYY-MM/type/filename.ext sub-path format, old UUID-only validation is removed, all tests pass with updated fixtures including Chinese filename test</done>
</task>

<task type="auto">
  <name>Task 3: Delete old flat-path cache files and verify full chain</name>
  <files>
    data/cache/assistant/ (cleanup)
  </files>
  <read_first>
    src/app/api/internal/cache/[...segments]/route.ts
    src/lib/build-multimodal-prompt.ts
  </read_first>
  <action>
1. Delete the two old flat UUID files from `data/cache/assistant/`:
   - `data/cache/assistant/b415b3ca-3fe4-4260-8d1c-bfb69ef4d42f.png`
   - `data/cache/assistant/da7798ca-4ab2-4d26-b183-7ee02f60ddad.png`
   Use `rm -f` for each file. Verify no flat files remain in the assistant cache root (only subdirectories like `2026-04/` should exist).

2. Run the full test suite to verify nothing is broken:
   `pnpm test:run`

3. Verify the dev server starts without route conflicts:
   Start `pnpm dev` briefly and check for "Conflicting routes" errors in output. Kill after confirming clean startup.
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && ls data/cache/assistant/*.png 2>&1 | grep -c "No such file" && pnpm test:run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - No `.png` files exist directly in `data/cache/assistant/` (only subdirectories)
    - `pnpm test:run` passes with 0 failures
    - No route conflict errors from Next.js
  </acceptance_criteria>
  <done>Old flat-path cache files deleted, full test suite passes, dev server starts cleanly with catch-all route</done>
</task>

</tasks>

<verification>
1. `pnpm test:run` — all tests pass
2. `ls src/app/api/internal/cache/[...segments]/route.ts` — catch-all route exists
3. `! ls src/app/api/internal/cache/[filename]/route.ts 2>/dev/null` — old route removed
4. `grep SAFE_SUBPATH_RE src/lib/build-multimodal-prompt.ts` — new regex in place
5. `grep 'images|files' src/app/api/internal/assistant/chat/route.ts` — chat route regex updated
6. `ls data/cache/assistant/*.png 2>/dev/null` — no flat files remain
</verification>

<success_criteria>
- Catch-all route serves files at sub-paths like `/api/internal/cache/2026-04/images/foo.png`
- Path traversal attempts are rejected with 400
- buildMultimodalPrompt resolves sub-path filenames to correct absolute paths
- Chat route accepts sub-path format in imageFilenames validation
- All tests pass including new Chinese filename test
- No old flat UUID files in assistant cache root
- ROUTE-01, ROUTE-02, and ROUTE-03 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/45-route-frontend-adaptation/45-01-SUMMARY.md`
</output>
