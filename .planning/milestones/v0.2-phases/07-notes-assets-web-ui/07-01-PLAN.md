---
phase: 07-notes-assets-web-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n.tsx
  - src/components/layout/app-sidebar.tsx
  - src/app/workspaces/[workspaceId]/notes/page.tsx
  - src/app/workspaces/[workspaceId]/notes/notes-page-client.tsx
  - src/components/notes/note-list.tsx
  - src/components/notes/note-editor.tsx
  - src/components/notes/note-card.tsx
  - src/components/notes/category-filter.tsx
  - tests/unit/components/note-card.test.tsx
  - tests/unit/components/category-filter.test.tsx
  - tests/unit/components/note-editor.test.tsx
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "User can navigate to a notes page for any project in a workspace"
    - "User can see existing notes listed, filtered by category"
    - "User can create a new note with title, content (Markdown), and category"
    - "User can edit an existing note in a Markdown editor"
    - "User can delete a note"
    - "All notes UI strings appear correctly in both Chinese and English"
  artifacts:
    - path: "src/app/workspaces/[workspaceId]/notes/page.tsx"
      provides: "Server Component fetching notes data"
      contains: "await params"
    - path: "src/app/workspaces/[workspaceId]/notes/notes-page-client.tsx"
      provides: "Client Component with full notes interactivity"
      contains: "use client"
    - path: "src/components/notes/note-editor.tsx"
      provides: "Markdown editor (dynamic @uiw or textarea+react-markdown fallback)"
      contains: "use client"
    - path: "src/components/notes/note-card.tsx"
      provides: "Single note display with edit/delete actions"
    - path: "src/components/notes/category-filter.tsx"
      provides: "Category filter button group"
    - path: "src/lib/i18n.tsx"
      provides: "i18n keys for notes and assets pages"
      contains: "notes.title"
    - path: "tests/unit/components/note-card.test.tsx"
      provides: "Unit tests for NoteCard component"
    - path: "tests/unit/components/category-filter.test.tsx"
      provides: "Unit tests for CategoryFilter component"
    - path: "tests/unit/components/note-editor.test.tsx"
      provides: "Unit tests for NoteEditor component"
  key_links:
    - from: "src/app/workspaces/[workspaceId]/notes/page.tsx"
      to: "src/actions/note-actions.ts"
      via: "getProjectNotes import"
      pattern: "getProjectNotes"
    - from: "src/app/workspaces/[workspaceId]/notes/notes-page-client.tsx"
      to: "src/actions/note-actions.ts"
      via: "createNote, updateNote, deleteNote imports"
      pattern: "createNote|updateNote|deleteNote"
    - from: "src/components/layout/app-sidebar.tsx"
      to: "/workspaces/{id}/notes"
      via: "Link component"
      pattern: "notes"
---

<objective>
Build the Notes page for project knowledge management, including sidebar navigation, i18n strings, a Markdown editor, category filtering, and full CRUD operations.

Purpose: Users need a web interface to manage project notes (UI-01). All backing server actions (createNote, updateNote, deleteNote, getProjectNotes) already exist from Phase 4.
Output: A fully functional Notes page at `/workspaces/[workspaceId]/notes?projectId=...` with Markdown editing, category filter, and bilingual UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-notes-assets-web-ui/07-RESEARCH.md

@src/actions/note-actions.ts
@src/lib/constants.ts
@src/lib/i18n.tsx
@src/lib/file-serve.ts
@src/app/workspaces/[workspaceId]/archive/page.tsx
@src/app/workspaces/[workspaceId]/archive/archive-page-client.tsx
@src/components/layout/app-sidebar.tsx
@tests/unit/components/board-stats.test.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From prisma/schema.prisma:
```prisma
model ProjectNote {
  id        String   @id @default(cuid())
  title     String
  content   String
  category  String   @default("备忘")
  projectId String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  project   Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  @@index([projectId])
}
```

From src/actions/note-actions.ts:
```typescript
export async function createNote(data: { title: string; content: string; category?: string; projectId: string })
export async function updateNote(noteId: string, data: { title?: string; content?: string; category?: string })
export async function deleteNote(noteId: string)
export async function getProjectNotes(projectId: string, options?: { category?: string })
export async function getNoteById(noteId: string)
```

From src/lib/constants.ts:
```typescript
export const NOTE_CATEGORIES_PRESET = ["账号", "环境", "需求", "备忘"] as const;
export type NoteCategoryPreset = (typeof NOTE_CATEGORIES_PRESET)[number];
```

From src/lib/i18n.tsx:
```typescript
export type Locale = "zh" | "en";
// useI18n() returns { locale, setLocale, t }
// t(key) returns translated string; t(key, { name: "value" }) for interpolation
// translations object has zh: {} and en: {} sections
```

From archive/page.tsx (pattern to follow):
```typescript
interface Props {
  params: Promise<{ workspaceId: string }>;
  searchParams: Promise<{ projectId?: string }>;
}
// Always: const { workspaceId } = await params;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n strings and sidebar navigation links</name>
  <files>src/lib/i18n.tsx, src/components/layout/app-sidebar.tsx</files>
  <read_first>src/lib/i18n.tsx, src/components/layout/app-sidebar.tsx</read_first>
  <action>
1. In `src/lib/i18n.tsx`, add the following keys to BOTH `zh` and `en` sections of the `translations` object. Place them after the existing sidebar/board/task keys.

**zh section keys to add:**
```
"sidebar.notes": "笔记",
"sidebar.assets": "资源",
"notes.title": "笔记",
"notes.newNote": "新建笔记",
"notes.allCategories": "全部分类",
"notes.category.account": "账号",
"notes.category.env": "环境",
"notes.category.requirements": "需求",
"notes.category.memo": "备忘",
"notes.edit": "编辑",
"notes.delete": "删除",
"notes.save": "保存",
"notes.cancel": "取消",
"notes.titlePlaceholder": "笔记标题",
"notes.contentPlaceholder": "笔记内容（支持 Markdown）",
"notes.categoryLabel": "分类",
"notes.empty": "暂无笔记",
"notes.emptyHint": "点击「新建笔记」创建第一条笔记",
"notes.deleteConfirm": "确认删除笔记「{title}」？",
"notes.noProject": "当前工作空间没有项目",
"notes.noProjectHint": "请先创建一个项目",
"assets.title": "资源",
"assets.upload": "上传文件",
"assets.empty": "暂无资源",
"assets.emptyHint": "上传文件作为项目资源",
"assets.filename": "文件名",
"assets.size": "大小",
"assets.uploadedAt": "上传时间",
"assets.preview": "预览",
"assets.download": "下载",
"assets.delete": "删除",
"assets.deleteConfirm": "确认删除资源「{filename}」？",
"assets.noProject": "当前工作空间没有项目",
"assets.noProjectHint": "请先创建一个项目",
"assets.uploading": "上传中...",
```

**en section — same keys, English values:**
```
"sidebar.notes": "Notes",
"sidebar.assets": "Assets",
"notes.title": "Notes",
"notes.newNote": "New Note",
"notes.allCategories": "All",
"notes.category.account": "Account",
"notes.category.env": "Environment",
"notes.category.requirements": "Requirements",
"notes.category.memo": "Memo",
"notes.edit": "Edit",
"notes.delete": "Delete",
"notes.save": "Save",
"notes.cancel": "Cancel",
"notes.titlePlaceholder": "Note title",
"notes.contentPlaceholder": "Note content (Markdown supported)",
"notes.categoryLabel": "Category",
"notes.empty": "No notes yet",
"notes.emptyHint": "Click \"New Note\" to create your first note",
"notes.deleteConfirm": "Delete note \"{title}\"?",
"notes.noProject": "No projects in this workspace",
"notes.noProjectHint": "Create a project first",
"assets.title": "Assets",
"assets.upload": "Upload File",
"assets.empty": "No assets yet",
"assets.emptyHint": "Upload files as project assets",
"assets.filename": "Filename",
"assets.size": "Size",
"assets.uploadedAt": "Uploaded At",
"assets.preview": "Preview",
"assets.download": "Download",
"assets.delete": "Delete",
"assets.deleteConfirm": "Delete asset \"{filename}\"?",
"assets.noProject": "No projects in this workspace",
"assets.noProjectHint": "Create a project first",
"assets.uploading": "Uploading...",
```

2. In `src/components/layout/app-sidebar.tsx`:
   - Add `FileText, FolderOpen` to the lucide-react imports (alongside existing Archive import).
   - In the footer section, BEFORE the existing Archive link, add two navigation links:
     - Notes link: `href={/workspaces/${activeWorkspaceId}/notes}` with FileText icon and `t("sidebar.notes")` text.
     - Assets link: `href={/workspaces/${activeWorkspaceId}/assets}` with FolderOpen icon and `t("sidebar.assets")` text.
   - Use the exact same className pattern as the existing Archive link (the `<Link>` with classes `"relative z-10 flex items-center gap-2 border-t border-border px-4 py-3 text-[11px] text-muted-foreground cursor-pointer hover:text-foreground transition-colors"`).
   - Only the first link (Notes) should have `border-t`; Assets and Archive links should NOT have `border-t` to avoid double borders.
   - Wrap Notes + Assets + Archive links inside the same conditional block that checks `activeWorkspaceId`.
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && grep -c "notes\." src/lib/i18n.tsx | head -1 && grep -c "assets\." src/lib/i18n.tsx | head -1 && grep "sidebar.notes" src/lib/i18n.tsx && grep "FileText" src/components/layout/app-sidebar.tsx && grep "/notes" src/components/layout/app-sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "notes.title" src/lib/i18n.tsx` returns matches in both zh and en sections (at least 2 lines)
    - `grep "assets.title" src/lib/i18n.tsx` returns matches in both zh and en sections (at least 2 lines)
    - `grep "sidebar.notes" src/lib/i18n.tsx` returns 2 matches (zh + en)
    - `grep "sidebar.assets" src/lib/i18n.tsx` returns 2 matches (zh + en)
    - `grep "FileText" src/components/layout/app-sidebar.tsx` returns at least 1 match
    - `grep "/notes" src/components/layout/app-sidebar.tsx` returns at least 1 match
    - `grep "/assets" src/components/layout/app-sidebar.tsx` returns at least 1 match
    - `pnpm tsc --noEmit` passes without errors
  </acceptance_criteria>
  <done>All i18n keys for notes and assets pages exist in both zh and en. Sidebar has navigation links to /notes and /assets pages.</done>
</task>

<task type="auto">
  <name>Task 2: Build Notes page with server component, client component, and all note sub-components</name>
  <files>
    src/app/workspaces/[workspaceId]/notes/page.tsx,
    src/app/workspaces/[workspaceId]/notes/notes-page-client.tsx,
    src/components/notes/note-list.tsx,
    src/components/notes/note-editor.tsx,
    src/components/notes/note-card.tsx,
    src/components/notes/category-filter.tsx
  </files>
  <read_first>
    src/app/workspaces/[workspaceId]/archive/page.tsx,
    src/app/workspaces/[workspaceId]/archive/archive-page-client.tsx,
    src/actions/note-actions.ts,
    src/lib/constants.ts,
    src/lib/i18n.tsx,
    src/components/board/board-filters.tsx
  </read_first>
  <action>
**Step 0 — Test @uiw/react-md-editor:**
Run `pnpm add @uiw/react-md-editor` to install it. Then create a minimal test: in `note-editor.tsx`, use `dynamic(() => import("@uiw/react-md-editor"), { ssr: false })`. Run `pnpm build` (or `pnpm dev` and check for errors). If hydration or build errors occur, remove `@uiw/react-md-editor` (`pnpm remove @uiw/react-md-editor`) and use the textarea + react-markdown fallback instead. This is per the locked decision in STATE.md.

**Step 1 — Create `src/components/notes/category-filter.tsx`:**
- `"use client"` component
- Props: `{ active: string; onSelect: (cat: string) => void }`
- Import `NOTE_CATEGORIES_PRESET` from `@/lib/constants`
- Import `useI18n` from `@/lib/i18n`
- Render button group: first button is "All" (`t("notes.allCategories")`) with value `"all"`, then one button per preset category
- Active button styled with `bg-amber-500/20 text-amber-300 ring-1 ring-amber-500/20`
- Inactive button styled with `text-muted-foreground hover:bg-accent`
- Use `cn()` from `@/lib/utils` for conditional classes
- Named export: `CategoryFilter`

**Step 2 — Create `src/components/notes/note-card.tsx`:**
- `"use client"` component
- Props: `{ note: { id: string; title: string; content: string; category: string; updatedAt: Date }; onEdit: (note: NoteItem) => void; onDelete: (noteId: string) => void }`
- Define `NoteItem` interface matching the props note shape
- Import `Pencil`, `Trash2` from lucide-react
- Import `ReactMarkdown` from `react-markdown` and `remarkGfm` from `remark-gfm`
- Import `useI18n` from `@/lib/i18n`
- Render: card with title, category badge (rounded-full bg-muted), edit/delete icon buttons in top-right, content preview truncated to 300 chars rendered via ReactMarkdown inside `prose prose-sm dark:prose-invert max-h-24 overflow-hidden`
- Named export: `NoteCard`

**Step 3 — Create `src/components/notes/note-editor.tsx`:**
- `"use client"` component
- If @uiw/react-md-editor installed and working: use `dynamic(() => import("@uiw/react-md-editor"), { ssr: false })` and render `<MDEditor value={value} onChange={(v) => onChange(v ?? "")} height={400} />`
- If fallback: render side-by-side grid with `<textarea>` on left and `<ReactMarkdown>` preview on right, both in a `grid grid-cols-2 gap-4 h-96` container. Textarea: `resize-none font-mono text-sm h-full border rounded-md p-3 bg-background text-foreground`. Preview: `prose prose-sm dark:prose-invert overflow-y-auto border rounded-md p-3`
- Props: `{ value: string; onChange: (val: string) => void }`
- Named export: `NoteEditor`

**Step 4 — Create `src/components/notes/note-list.tsx`:**
- `"use client"` component
- Props: `{ notes: NoteItem[]; onEdit: (note: NoteItem) => void; onDelete: (noteId: string) => void }`
- Import `NoteCard` from `./note-card`
- Import `useI18n` from `@/lib/i18n`
- If notes is empty, render empty state: centered text with `t("notes.empty")` and `t("notes.emptyHint")` in muted-foreground
- Otherwise render a grid of NoteCards: `grid gap-4 sm:grid-cols-2 lg:grid-cols-3`
- Named export: `NoteList`

**Step 5 — Create `src/app/workspaces/[workspaceId]/notes/page.tsx`:**
- Server Component (NO "use client")
- Follow the exact pattern from `archive/page.tsx`:
  - Props type: `{ params: Promise<{ workspaceId: string }>; searchParams: Promise<{ projectId?: string }> }`
  - `const { workspaceId } = await params;`
  - `const { projectId: selectedProjectId } = await searchParams;`
  - Fetch workspace with projects: `db.workspace.findUnique({ where: { id: workspaceId }, include: { projects: { select: { id: true, name: true, alias: true }, orderBy: { createdAt: "asc" } } } })`
  - `if (!workspace) notFound();`
  - If no projects, render `NotesPageClient` with empty projects array
  - Determine active project: `selectedProjectId ? find(...) ?? projects[0] : projects[0]`
  - Fetch notes: `const notes = project ? await getProjectNotes(project.id) : [];`
  - Return `<NotesPageClient workspaceId={workspaceId} project={project} projects={workspace.projects} initialNotes={notes} />`

**Step 6 — Create `src/app/workspaces/[workspaceId]/notes/notes-page-client.tsx`:**
- `"use client"` component
- Props: `{ workspaceId: string; project?: { id: string; name: string; alias: string | null }; projects: { id: string; name: string; alias: string | null }[]; initialNotes: ProjectNote[] }` where ProjectNote is from `@prisma/client`
- State: `notes` (initialized from initialNotes), `activeCategory` (string, default "all"), `editingNote` (NoteItem | null), `isCreating` (boolean), `isPending` (useTransition)
- Import `useRouter` from `next/navigation`, `useTransition` from `react`
- Import all note components: `CategoryFilter`, `NoteList`, `NoteEditor`
- Import server actions: `createNote`, `updateNote`, `deleteNote` from `@/actions/note-actions`
- Import `useI18n` from `@/lib/i18n`
- Import `NOTE_CATEGORIES_PRESET` from `@/lib/constants`

**Layout structure:**
- Header row: back link to board (`/workspaces/${workspaceId}`), page title `t("notes.title")`, project selector dropdown (simple `<select>` that navigates to `?projectId=xxx` on change), "New Note" button
- If no projects: show `t("notes.noProject")` + `t("notes.noProjectHint")` centered
- Below header: `CategoryFilter` component
- Main area: if `isCreating` or `editingNote`, show the note form (title input + category select + NoteEditor + save/cancel buttons); otherwise show `NoteList`

**Note form (inline, not dialog):**
- Title: `<input>` with placeholder `t("notes.titlePlaceholder")`
- Category: `<select>` with options from `NOTE_CATEGORIES_PRESET`
- Content: `<NoteEditor value={content} onChange={setContent} />`
- Buttons: Save (`t("notes.save")`) and Cancel (`t("notes.cancel")`)

**Handlers:**
- `handleCreate`: call `createNote({ title, content, category, projectId: project.id })`, then `startTransition(() => router.refresh())`, set isCreating=false
- `handleUpdate`: call `updateNote(editingNote.id, { title, content, category })`, then `startTransition(() => router.refresh())`, set editingNote=null
- `handleDelete`: `if (confirm(t("notes.deleteConfirm", { title })))` then call `deleteNote(noteId)`, then `startTransition(() => router.refresh())`
- `handleProjectChange`: `router.push(/workspaces/${workspaceId}/notes?projectId=${newProjectId})`
- Category filter: filter `initialNotes` client-side: `activeCategory === "all" ? initialNotes : initialNotes.filter(n => n.category === activeCategory)`

**Sync initialNotes prop to local state:**
- Use `useEffect` to update local notes state when `initialNotes` prop changes (same pattern as board-page-client.tsx syncing tasks)

Named export: `NotesPageClient`
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm tsc --noEmit 2>&1 | tail -5 && test -f src/app/workspaces/\[workspaceId\]/notes/page.tsx && test -f src/app/workspaces/\[workspaceId\]/notes/notes-page-client.tsx && test -f src/components/notes/note-editor.tsx && test -f src/components/notes/note-card.tsx && test -f src/components/notes/category-filter.tsx && test -f src/components/notes/note-list.tsx && echo "ALL_FILES_EXIST"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` passes
    - `grep "await params" src/app/workspaces/\[workspaceId\]/notes/page.tsx` matches (Next.js 16 Promise params)
    - `grep "use client" src/app/workspaces/\[workspaceId\]/notes/notes-page-client.tsx` matches
    - `grep "getProjectNotes" src/app/workspaces/\[workspaceId\]/notes/page.tsx` matches
    - `grep "createNote\|updateNote\|deleteNote" src/app/workspaces/\[workspaceId\]/notes/notes-page-client.tsx` matches all three
    - `grep "CategoryFilter" src/app/workspaces/\[workspaceId\]/notes/notes-page-client.tsx` matches
    - `grep "NoteEditor" src/app/workspaces/\[workspaceId\]/notes/notes-page-client.tsx` matches
    - `grep "NOTE_CATEGORIES_PRESET" src/components/notes/category-filter.tsx` matches
    - `grep "ReactMarkdown\|react-md-editor" src/components/notes/note-editor.tsx` matches (one or the other)
    - `grep "useI18n" src/app/workspaces/\[workspaceId\]/notes/notes-page-client.tsx` matches
    - All 6 files exist in their expected paths
  </acceptance_criteria>
  <done>Notes page is fully functional: user can navigate to /workspaces/{id}/notes, see notes filtered by category, create/edit/delete notes with Markdown editor, and all strings are bilingual.</done>
</task>

<task type="auto">
  <name>Task 3: Create unit test stubs for note components</name>
  <files>
    tests/unit/components/note-card.test.tsx,
    tests/unit/components/category-filter.test.tsx,
    tests/unit/components/note-editor.test.tsx
  </files>
  <read_first>
    tests/unit/components/board-stats.test.tsx,
    src/components/notes/note-card.tsx,
    src/components/notes/category-filter.tsx,
    src/components/notes/note-editor.tsx
  </read_first>
  <action>
Follow the existing test pattern from `tests/unit/components/board-stats.test.tsx` (import from vitest, use @testing-library/react render+screen, afterEach cleanup).

**1. Create `tests/unit/components/note-card.test.tsx`:**
```typescript
import { describe, it, expect, afterEach, vi } from "vitest";
import { render, screen, cleanup, fireEvent } from "@testing-library/react";
import { NoteCard } from "@/components/notes/note-card";
```
Tests:
- Renders note title text
- Renders category badge text
- Renders truncated content preview (pass content with >300 chars, verify it does not render full text)
- Calls onEdit when edit button is clicked (use `vi.fn()`, find button by accessible role or icon, click, assert called with note)
- Calls onDelete when delete button is clicked (use `vi.fn()`, click, assert called with note id)

**2. Create `tests/unit/components/category-filter.test.tsx`:**
```typescript
import { describe, it, expect, afterEach, vi } from "vitest";
import { render, screen, cleanup, fireEvent } from "@testing-library/react";
import { CategoryFilter } from "@/components/notes/category-filter";
```
Tests:
- Renders "All" button (zh default: "全部分类")
- Renders all 4 preset category buttons
- Active category button has active styling class (check for `bg-amber-500/20` or similar)
- Calls onSelect with category value when a category button is clicked (use `vi.fn()`)

**3. Create `tests/unit/components/note-editor.test.tsx`:**
```typescript
import { describe, it, expect, afterEach, vi } from "vitest";
import { render, screen, cleanup, fireEvent } from "@testing-library/react";
import { NoteEditor } from "@/components/notes/note-editor";
```
Tests:
- Renders with initial value (check textarea or editor contains value text)
- Calls onChange when user types in textarea (use fireEvent.change on textarea, assert onChange called with new value)
- Note: If @uiw/react-md-editor is used with dynamic import, the editor may not render in jsdom. In that case, mock the dynamic import and test that the component renders without error. If textarea fallback is used, test the textarea directly.
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && pnpm vitest run tests/unit/components/note-card.test.tsx tests/unit/components/category-filter.test.tsx tests/unit/components/note-editor.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 note component test files exist and pass. Tests verify rendering behavior (title, category, content preview, active state) and callback invocations (onEdit, onDelete, onSelect, onChange).</done>
</task>

</tasks>

<verification>
1. `pnpm tsc --noEmit` passes with no errors
2. `pnpm vitest run tests/unit/components/note-card.test.tsx tests/unit/components/category-filter.test.tsx tests/unit/components/note-editor.test.tsx` all pass
3. `pnpm dev` starts without errors, navigating to `/workspaces/{id}/notes` shows the notes page
4. All i18n keys exist in both zh and en sections
5. Sidebar shows Notes and Assets navigation links
6. Notes CRUD operations work (create, edit, delete)
7. Category filter correctly filters the notes list
8. Markdown editor renders (either @uiw or fallback textarea+preview)
</verification>

<success_criteria>
- Notes page accessible at `/workspaces/[workspaceId]/notes?projectId=...`
- Notes list shows existing notes with title, category badge, content preview
- Category filter shows All + 4 preset categories with active state
- Create note form has title, category, Markdown editor, save/cancel
- Edit note pre-fills the form with existing note data
- Delete note shows confirmation dialog
- Sidebar has Notes and Assets navigation links
- All UI strings display in both Chinese and English
- Unit tests for note-card, category-filter, note-editor all pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-notes-assets-web-ui/07-01-SUMMARY.md`
</output>
