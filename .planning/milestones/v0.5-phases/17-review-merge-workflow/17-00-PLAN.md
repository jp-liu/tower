---
phase: 17-review-merge-workflow
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/api/stream-persist-result.test.ts
  - tests/unit/api/diff-route.test.ts
  - tests/unit/api/merge-route.test.ts
  - tests/unit/api/stream-send-back.test.ts
autonomous: true
requirements: [MR-01, MR-02, MR-03, RV-01]

must_haves:
  truths:
    - "All four test stub files exist and are runnable by vitest"
    - "Each stub contains at least one describe block and one test.todo or failing test placeholder"
  artifacts:
    - path: "tests/unit/api/stream-persist-result.test.ts"
      provides: "Test stubs for IN_REVIEW transition in persistResult"
      contains: "describe"
    - path: "tests/unit/api/diff-route.test.ts"
      provides: "Test stubs for diff API response shape"
      contains: "describe"
    - path: "tests/unit/api/merge-route.test.ts"
      provides: "Test stubs for squash merge and conflict detection"
      contains: "describe"
    - path: "tests/unit/api/stream-send-back.test.ts"
      provides: "Test stubs for send-back transition"
      contains: "describe"
  key_links: []
---

<objective>
Create Wave 0 test stub files required by VALIDATION.md so that subsequent plans can reference them in their verify blocks.

Purpose: Establishes the test scaffolding before any implementation begins, satisfying Nyquist requirements.
Output: Four test stub files under tests/unit/api/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-review-merge-workflow/17-CONTEXT.md
@.planning/phases/17-review-merge-workflow/17-VALIDATION.md

@tests/unit/api/file-serving.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create four test stub files for Phase 17 API routes</name>
  <files>tests/unit/api/stream-persist-result.test.ts, tests/unit/api/diff-route.test.ts, tests/unit/api/merge-route.test.ts, tests/unit/api/stream-send-back.test.ts</files>
  <read_first>
    - tests/unit/api/file-serving.test.ts (existing test file pattern — imports, describe structure, mock patterns)
    - vitest.config.ts (test configuration)
  </read_first>
  <action>
Create four test stub files following the existing pattern from `file-serving.test.ts`. Each file uses vitest `describe`/`it`/`test.todo` and mocks relevant modules.

**1. `tests/unit/api/stream-persist-result.test.ts`** (MR-01: IN_REVIEW transition):
```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";

describe("stream route - persistResult IN_REVIEW transition", () => {
  it.todo("sets task status to IN_REVIEW when exitCode is 0");
  it.todo("does NOT set IN_REVIEW when exitCode is non-zero");
  it.todo("emits status_changed SSE event after IN_REVIEW transition");
});
```

**2. `tests/unit/api/diff-route.test.ts`** (MR-01: diff API):
```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";

describe("GET /api/tasks/[taskId]/diff", () => {
  it.todo("returns 404 when task does not exist");
  it.todo("returns 400 when task has no baseBranch");
  it.todo("returns 400 when project has no localPath");
  it.todo("returns diff response with files, totalAdded, totalRemoved, hasConflicts, commitCount");
  it.todo("validates taskId with Zod");
});
```

**3. `tests/unit/api/merge-route.test.ts`** (MR-02, MR-03: merge + conflicts):
```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";

describe("POST /api/tasks/[taskId]/merge", () => {
  it.todo("returns 400 when task is not IN_REVIEW");
  it.todo("returns 409 when conflicts detected");
  it.todo("performs squash merge and sets task status to DONE on success");
  it.todo("returns 400 when baseBranch is not set");
  it.todo("validates taskId with Zod");
});
```

**4. `tests/unit/api/stream-send-back.test.ts`** (RV-01: send-back):
```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";

describe("stream route - send-back flow", () => {
  it.todo("transitions IN_REVIEW task to IN_PROGRESS before creating execution");
  it.todo("does NOT transition task if status is not IN_REVIEW");
  it.todo("creates a new TaskExecution record for send-back");
});
```

Each file should import from vitest and use `describe`/`it.todo` for now. These are stubs that will be filled in during or after plan execution. Ensure all four files pass `npx vitest run tests/unit/api/ --passWithNoTests`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/api/stream-persist-result.test.ts tests/unit/api/diff-route.test.ts tests/unit/api/merge-route.test.ts tests/unit/api/stream-send-back.test.ts --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>All four test stub files exist under tests/unit/api/, each containing describe blocks with todo test cases. vitest runs them without errors.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/api/ --passWithNoTests` passes
- All four files exist: stream-persist-result.test.ts, diff-route.test.ts, merge-route.test.ts, stream-send-back.test.ts
- Each file contains at least one `describe` block
</verification>

<success_criteria>
- Four test stub files created matching VALIDATION.md Wave 0 requirements
- All stubs are runnable by vitest (no syntax errors)
- Subsequent plans can reference these files in their verify blocks
</success_criteria>

<output>
After completion, create `.planning/phases/17-review-merge-workflow/17-00-SUMMARY.md`
</output>
