---
phase: 15-schema-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/branch-template.ts
  - src/lib/config-defaults.ts
  - src/components/settings/system-config.tsx
  - src/components/task/task-detail-panel.tsx
  - src/lib/i18n.tsx
  - tests/unit/lib/branch-template.test.ts
autonomous: true
requirements: [CL-01]

must_haves:
  truths:
    - "No file in the codebase imports or references branch-template.ts"
    - "The settings page has no branch template input field"
    - "CONFIG_DEFAULTS has no git.branchTemplate entry"
    - "The i18n translations have no branchTemplate keys"
    - "The task detail panel shows a fixed branch format instead of a configurable template"
    - "The project builds successfully with no missing module errors"
  artifacts:
    - path: "src/lib/config-defaults.ts"
      provides: "CONFIG_DEFAULTS without git.branchTemplate"
    - path: "src/components/settings/system-config.tsx"
      provides: "Git params section with only timeout, no branchTemplate UI"
    - path: "src/components/task/task-detail-panel.tsx"
      provides: "Fixed branch display using task/{taskId} format"
    - path: "src/lib/i18n.tsx"
      provides: "Translations without branchTemplate keys, updated gitParams.desc"
  key_links:
    - from: "src/components/task/task-detail-panel.tsx"
      to: "src/components/task/task-metadata.tsx"
      via: "branch prop with fixed task/{id} value"
      pattern: "branch=.*task/"
    - from: "src/components/settings/system-config.tsx"
      to: "src/actions/config-actions.ts"
      via: "handleSaveGitParams only saves git.timeoutSec"
      pattern: "setConfigValue.*git\\.timeoutSec"
---

<objective>
Remove all branchTemplate configuration code from the codebase -- the v0.5 milestone uses a fixed `task/{taskId}` branch format, making the configurable template dead code.

Purpose: The v0.5 decision (STATE.md) mandates fixed branch naming `task/{taskId}`. The old branchTemplate config (added in Phase 13) is now dead code. Removing it prevents confusion and stale references.

Output: Deleted branch-template.ts and its test, cleaned config-defaults.ts, system-config.tsx, task-detail-panel.tsx, and i18n.tsx. Build succeeds with zero branchTemplate references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-schema-cleanup/15-RESEARCH.md

@src/lib/branch-template.ts
@src/lib/config-defaults.ts
@src/components/settings/system-config.tsx
@src/components/task/task-detail-panel.tsx
@src/lib/i18n.tsx
@tests/unit/lib/branch-template.test.ts

<interfaces>
<!-- TaskMetadata accepts branch as optional string -- safe to change value or keep passing -->
From src/components/task/task-metadata.tsx:
```typescript
interface TaskMetadataProps {
  title: string;
  description?: string;
  branch?: string;  // optional -- can pass fixed value or omit
  hasConversation: boolean;
  updatedAt: Date;
  onBack: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete branch-template lib and test, clean config-defaults</name>
  <files>src/lib/branch-template.ts, tests/unit/lib/branch-template.test.ts, src/lib/config-defaults.ts</files>
  <read_first>
    - src/lib/branch-template.ts (entire file to confirm it only contains interpolateBranchTemplate and validateBranchTemplate)
    - tests/unit/lib/branch-template.test.ts (entire file to confirm it only tests branch-template.ts functions)
    - src/lib/config-defaults.ts (find the "git.branchTemplate" entry to remove)
  </read_first>
  <action>
    1. DELETE the file `src/lib/branch-template.ts` entirely (10 lines, contains `interpolateBranchTemplate` and `validateBranchTemplate`).

    2. DELETE the file `tests/unit/lib/branch-template.test.ts` entirely (57 lines, tests for the deleted functions).

    3. In `src/lib/config-defaults.ts`, remove the `"git.branchTemplate"` entry (5 lines):
       ```
       // DELETE these lines:
       "git.branchTemplate": {
         defaultValue: "vk/{taskIdShort}-",
         type: "string",
         label: "Branch Naming Template",
       },
       ```
       After removal, the CONFIG_DEFAULTS object should have 8 entries (down from 9): git.pathMappingRules, system.maxUploadBytes, system.maxConcurrentExecutions, git.timeoutSec, search.resultLimit, search.allModeCap, search.debounceMs, search.snippetLength.
  </action>
  <verify>
    <automated>test ! -f src/lib/branch-template.ts && test ! -f tests/unit/lib/branch-template.test.ts && ! grep -q "git.branchTemplate" src/lib/config-defaults.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/branch-template.ts does not exist
    - tests/unit/lib/branch-template.test.ts does not exist
    - src/lib/config-defaults.ts does NOT contain the string "git.branchTemplate"
    - src/lib/config-defaults.ts does NOT contain the string "Branch Naming Template"
    - src/lib/config-defaults.ts still contains "git.timeoutSec" (adjacent entry preserved)
    - src/lib/config-defaults.ts still contains "search.resultLimit" (other entries preserved)
  </acceptance_criteria>
  <done>branch-template.ts and its test file are deleted, git.branchTemplate removed from CONFIG_DEFAULTS, 8 config entries remain.</done>
</task>

<task type="auto">
  <name>Task 2: Clean branchTemplate from settings UI, task panel, and i18n</name>
  <files>src/components/settings/system-config.tsx, src/components/task/task-detail-panel.tsx, src/lib/i18n.tsx</files>
  <read_first>
    - src/components/settings/system-config.tsx (lines 7, 35, 47, 49, 57, 70, 87-93, 473-489 for all branchTemplate references)
    - src/components/task/task-detail-panel.tsx (lines 9-10, 29, 69-72, 206 for branchTemplate references)
    - src/lib/i18n.tsx (lines 208, 211-213, 519, 522-524 for branchTemplate i18n keys)
    - src/components/task/task-metadata.tsx (confirm branch prop is optional -- line 11: `branch?: string`)
  </read_first>
  <action>
    **In `src/components/settings/system-config.tsx`:**

    1. Remove the import line: `import { validateBranchTemplate } from "@/lib/branch-template";`

    2. Change the GitParamsForm type from `{ timeoutSec: number; branchTemplate: string }` to `{ timeoutSec: number }`.

    3. Change the initial state from `{ timeoutSec: 30, branchTemplate: "vk/{taskIdShort}-" }` to `{ timeoutSec: 30 }`.

    4. Remove the line: `const [branchTemplateError, setBranchTemplateError] = useState("");`

    5. In the `getConfigValues` call array (useEffect around line 53), remove the `"git.branchTemplate"` entry. The array should go from 8 keys to 7 keys:
       ```typescript
       getConfigValues([
         "system.maxUploadBytes",
         "system.maxConcurrentExecutions",
         "git.timeoutSec",
         "search.resultLimit",
         "search.allModeCap",
         "search.debounceMs",
         "search.snippetLength",
       ])
       ```

    6. In the state mapping from config values, remove the `branchTemplate` line:
       ```typescript
       setGitParamsForm({
         timeoutSec: (cfg["git.timeoutSec"] as number) ?? 30,
       });
       ```

    7. Replace the entire `handleSaveGitParams` function body with:
       ```typescript
       const handleSaveGitParams = async () => {
         await setConfigValue("git.timeoutSec", gitParamsForm.timeoutSec);
       };
       ```
       (Remove the validateBranchTemplate check, setBranchTemplateError, and the git.branchTemplate setConfigValue call.)

    8. Remove the entire `{/* Branch template */}` UI block (lines 473-489 approximately):
       ```
       {/* Branch template */}
       <div className="flex items-center gap-4">
         ... branchTemplate label, hint, error, input ...
       </div>
       ```
       Keep the timeout `<div>` and the save `<Button>` intact.

    **In `src/components/task/task-detail-panel.tsx`:**

    9. Remove the import: `import { interpolateBranchTemplate } from "@/lib/branch-template";`

    10. Remove the import: `import { getConfigValue } from "@/actions/config-actions";`
        (Confirmed: getConfigValue is only used for branchTemplate in this file.)

    11. Remove the state: `const [branchTemplate, setBranchTemplate] = useState("vk/{taskIdShort}-");`

    12. Remove the useEffect that loads branchTemplate config (lines 69-72):
        ```typescript
        // DELETE this entire useEffect:
        useEffect(() => {
          getConfigValue<string>("git.branchTemplate", "vk/{taskIdShort}-").then(setBranchTemplate);
        }, []);
        ```

    13. Change the branch prop on `<TaskMetadata>` from:
        ```
        branch={interpolateBranchTemplate(branchTemplate, task.id)}
        ```
        to:
        ```
        branch={`task/${task.id}`}
        ```
        This matches the v0.5 fixed branch convention from STATE.md: "Branch name is task/{taskId}".

    **In `src/lib/i18n.tsx`:**

    14. In the ZH locale object, remove these 3 keys (around lines 211-213):
        ```
        "settings.config.gitParams.branchTemplate": "...",
        "settings.config.gitParams.branchTemplateHint": "...",
        "settings.config.gitParams.branchTemplateInvalid": "...",
        ```

    15. In the ZH locale, update the desc key (around line 208) from:
        ```
        "settings.config.gitParams.desc": "Git 操作超时与分支命名模板",
        ```
        to:
        ```
        "settings.config.gitParams.desc": "Git 操作超时",
        ```

    16. In the EN locale object, remove these 3 keys (around lines 522-524):
        ```
        "settings.config.gitParams.branchTemplate": "...",
        "settings.config.gitParams.branchTemplateHint": "...",
        "settings.config.gitParams.branchTemplateInvalid": "...",
        ```

    17. In the EN locale, update the desc key (around line 519) from:
        ```
        "settings.config.gitParams.desc": "Git operation timeout and branch naming template",
        ```
        to:
        ```
        "settings.config.gitParams.desc": "Git operation timeout",
        ```
  </action>
  <verify>
    <automated>! grep -rn "branchTemplate\|branch-template\|interpolateBranchTemplate\|validateBranchTemplate" src/ --include="*.ts" --include="*.tsx" && pnpm build && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "branchTemplate" src/` returns zero results
    - `grep -rn "branch-template" src/` returns zero results
    - `grep -rn "interpolateBranchTemplate" src/` returns zero results
    - `grep -rn "validateBranchTemplate" src/` returns zero results
    - src/components/settings/system-config.tsx does NOT contain the string "branchTemplate"
    - src/components/settings/system-config.tsx does NOT import from "@/lib/branch-template"
    - src/components/task/task-detail-panel.tsx does NOT import from "@/lib/branch-template"
    - src/components/task/task-detail-panel.tsx does NOT import getConfigValue from "@/actions/config-actions"
    - src/components/task/task-detail-panel.tsx contains `branch={`task/${task.id}`}`
    - src/lib/i18n.tsx line containing "gitParams.desc" in ZH reads "Git 操作超时" (not "与分支命名模板")
    - src/lib/i18n.tsx line containing "gitParams.desc" in EN reads "Git operation timeout" (not "and branch naming template")
    - src/lib/i18n.tsx does NOT contain "gitParams.branchTemplate"
    - `pnpm build` exits 0
    - `pnpm vitest run` exits 0 (remaining tests still pass)
  </acceptance_criteria>
  <done>Zero references to branchTemplate remain in src/. Settings page shows only git timeout. Task panel shows fixed `task/{taskId}` branch. i18n keys cleaned. Build and all tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -rn "branchTemplate\|branch-template\|interpolateBranchTemplate\|validateBranchTemplate" src/ tests/` returns zero results
2. `pnpm vitest run --reporter=verbose` — all remaining tests pass
3. `pnpm build` — builds successfully with no module-not-found errors
</verification>

<success_criteria>
- src/lib/branch-template.ts deleted
- tests/unit/lib/branch-template.test.ts deleted
- git.branchTemplate removed from CONFIG_DEFAULTS
- Settings UI has no branch template input
- Task detail panel uses fixed `task/${task.id}` branch format
- All 6 i18n branchTemplate keys removed, 2 desc keys updated
- Zero grep hits for branchTemplate across src/ and tests/
- Build succeeds, all remaining tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-schema-cleanup/15-02-SUMMARY.md`
</output>
