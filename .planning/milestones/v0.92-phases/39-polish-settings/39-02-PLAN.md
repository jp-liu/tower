---
phase: 39-polish-settings
plan: 02
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - src/components/assistant/assistant-panel.tsx
  - src/components/layout/layout-client.tsx
autonomous: true
requirements: [UX-03]

must_haves:
  truths:
    - "Sidebar mode adapts width between 320px and 480px based on viewport"
    - "Dialog mode adapts width to 90vw capped at 600px"
    - "No content overflow or truncation at 1024px viewport width"
    - "No content overflow or truncation at 2560px viewport width"
  artifacts:
    - path: "src/components/assistant/assistant-panel.tsx"
      provides: "Responsive sidebar width classes"
      contains: "min-w-"
    - path: "src/components/layout/layout-client.tsx"
      provides: "Responsive dialog width"
      contains: "90vw"
  key_links:
    - from: "src/components/assistant/assistant-panel.tsx"
      to: "layout-client.tsx sidebar rendering"
      via: "flex sibling in layout"
      pattern: "min-w-\\[320px\\]"
    - from: "src/components/layout/layout-client.tsx"
      to: "DialogContent styles"
      via: "inline style maxWidth"
      pattern: "maxWidth.*600"
---

<objective>
Make assistant panel responsive across viewport sizes from 1024px to 2560px.

Purpose: Both sidebar and dialog modes render correctly without overflow or truncation on all target viewports (UX-03).
Output: Updated width classes in assistant-panel.tsx and dialog styles in layout-client.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-polish-settings/39-01-SUMMARY.md

@src/components/assistant/assistant-panel.tsx
@src/components/layout/layout-client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply responsive width classes to sidebar and dialog modes</name>
  <files>src/components/assistant/assistant-panel.tsx, src/components/layout/layout-client.tsx</files>
  <read_first>src/components/assistant/assistant-panel.tsx, src/components/layout/layout-client.tsx</read_first>
  <action>
1. In `src/components/assistant/assistant-panel.tsx`:
- Change the sidebar containerClass from `w-[420px]` to `min-w-[320px] max-w-[480px] w-[30vw]`
- This makes the sidebar 30% of viewport width, but clamped between 320px (readable minimum) and 480px (maximum to avoid consuming too much space)
- Keep all other classes unchanged: `shrink-0 border-r border-border flex flex-col bg-popover overflow-hidden`
- The dialog mode container class stays as-is (the dialog sizing is handled by DialogContent in layout-client.tsx)

2. In `src/components/layout/layout-client.tsx`:
- Update the DialogContent `style` prop to use responsive width:
  - Change `maxWidth: "600px"` to `maxWidth: "600px", width: "90vw"` so the dialog shrinks on smaller viewports
  - Keep existing `height: "70vh"`, `minHeight: "480px"`, `maxHeight: "800px"`, `padding: 0`
- Also add `minWidth: "360px"` to prevent the dialog from becoming too narrow

3. Verify no horizontal overflow occurs:
- At 1024px viewport: sidebar = 30vw = ~307px, clamped to min 320px. Main content gets ~704px. Adequate.
- At 1440px viewport: sidebar = 30vw = 432px. Main content gets ~1008px. Good.
- At 2560px viewport: sidebar = 30vw = 768px, clamped to max 480px. Main content gets ~2080px. Good.
- Dialog at 1024px: 90vw = ~922px, capped at 600px. Good.
- Dialog at 2560px: 90vw = ~2304px, capped at 600px. Good.
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && grep "min-w-\[320px\]" src/components/assistant/assistant-panel.tsx && grep "90vw" src/components/layout/layout-client.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "min-w-\[320px\]" src/components/assistant/assistant-panel.tsx returns a match
    - grep "max-w-\[480px\]" src/components/assistant/assistant-panel.tsx returns a match
    - grep "w-\[30vw\]" src/components/assistant/assistant-panel.tsx returns a match
    - grep "w-\[420px\]" src/components/assistant/assistant-panel.tsx returns 0 matches (old fixed width removed)
    - grep "90vw" src/components/layout/layout-client.tsx returns a match
    - pnpm build succeeds without errors
  </acceptance_criteria>
  <done>Sidebar uses responsive min-w/max-w/w-[30vw] instead of fixed 420px; dialog uses 90vw capped at 600px with 360px minimum; no overflow or truncation at any viewport between 1024px and 2560px</done>
</task>

</tasks>

<verification>
1. `grep "w-\[420px\]" src/components/assistant/assistant-panel.tsx` returns no matches (fixed width replaced)
2. `grep "min-w-\[320px\]" src/components/assistant/assistant-panel.tsx` returns a match
3. `grep "90vw" src/components/layout/layout-client.tsx` returns a match
4. `pnpm build` completes without errors
</verification>

<success_criteria>
- Sidebar mode: width adapts between 320px-480px based on 30vw
- Dialog mode: width is 90vw capped at 600px with 360px minimum
- No horizontal overflow or content truncation at 1024px, 1440px, or 2560px viewports
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/39-polish-settings/39-02-SUMMARY.md`
</output>
