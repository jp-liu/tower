---
phase: 39-polish-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/settings/general-config.tsx
  - src/lib/i18n.tsx
  - src/components/assistant/assistant-panel.tsx
  - src/components/assistant/assistant-chat.tsx
  - src/components/assistant/assistant-chat-bubble.tsx
autonomous: true
requirements: [UI-05, UI-07]

must_haves:
  truths:
    - "Users can switch between terminal and chat mode via a select in Settings > General"
    - "The communication mode setting persists in SystemConfig and takes effect on next assistant open"
    - "All assistant UI text renders in Chinese when locale is zh"
    - "All assistant UI text renders in English when locale is en"
  artifacts:
    - path: "src/components/settings/general-config.tsx"
      provides: "Communication mode select"
      contains: "assistant.communicationMode"
    - path: "src/lib/i18n.tsx"
      provides: "All assistant i18n keys"
      contains: "assistant.title"
    - path: "src/components/assistant/assistant-panel.tsx"
      provides: "i18n-ified panel title and aria labels"
      contains: "t(\"assistant."
    - path: "src/components/assistant/assistant-chat.tsx"
      provides: "i18n-ified chat empty state and input"
      contains: "t(\"assistant."
    - path: "src/components/assistant/assistant-chat-bubble.tsx"
      provides: "i18n-ified bubble aria labels and tool badge"
      contains: "t(\"assistant."
  key_links:
    - from: "src/components/settings/general-config.tsx"
      to: "assistant.communicationMode config key"
      via: "setConfigValue"
      pattern: "setConfigValue.*assistant\\.communicationMode"
    - from: "src/components/assistant/assistant-provider.tsx"
      to: "assistant.communicationMode config key"
      via: "getConfigValue on mount"
      pattern: "getConfigValue.*assistant\\.communicationMode"
---

<objective>
Add communication mode setting to Settings > General and migrate all hardcoded assistant strings to i18n.

Purpose: Users can configure terminal vs chat mode (UI-05) and see all assistant UI text in their preferred language (UI-07).
Output: Updated general-config.tsx with select, i18n.tsx with assistant keys, all assistant components using t() calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/settings/general-config.tsx
@src/lib/i18n.tsx
@src/components/assistant/assistant-panel.tsx
@src/components/assistant/assistant-chat.tsx
@src/components/assistant/assistant-chat-bubble.tsx
@src/components/assistant/assistant-provider.tsx
@.claude/rules/ui.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add assistant i18n keys and communication mode setting</name>
  <files>src/lib/i18n.tsx, src/components/settings/general-config.tsx</files>
  <read_first>src/lib/i18n.tsx, src/components/settings/general-config.tsx, src/components/assistant/assistant-provider.tsx</read_first>
  <action>
1. In `src/lib/i18n.tsx`, add the following keys to BOTH `zh` and `en` translation objects:

**zh keys:**
```
"assistant.title": "Tower ??????"
"assistant.iconLabel": "??????"
"assistant.starting": "???..."
"assistant.errorTitle": "????????????"
"assistant.errorBody": "?????????????????????"
"assistant.emptyTitle": "???????????????"
"assistant.emptyBody": "????????????????"
"assistant.inputPlaceholder": "?????????..."
"assistant.sendLabel": "????????????"
"assistant.thinking": "?????????"
"assistant.toolLabel": "??????"
"assistant.parseError": "????????????"
"assistant.parseErrorBody": "??????????????????????????"
"assistant.closeLabel": "????????????"
"assistant.expandTool": "????????????????????????"
"settings.assistant.title": "??????"
"settings.assistant.desc": "???????????????????????????"
"settings.assistant.communicationMode": "????????????"
"settings.assistant.communicationModeDesc": "????????????????????????????????????"
"settings.assistant.modeTerminal": "????????????"
"settings.assistant.modeChat": "????????????"
```

**en keys:**
```
"assistant.title": "Tower Assistant"
"assistant.iconLabel": "Assistant"
"assistant.starting": "Starting..."
"assistant.errorTitle": "Session failed"
"assistant.errorBody": "Failed to start. Try again."
"assistant.emptyTitle": "Chat with Assistant"
"assistant.emptyBody": "Type a message to start"
"assistant.inputPlaceholder": "Type a message..."
"assistant.sendLabel": "Send message"
"assistant.thinking": "Thinking"
"assistant.toolLabel": "Tool"
"assistant.parseError": "Parse error"
"assistant.parseErrorBody": "Could not parse assistant output"
"assistant.closeLabel": "Close assistant"
"assistant.expandTool": "Expand tool call details"
"settings.assistant.title": "Assistant"
"settings.assistant.desc": "Configure assistant behavior"
"settings.assistant.communicationMode": "Communication Mode"
"settings.assistant.communicationModeDesc": "Choose how to interact with the assistant"
"settings.assistant.modeTerminal": "Terminal"
"settings.assistant.modeChat": "Chat"
```

2. In `src/components/settings/general-config.tsx`:
- Import `Select, SelectTrigger, SelectContent, SelectItem` from `@/components/ui/select`
- Add state: `const [commMode, setCommMode] = useState<"terminal" | "chat">("terminal")`
- In useEffect, load: `getConfigValue<string>("assistant.communicationMode", "terminal").then((v) => setCommMode(v === "chat" ? "chat" : "terminal"))`
- Add save handler: `async function handleSaveCommMode(value: string) { const mode = value === "chat" ? "chat" : "terminal"; setCommMode(mode); await setConfigValue("assistant.communicationMode", mode); }`
- Add a new section AFTER the Language section and BEFORE the Terminal app section with:
  - h3: `t("settings.assistant.communicationMode")`
  - p description: `t("settings.assistant.communicationModeDesc")`
  - Select with value={commMode}, onValueChange={handleSaveCommMode}
  - Per ui.md Select rules: use manual span inside SelectTrigger showing `commMode === "chat" ? t("settings.assistant.modeChat") : t("settings.assistant.modeTerminal")`
  - Two SelectItems: value="terminal" label={t("settings.assistant.modeTerminal")}, value="chat" label={t("settings.assistant.modeChat")}
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && grep -c "assistant\." src/lib/i18n.tsx | head -1</automated>
  </verify>
  <acceptance_criteria>
    - grep "assistant.communicationMode" src/components/settings/general-config.tsx returns matches
    - grep "assistant.title" src/lib/i18n.tsx returns matches in both zh and en sections
    - grep "settings.assistant.communicationMode" src/lib/i18n.tsx returns matches
    - grep "SelectTrigger" src/components/settings/general-config.tsx returns matches
  </acceptance_criteria>
  <done>i18n keys added for all assistant strings in both locales; communication mode select appears in Settings > General between Language and Terminal sections</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded strings in assistant components with t() calls</name>
  <files>src/components/assistant/assistant-panel.tsx, src/components/assistant/assistant-chat.tsx, src/components/assistant/assistant-chat-bubble.tsx</files>
  <read_first>src/components/assistant/assistant-panel.tsx, src/components/assistant/assistant-chat.tsx, src/components/assistant/assistant-chat-bubble.tsx, src/lib/i18n.tsx</read_first>
  <action>
1. In `src/components/assistant/assistant-panel.tsx`:
- Import `useI18n` from `@/lib/i18n`
- Add `const { t } = useI18n();` inside the AssistantPanel component
- Replace `"Tower Assistant"` with `{t("assistant.title")}`
- Replace `aria-label="Close assistant"` with `aria-label={t("assistant.closeLabel")}`
- Replace `"Starting..."` text with `{t("assistant.starting")}`

2. In `src/components/assistant/assistant-chat.tsx`:
- Import `useI18n` from `@/lib/i18n`
- Add `const { t } = useI18n();` inside AssistantChat component
- Pass `t` to `EmptyState` component as a prop, or move `useI18n` into EmptyState
- In EmptyState: replace `"Chat with Assistant"` with `{t("assistant.emptyTitle")}`, replace `"Type a message to start"` with `{t("assistant.emptyBody")}`
- Replace `placeholder="Type a message..."` with `placeholder={t("assistant.inputPlaceholder")}`
- Replace `aria-label="Send message"` with `aria-label={t("assistant.sendLabel")}`
- Remove all `// TODO Phase 39: i18n` comments

3. In `src/components/assistant/assistant-chat-bubble.tsx`:
- Import `useI18n` from `@/lib/i18n`
- Add `const { t } = useI18n();` inside AssistantChatBubble component
- Pass `t` down to sub-components via props, OR use useI18n() directly in ThinkingBubble and ToolBubble
- In ThinkingBubble: replace `aria-label="Thinking"` with `aria-label={t("assistant.thinking")}`
- In ToolBubble: replace the fallback `"Tool"` in `const displayName = toolName ?? t("assistant.toolLabel")`, replace the badge text `"Tool"` with `{t("assistant.toolLabel")}`, replace `aria-label="Expand tool call details"` with `aria-label={t("assistant.expandTool")}`
- Remove all `// TODO Phase 39: i18n` comments

Note: For sub-components like ThinkingBubble and ToolBubble that need `t`, the simplest approach is to call `useI18n()` at the top of each sub-component since they are rendered within I18nProvider. This avoids prop drilling.
  </action>
  <verify>
    <automated>cd /Users/liujunping/project/i/ai-manager && grep -r "TODO Phase 39" src/components/assistant/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep "TODO Phase 39" src/components/assistant/ returns 0 matches
    - grep 't("assistant.' src/components/assistant/assistant-panel.tsx returns at least 3 matches
    - grep 't("assistant.' src/components/assistant/assistant-chat.tsx returns at least 3 matches
    - grep 't("assistant.' src/components/assistant/assistant-chat-bubble.tsx returns at least 3 matches
    - grep "useI18n" src/components/assistant/assistant-panel.tsx returns a match
  </acceptance_criteria>
  <done>All hardcoded English strings in assistant components replaced with t() calls; no TODO Phase 39 comments remain; UI renders in Chinese or English based on locale setting</done>
</task>

</tasks>

<verification>
1. `grep -r "TODO Phase 39" src/components/assistant/` returns 0 results
2. `grep "assistant.communicationMode" src/components/settings/general-config.tsx` finds the config key usage
3. `grep -c "assistant\." src/lib/i18n.tsx` shows i18n keys were added (should be 30+ matches covering zh and en)
4. `pnpm build` completes without type errors
</verification>

<success_criteria>
- Communication mode select visible in Settings > General, persists to SystemConfig
- All assistant UI text uses t() calls, renders correctly in both zh and en locales
- No hardcoded English strings remain in assistant components
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/39-polish-settings/39-01-SUMMARY.md`
</output>
