---
phase: 25-xterm-terminal-component
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/i18n.tsx
autonomous: true
requirements:
  - TERM-01
  - TERM-02
  - TERM-03
  - TERM-04

must_haves:
  truths:
    - "@xterm/addon-attach is in package.json dependencies"
    - "@xterm/addon-webgl is in package.json dependencies"
    - "i18n has terminal connection status keys in both zh and en locales"
  artifacts:
    - path: "package.json"
      provides: "xterm addon dependencies installed"
      contains: "@xterm/addon-attach"
    - path: "src/lib/i18n.tsx"
      provides: "i18n keys for terminal component"
      contains: "terminal.connecting"
  key_links:
    - from: "src/components/task/task-terminal.tsx"
      to: "package.json"
      via: "import @xterm/addon-attach"
      pattern: "@xterm/addon-attach"
---

<objective>
Install missing xterm.js addons and add i18n keys needed by the terminal component.

Purpose: TaskTerminal (Plan 02) depends on @xterm/addon-attach for WebSocket↔xterm piping and @xterm/addon-webgl for GPU rendering. The i18n keys provide connection status labels used in the component UI.
Output: package.json updated with two new deps; src/lib/i18n.tsx has terminal.* keys in both locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-xterm-terminal-component/25-CONTEXT.md

<interfaces>
<!-- Existing xterm packages already in package.json -->
"@xterm/addon-fit": "^0.11.0"
"@xterm/xterm": "^6.0.0"

<!-- New packages to add (per D-02 in CONTEXT.md) -->
"@xterm/addon-attach": "^0.12.0"   — Official WebSocket↔xterm pipe bridge
"@xterm/addon-webgl": latest       — GPU-accelerated renderer for high-volume PTY output

<!-- i18n pattern from src/lib/i18n.tsx (TranslationKey union must include new keys) -->
// Existing pattern — add under "zh" and "en" blocks, after preview keys:
"terminal.connecting": "正在连接...",
"terminal.connected": "已连接",
"terminal.disconnected": "已断开",
"terminal.reconnecting": "正在重连...",
"terminal.noWorktree": "无工作区",
"terminal.noWorktreeDesc": "请先运行任务以启动终端",
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @xterm/addon-attach and @xterm/addon-webgl</name>
  <files>package.json</files>
  <read_first>
    - package.json: check current @xterm versions and existing devDependencies structure
  </read_first>
  <action>
Run:
```
pnpm add @xterm/addon-attach @xterm/addon-webgl
```

Verify the install succeeded by checking node_modules contains both packages. No code changes — package.json and pnpm-lock.yaml are updated automatically.

Note: @xterm/addon-attach ^0.12.0 is the official WebSocket bridge addon (per D-02 in CONTEXT.md). @xterm/addon-webgl provides GPU-accelerated canvas rendering, which is important for high-volume Claude CLI output.

Do NOT add @xterm/addon-serialize or @xterm/addon-search — these are deferred to v0.8+ per REQUIREMENTS.md.
  </action>
  <verify>
    <automated>node -e "require('@xterm/addon-attach'); require('@xterm/addon-webgl'); console.log('ok')"</automated>
  </verify>
  <done>Both packages importable from node_modules; package.json lists @xterm/addon-attach and @xterm/addon-webgl in dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Add terminal i18n keys to both zh and en locales</name>
  <files>src/lib/i18n.tsx</files>
  <read_first>
    - src/lib/i18n.tsx: read the full file to find the zh and en translation blocks and the TranslationKey union
  </read_first>
  <action>
In `src/lib/i18n.tsx`, add the following keys to both the `zh` and `en` translation objects. Place them after the existing `"settings.terminal.*"` keys (around line 415 for zh, line 806 for en).

Chinese (zh) block — add after `"settings.terminal.placeholder": "Terminal"`:
```
// Terminal component (Phase 25)
"terminal.connecting": "正在连接...",
"terminal.connected": "已连接",
"terminal.disconnected": "已断开",
"terminal.reconnecting": "正在重连...",
"terminal.noWorktree": "无工作区",
"terminal.noWorktreeDesc": "请先运行任务以启动终端",
```

English (en) block — add after `"settings.terminal.placeholder": "Terminal"`:
```
// Terminal component (Phase 25)
"terminal.connecting": "Connecting...",
"terminal.connected": "Connected",
"terminal.disconnected": "Disconnected",
"terminal.reconnecting": "Reconnecting...",
"terminal.noWorktree": "No Worktree",
"terminal.noWorktreeDesc": "Run the task first to start the terminal",
```

The i18n system uses a `TranslationKey = keyof typeof translations.zh` union — adding keys to the `zh` block automatically extends the type. No manual type update needed.
  </action>
  <verify>
    <automated>grep -c "terminal.connecting" src/lib/i18n.tsx</automated>
  </verify>
  <done>grep shows "terminal.connecting" appears twice (once in zh, once in en); TypeScript compiler accepts the keys without error (run `pnpm tsc --noEmit` to confirm).</done>
</task>

</tasks>

<verification>
1. `node -e "require('@xterm/addon-attach'); require('@xterm/addon-webgl'); console.log('ok')"` prints "ok"
2. `grep "terminal.connecting" src/lib/i18n.tsx | wc -l` returns 2
3. `pnpm tsc --noEmit` exits 0 (no TypeScript errors from new i18n keys)
</verification>

<success_criteria>
- @xterm/addon-attach and @xterm/addon-webgl are installed and importable
- Six terminal.* i18n keys exist in both zh and en locales
- TypeScript build passes with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-xterm-terminal-component/25-01-SUMMARY.md`
</output>
