# Tower 不通过 .env 选择数据目录 —— 不要在这里放 TOWER_DATA_DIR / DATABASE_URL。
#
# 运行时 DB 位置由 getTowerDbPath() 从数据根目录派生（src/lib/tower-dir.ts），
# 数据根目录的选择方式：
#   - 本地开发：`pnpm dev` / `pnpm db:*` 脚本已显式指向 ~/.tower-dev
#   - 生产启动：`pnpm start`（bin/tower.mjs）pin 到 ~/.tower（或自定义 TOWER_DATA_DIR）
#   - 默认：~/.tower
# 把 TOWER_DATA_DIR 写进 .env 会被 Prisma 运行时自动加载并串进所有进程（含 MCP），
# 详见 .env 顶部说明。

# Set to "true" to allow Claude CLI to run without permission prompts
# WARNING: Only enable in trusted local environments
# CLAUDE_SKIP_PERMISSIONS=true
