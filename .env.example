# SQLite database (Prisma) — auto-configured by `tower` CLI to ~/.tower/database/tower.db
# For development without CLI, use a local path:
DATABASE_URL="file:./dev.db"

# Set to "true" to allow Claude CLI to run without permission prompts
# WARNING: Only enable in trusted local environments
# CLAUDE_SKIP_PERMISSIONS=true
