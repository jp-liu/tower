# I18n 模块

**Slug:** `i18n`

## 概述

国际化系统，支持中文和英文双语。所有用户可见文本使用 `t("key")` 函数。

## 文件清单

### 核心库 (`src/lib/i18n/`)

i18n 配置和资源管理。

### 使用

- `src/lib/i18n.tsx` — i18n Provider 和 hook

## 规范

- 所有用户可见文本必须用 `t("key")` 包裹
- 同时维护 zh/en 两套翻译
- Key 命名格式：`模块.功能.描述`
