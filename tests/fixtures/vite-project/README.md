# Preview E2E Fixture

Minimal Vite project used by `tests/e2e/preview.spec.ts`.

Before running full E2E, install dependencies:

```bash
cd tests/fixtures/vite-project
pnpm install
```

Currently the e2e spec is `.skip`ped — remove the skip to enable when fixture and Tower dev server are running.
